{
  "version": "1.0",
  "exit_key": "0",
  "banner": [
    "=============================================",
    "🔧  工具中枢 v1.0",
    "============================================="
  ],
  "menu": [
    "  1. 添加新功能",
    "  2. 运行功能",
    "  3. 修改功能",
    "  4. 删除功能",
    "  5. 搜索功能",
    "  6. 快照管理",
    "  7. 修改目标",
    "  8. 生成AI上下文",
    "  9. 查看功能详情"
  ],
  "footer": [
    "  0. 退出",
    "============================================="
  ],
  "goal": "我的个人工具中枢",
  "log": [],
  "actions": {
    "1": {
      "name": "添加新功能",
      "desc": "粘贴带备忘录的代码，加入中枢",
      "code": "\nimport re, shutil, traceback\nfrom datetime import datetime\n\nMEMO_FIELDS = [\"功能名称\",\"原始需求\",\"设计思路\",\"注意事项\",\"后续优化\"]\n\ndef _parse_memo(code):\n    m = re.search(r'#\\s*@AI_MEMO_START(.*?)#\\s*@AI_MEMO_END', code, re.DOTALL)\n    if not m: return None\n    memo = {}\n    for line in m.group(1).splitlines():\n        line = line.strip().lstrip(\"#\").strip()\n        for f in MEMO_FIELDS:\n            if line.startswith(f+\"：\") or line.startswith(f+\":\"):\n                memo[f] = line.split(\"：\",1)[-1].split(\":\",1)[-1].strip()\n    return memo if memo else None\n\nprint(\"\\n📋 粘贴代码，完成后单独一行输入 END 回车：\")\nlines = []\nwhile True:\n    line = input()\n    if line.strip() == \"END\": break\n    lines.append(line)\ncode = \"\\n\".join(lines)\nif not code.strip():\n    print(\"⚠️  没有代码。\"); input(\"回车继续...\")\nelse:\n    memo = _parse_memo(code)\n    if not memo:\n        print(\"\\n❌ 未检测到备忘录\"); input(\"\\n回车继续...\")\n    else:\n        name = memo.get(\"功能名称\",\"\").strip() or input(\"功能名称：\").strip()\n        print(f\"\\n📋 备忘录：\")\n        for k,v in memo.items(): print(f\"   {k}：{v}\")\n        print(\"\\n代码预览（前8行）：\")\n        for l in code.splitlines()[:8]: print(f\"  {l}\")\n        if input(\"\\n确认添加并执行？(y/n)：\").strip().lower()==\"y\":\n            _snap()\n            try:\n                exec(code, {**CORE_ENV, \"data\": data, \"CORE_ENV\": CORE_ENV})\n                data[\"actions\"][name] = {\"name\":name,\"code\":code,\"memo\":memo,\"desc\":memo.get(\"原始需求\",\"\"),\"added\":__import__('datetime').datetime.now().strftime(\"%Y-%m-%d %H:%M\"),\"history\":[]}\n                data = _log(data, f\"添加「{name}」\")\n                _atomic_save(data)\n                print(f\"\\n✅ 「{name}」已加入中枢。\")\n            except Exception as e:\n                print(f\"\\n❌ 执行出错：{e}\\n{traceback.format_exc()}\")\n            input(\"\\n回车继续...\")\n"
    },
    "2": {
      "name": "运行功能",
      "code": "# @AI_MEMO_START\n# 功能名称：运行功能\n# 原始需求：选择并运行一个已有工具功能\n# 设计思路：用globals()透传CORE_ENV，确保_atomic_save等内核函数传入嵌套exec\n# 注意事项：必须用globals()而不是新字典，否则_atomic_save丢失\n# 后续优化：无\n# @AI_MEMO_END\n\nimport json, traceback\nfrom datetime import datetime\nDATA_FILE = _data_file\n\nwith open(DATA_FILE, encoding=\"utf-8\") as f:\n    data = json.load(f)\n\nfns = {k:v for k,v in data[\"actions\"].items() if k not in [\"1\",\"2\",\"3\",\"4\",\"5\",\"6\",\"7\",\"8\",\"9\"]}\nif not fns:\n    print(\"⚠️  还没有任何工具功能。\"); input(\"回车继续...\")\nelse:\n    items = list(fns.items())\n    print(\"\\n选择要运行的功能：\")\n    for i,(k,v) in enumerate(items, 1):\n        print(f\"  {i}. {v['name']} — {v.get('desc','')[:20]}\")\n    choice = input(\"编号：\").strip()\n    try:\n        key, fn = items[int(choice)-1]\n        print(f\"\\n▶️  运行「{fn['name']}」...\\n\")\n        exec(fn[\"code\"], {**CORE_ENV, \"data\": data, \"CORE_ENV\": CORE_ENV})\n        ts = datetime.now().strftime(\"%m-%d %H:%M\")\n        with open(DATA_FILE, encoding=\"utf-8\") as f:\n            data = json.load(f)\n        data[\"log\"].append(f\"{ts} 运行「{fn['name']}」\")\n        _atomic_save(data, DATA_FILE)\n    except (ValueError, IndexError): print(\"⚠️  无效编号。\")\n    except Exception as e: print(f\"❌ 出错：{e}\\n{traceback.format_exc()}\")\n    input(\"\\n回车继续...\")",
      "memo": {
        "功能名称": "运行功能",
        "原始需求": "选择并运行一个已有工具功能",
        "设计思路": "显示功能名称而不是key，让用户看得懂",
        "注意事项": "工具功能key是10以上的数字",
        "后续优化": "可加功能描述预览"
      },
      "desc": "选择并运行一个已有工具功能",
      "added": "",
      "modified": "2026-03-15 20:40:18",
      "history": [
        {
          "time": "2026-03-10 14:11",
          "code": "\nimport json, traceback\nfrom datetime import datetime\nDATA_FILE = _data_file\ndef _load():\n    with open(DATA_FILE,encoding=\"utf-8\") as f: return json.load(f)\ndef _save(data):\n    import os; tmp=DATA_FILE+\".tmp\"\n    with open(tmp,\"w\",encoding=\"utf-8\") as f: json.dump(data,f,ensure_ascii=False,indent=2)\n    os.replace(tmp,DATA_FILE)\ndata = _load()\nfns = {k:v for k,v in data[\"actions\"].items() if k not in [\"1\",\"2\",\"3\",\"4\",\"5\",\"6\",\"7\",\"8\",\"9\"]}\nif not fns:\n    print(\"⚠️  还没有任何工具功能。\"); input(\"回车继续...\")\nelse:\n    names = list(fns.keys())\n    print(\"\\n选择要运行的功能：\")\n    for i,n in enumerate(names,1): print(f\"  {i}. {n} — {fns[n].get('desc','')[:30]}\")\n    choice = input(\"编号：\").strip()\n    try:\n        name = names[int(choice)-1]\n        print(f\"\\n▶️  运行「{name}」...\\n\")\n        exec(fns[name][\"code\"], {\"__name__\":\"__main__\",\"_data_file\":DATA_FILE,\"_base_dir\":_base_dir})\n        ts = datetime.now().strftime(\"%m-%d %H:%M\")\n        data[\"log\"].append(f\"{ts} 运行「{name}」\")\n        _save(data)\n    except (ValueError,IndexError): print(\"⚠️  无效编号。\")\n    except Exception as e: print(f\"❌ 出错：{e}\\n{traceback.format_exc()}\")\n    input(\"\\n回车继续...\")\n",
          "memo": {}
        },
        {
          "time": "2026-03-15 20:30:26",
          "code": "# @AI_MEMO_START\n# 功能名称：运行功能\n# 原始需求：选择并运行一个已有工具功能\n# 设计思路：用globals()透传CORE_ENV，确保_atomic_save等内核函数传入嵌套exec\n# 注意事项：必须用globals()而不是新字典，否则_atomic_save丢失\n# 后续优化：无\n# @AI_MEMO_END\n\nimport json, traceback\nfrom datetime import datetime\nDATA_FILE = _data_file\n\nwith open(DATA_FILE, encoding=\"utf-8\") as f:\n    data = json.load(f)\n\nfns = {k:v for k,v in data[\"actions\"].items() if k not in [\"1\",\"2\",\"3\",\"4\",\"5\",\"6\",\"7\",\"8\",\"9\"]}\nif not fns:\n    print(\"⚠️  还没有任何工具功能。\"); input(\"回车继续...\")\nelse:\n    items = list(fns.items())\n    print(\"\\n选择要运行的功能：\")\n    for i,(k,v) in enumerate(items, 1):\n        print(f\"  {i}. {v['name']} — {v.get('desc','')[:20]}\")\n    choice = input(\"编号：\").strip()\n    try:\n        key, fn = items[int(choice)-1]\n        print(f\"\\n▶️  运行「{fn['name']}」...\\n\")\n        exec(fn[\"code\"], globals())\n        ts = datetime.now().strftime(\"%m-%d %H:%M\")\n        with open(DATA_FILE, encoding=\"utf-8\") as f:\n            data = json.load(f)\n        data[\"log\"].append(f\"{ts} 运行「{fn['name']}」\")\n        _atomic_save(data, DATA_FILE)\n    except (ValueError, IndexError): print(\"⚠️  无效编号。\")\n    except Exception as e: print(f\"❌ 出错：{e}\\n{traceback.format_exc()}\")\n    input(\"\\n回车继续...\")"
        },
        {
          "time": "2026-03-15 20:40:18",
          "code": "# @AI_MEMO_START\n# 功能名称：运行功能\n# 原始需求：选择并运行一个已有工具功能\n# 设计思路：用globals()透传CORE_ENV，确保_atomic_save等内核函数传入嵌套exec\n# 注意事项：必须用globals()而不是新字典，否则_atomic_save丢失\n# 后续优化：无\n# @AI_MEMO_END\n\nimport json, traceback\nfrom datetime import datetime\nDATA_FILE = _data_file\n\nwith open(DATA_FILE, encoding=\"utf-8\") as f:\n    data = json.load(f)\n\nfns = {k:v for k,v in data[\"actions\"].items() if k not in [\"1\",\"2\",\"3\",\"4\",\"5\",\"6\",\"7\",\"8\",\"9\"]}\nif not fns:\n    print(\"⚠️  还没有任何工具功能。\"); input(\"回车继续...\")\nelse:\n    items = list(fns.items())\n    print(\"\\n选择要运行的功能：\")\n    for i,(k,v) in enumerate(items, 1):\n        print(f\"  {i}. {v['name']} — {v.get('desc','')[:20]}\")\n    choice = input(\"编号：\").strip()\n    try:\n        key, fn = items[int(choice)-1]\n        print(f\"\\n▶️  运行「{fn['name']}」...\\n\")\n        exec(fn[\"code\"], {**globals(), \"data\": data})\n        ts = datetime.now().strftime(\"%m-%d %H:%M\")\n        with open(DATA_FILE, encoding=\"utf-8\") as f:\n            data = json.load(f)\n        data[\"log\"].append(f\"{ts} 运行「{fn['name']}」\")\n        _atomic_save(data, DATA_FILE)\n    except (ValueError, IndexError): print(\"⚠️  无效编号。\")\n    except Exception as e: print(f\"❌ 出错：{e}\\n{traceback.format_exc()}\")\n    input(\"\\n回车继续...\")"
        }
      ]
    },
    "3": {
      "name": "修改功能",
      "desc": "修改任意功能代码，包括系统功能，保留历史版本",
      "code": "\nimport json, os, re, shutil, traceback\nfrom datetime import datetime\nDATA_FILE = _data_file\nBASE_DIR = _base_dir\nSNAP_DIR = os.path.join(BASE_DIR,\"zhongqu_snapshots\")\nMEMO_FIELDS = [\"功能名称\",\"原始需求\",\"设计思路\",\"注意事项\",\"后续优化\"]\nSYSTEM_KEYS = [\"1\",\"2\",\"3\",\"4\",\"5\",\"6\",\"7\",\"8\",\"9\"]\n\ndef _load():\n    with open(DATA_FILE,encoding=\"utf-8\") as f: return json.load(f)\ndef _save(d):\n    tmp=DATA_FILE+\".tmp\"\n    with open(tmp,\"w\",encoding=\"utf-8\") as f: json.dump(d,f,ensure_ascii=False,indent=2)\n    os.replace(tmp,DATA_FILE)\ndef _snap():\n    os.makedirs(SNAP_DIR,exist_ok=True)\n    ts=datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n    if os.path.exists(DATA_FILE): shutil.copy2(DATA_FILE,os.path.join(SNAP_DIR,f\"snapshot_{ts}.json\"))\ndef _parse_memo(code):\n    m=re.search(r'#\\s*@AI_MEMO_START(.*?)#\\s*@AI_MEMO_END',code,re.DOTALL)\n    if not m: return None\n    memo={}\n    for line in m.group(1).splitlines():\n        line=line.strip().lstrip(\"#\").strip()\n        for f in MEMO_FIELDS:\n            if line.startswith(f+\"：\") or line.startswith(f+\":\"):\n                memo[f]=line.split(\"：\",1)[-1].split(\":\",1)[-1].strip()\n    return memo if memo else None\n\ndata=_load()\nall_items=list(data[\"actions\"].items())\nsys_fns=[(k,v) for k,v in all_items if k in SYSTEM_KEYS]\ntool_fns=[(k,v) for k,v in all_items if k not in SYSTEM_KEYS]\n\nprint(\"\\n选择要修改的功能：\")\nprint(\"  ── 系统功能 ──\")\nfor k,v in sys_fns:\n    print(f\"  {k}. {v['name']}\")\nprint(\"  ── 工具功能 ──\")\nif not tool_fns:\n    print(\"  （暂无工具功能）\")\nelse:\n    for i,(k,v) in enumerate(tool_fns,1):\n        print(f\"  t{i}. {v['name']}\")\n\nchoice=input(\"\\n编号（系统功能输数字，工具功能输t+数字）：\").strip()\n\nkey=None\nif choice in SYSTEM_KEYS:\n    key=choice\nelif choice.startswith(\"t\"):\n    try:\n        key=tool_fns[int(choice[1:])-1][0]\n    except (ValueError,IndexError): pass\n\nif not key:\n    print(\"⚠️  无效编号。\"); input(\"回车继续...\"); exit()\n\nname=data[\"actions\"][key][\"name\"]\nif key in SYSTEM_KEYS:\n    print(f\"\\n⚠️  你正在修改系统功能「{name}」，请确保代码正确。\")\n\nprint(f\"\\n粘贴新代码，输入 END 结束：\")\nlines=[]\nwhile True:\n    line=input()\n    if line.strip()==\"END\": break\n    lines.append(line)\ncode=\"\\n\".join(lines)\n\nif not code.strip():\n    print(\"⚠️  没有代码。\"); input(\"回车继续...\")\nelse:\n    memo=_parse_memo(code)\n    if not memo:\n        print(\"❌ 缺少备忘录，无法保存。\"); input(\"回车继续...\")\n    else:\n        _snap()\n        old=data[\"actions\"][key]\n        hist=old.get(\"history\",[])\n        hist.append({\"time\":datetime.now().strftime(\"%Y-%m-%d %H:%M\"),\"code\":old[\"code\"],\"memo\":old.get(\"memo\",{})})\n        try:\n            data[\"actions\"][key]={\"name\":name,\"code\":code,\"memo\":memo,\"desc\":memo.get(\"原始需求\",\"\"),\"added\":old.get(\"added\",\"\"),\"modified\":datetime.now().strftime(\"%Y-%m-%d %H:%M\"),\"history\":hist[-10:]}\n            data[\"log\"].append(f\"{datetime.now().strftime('%m-%d %H:%M')} 修改「{name}」\")\n            _save(data)\n            print(f\"\\n✅ 「{name}」已更新。\")\n        except Exception as e:\n            print(f\"❌ 出错：{e}\\n{traceback.format_exc()}\")\n        input(\"\\n回车继续...\")\n"
    },
    "4": {
      "name": "删除功能",
      "desc": "删除一个已有功能（自动快照）",
      "code": "\nimport json, os, shutil\nfrom datetime import datetime\nDATA_FILE = _data_file\nBASE_DIR = _base_dir\nSNAP_DIR = os.path.join(BASE_DIR,\"zhongqu_snapshots\")\ndef _load():\n    with open(DATA_FILE,encoding=\"utf-8\") as f: return json.load(f)\ndef _save(data):\n    tmp=DATA_FILE+\".tmp\"\n    with open(tmp,\"w\",encoding=\"utf-8\") as f: json.dump(data,f,ensure_ascii=False,indent=2)\n    os.replace(tmp,DATA_FILE)\ndata=_load()\nfns={k:v for k,v in data[\"actions\"].items() if k not in [\"1\",\"2\",\"3\",\"4\",\"5\",\"6\",\"7\",\"8\",\"9\"]}\nif not fns:\n    print(\"⚠️  没有可删除的功能。\"); input(\"回车继续...\")\nelse:\n    names=list(fns.keys())\n    print(\"\\n选择要删除的功能：\")\n    for i,n in enumerate(names,1): print(f\"  {i}. {n}\")\n    choice=input(\"编号：\").strip()\n    try:\n        name=names[int(choice)-1]\n        if input(f\"确认删除「{name}」？(y/n)：\").strip().lower()==\"y\":\n            os.makedirs(SNAP_DIR,exist_ok=True)\n            ts=datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n            if os.path.exists(DATA_FILE): shutil.copy2(DATA_FILE,os.path.join(SNAP_DIR,f\"snapshot_{ts}.json\"))\n            del data[\"actions\"][name]\n            ts2=datetime.now().strftime(\"%m-%d %H:%M\")\n            data[\"log\"].append(f\"{ts2} 删除「{name}」\")\n            _save(data); print(f\"✅ 已删除「{name}」\")\n    except (ValueError,IndexError): print(\"⚠️  无效编号。\")\n    input(\"\\n回车继续...\")\n"
    },
    "5": {
      "name": "搜索功能",
      "desc": "按关键词搜索已有功能",
      "code": "\nimport json\nDATA_FILE = _data_file\nwith open(DATA_FILE,encoding=\"utf-8\") as f: data=json.load(f)\nkw=input(\"\\n输入关键词：\").strip()\nif kw:\n    results=[(n,info) for n,info in data[\"actions\"].items() if n not in [\"1\",\"2\",\"3\",\"4\",\"5\",\"6\",\"7\",\"8\",\"9\"] and kw in n+\" \"+\" \".join(str(v) for v in info.get(\"memo\",{}).values())]\n    if results:\n        print(f\"\\n找到 {len(results)} 个：\")\n        for n,info in results: print(f\"  · [{n}] {info.get('desc','')[:40]}\")\n    else: print(\"没有找到相关功能。\")\ninput(\"\\n回车继续...\")\n"
    },
    "6": {
      "name": "快照管理",
      "desc": "创建、查看、恢复快照",
      "code": "\nimport json, os, shutil\nfrom datetime import datetime\nDATA_FILE = _data_file\nBASE_DIR = _base_dir\nSNAP_DIR = os.path.join(BASE_DIR,\"zhongqu_snapshots\")\ndef _load():\n    with open(DATA_FILE,encoding=\"utf-8\") as f: return json.load(f)\ndef _save(data):\n    tmp=DATA_FILE+\".tmp\"\n    with open(tmp,\"w\",encoding=\"utf-8\") as f: json.dump(data,f,ensure_ascii=False,indent=2)\n    os.replace(tmp,DATA_FILE)\ndef _list():\n    if not os.path.exists(SNAP_DIR): return []\n    return sorted([f for f in os.listdir(SNAP_DIR) if f.endswith(\".json\")],reverse=True)\nprint(\"\\n快照管理：\")\nprint(\"  1. 创建快照  2. 查看快照  3. 恢复快照\")\nc=input(\"选择：\").strip()\nif c==\"1\":\n    os.makedirs(SNAP_DIR,exist_ok=True)\n    ts=datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n    if os.path.exists(DATA_FILE):\n        shutil.copy2(DATA_FILE,os.path.join(SNAP_DIR,f\"snapshot_{ts}.json\"))\n        print(f\"✅ 快照已保存：snapshot_{ts}.json\")\nelif c==\"2\":\n    files=_list()\n    if not files: print(\"暂无快照。\")\n    else:\n        for i,f in enumerate(files,1): print(f\"  {i}. {f}\")\nelif c==\"3\":\n    files=_list()\n    if not files: print(\"暂无快照。\")\n    else:\n        for i,f in enumerate(files,1): print(f\"  {i}. {f}\")\n        try:\n            name=files[int(input(\"恢复哪个？编号：\").strip())-1]\n            if input(f\"确认恢复「{name}」？当前数据会被覆盖(y/n)：\").strip().lower()==\"y\":\n                with open(os.path.join(SNAP_DIR,name),encoding=\"utf-8\") as f: restored=json.load(f)\n                _save(restored); print(\"✅ 恢复成功，请重启中枢。\"); exit(0)\n        except (ValueError,IndexError): print(\"⚠️  无效编号。\")\ninput(\"\\n回车继续...\")\n"
    },
    "7": {
      "name": "修改目标",
      "desc": "修改中枢的当前目标描述",
      "code": "\nimport json, os\nfrom datetime import datetime\nDATA_FILE = _data_file\nwith open(DATA_FILE,encoding=\"utf-8\") as f: data=json.load(f)\nprint(f\"\\n当前目标：{data.get('goal','未设定')}\")\nnew=input(\"输入新目标（回车保持不变）：\").strip()\nif new:\n    data[\"goal\"]=new\n    ts=datetime.now().strftime(\"%m-%d %H:%M\")\n    data[\"log\"].append(f\"{ts} 更新目标\")\n    tmp=DATA_FILE+\".tmp\"\n    with open(tmp,\"w\",encoding=\"utf-8\") as f: json.dump(data,f,ensure_ascii=False,indent=2)\n    os.replace(tmp,DATA_FILE)\n    print(\"✅ 已更新。\")\ninput(\"\\n回车继续...\")\n"
    },
    "8": {
      "name": "生成AI上下文",
      "code": "# @AI_MEMO_START\n# 功能名称：生成AI上下文\n# 原始需求：生成给AI看的上下文，支持按日期筛选，带统计信息\n# 设计思路：所有导出模式都显示统计信息（总条数/最新时间）\n# 注意事项：日期筛选支持模糊匹配（输入03-13也能匹配）\n# 后续优化：可加时间段导出\n# @AI_MEMO_END\n\nimport json, os\nfrom datetime import datetime\n\ntry:\n    DATA_FILE = _get_data_file()\nexcept NameError:\n    DATA_FILE = os.path.join(os.path.expanduser(\"~\"), \"zhongqu_data.json\")\n\nDOWNLOAD = \"/sdcard/Download\"\n\ndef load():\n    with open(DATA_FILE, encoding=\"utf-8\") as f:\n        return json.load(f)\n\ndef build_context_log(data, date_filter=None):\n    entries = data.get(\"context_log\", [])\n    if not entries:\n        return \"（暂无对话记录）\"\n    total = len(entries)\n    all_times = [e.get('time', '') for e in entries if e.get('time')]\n    latest_all = max(all_times) if all_times else '未知'\n    if date_filter:\n        filtered = [e for e in entries if date_filter in e.get('time', '')]\n        if not filtered:\n            return f\"（{date_filter} 没有对话记录）\"\n        count = len(filtered)\n        times = [e.get('time', '') for e in filtered if e.get('time')]\n        latest = max(times) if times else '未知'\n        header = f\"【对话记录 - {date_filter}】（共 {count} 条，最新：{latest}）\"\n        entries = filtered\n    else:\n        header = f\"【对话记录 - 全部】（共 {total} 条，最新：{latest_all}）\"\n    lines = [header]\n    for e in entries:\n        lines.append(f\"\\n[{e.get('time','')}]\\n{e.get('content','')}\")\n    return \"\\n\".join(lines)\n\ndef build_output(data, include_details=False, date_filter=None):\n    lines = []\n    lines.append(\"=== 工具中枢上下文 ===\")\n    lines.append(f\"当前目标：{data.get('goal', '未设置')}\")\n    lines.append(\"\")\n\n    lines.append(\"【📁 中枢结构】\")\n    lines.append(\"运行在手机Termux里的Python系统：\")\n    lines.append(\"  · zhongqu.py        ← 80行终极内核（永不修改）\")\n    lines.append(\"  · zhongqu_data.json ← 所有功能数据\")\n    lines.append(\"\")\n\n    lines.append(\"【🤖 AI开发规范】\")\n    lines.append(\"1️⃣ 所有功能都能直接调用内核函数：_atomic_save、_snap、_log、_now\")\n    lines.append(\"2️⃣ 菜单规则：只显示1-9系统功能，工具功能不显示\")\n    lines.append(\"3️⃣ JSON结构：action的key是字符串，code字段直接exec\")\n    lines.append(\"4️⃣ 对话记录存在 json顶层 context_log 字段\")\n    lines.append(\"\")\n\n    lines.append(\"【指令协议】\")\n    lines.append(\"新AI收到上下文后，先说「看」确认进入状态，再接受指令。\")\n    lines.append(\"\")\n    lines.append(\"看\")\n    lines.append(\"→ 确认已读取上下文，输出当前状态简报，进入就绪状态\")\n    lines.append(\"\")\n    lines.append(\"存：[名称]\")\n    lines.append(\"→ 把本次对话核心提炼成结构化条目，格式：\")\n    lines.append(\"  ★ [日期] 【主题】\")\n    lines.append(\"  ↳ 核心结论\")\n    lines.append(\"  ↳ 详细说明\")\n    lines.append(\"    · 子要点\")\n    lines.append(\"  ↳ 注意事项\")\n    lines.append(\"→ 严格区分：\")\n    lines.append(\"  · 已验证的事实 → 直接写\")\n    lines.append(\"  · 未解决的问题 → 明确标注「⚠️ 未解决」\")\n    lines.append(\"  · 待验证的结论 → 明确标注「待验证」\")\n    lines.append(\"  · 不用「问题已解决」，用「做了X，测试结果是Y」\")\n    lines.append(\"→ 用户操作：回中枢运行「对话记录」选1粘贴导入\")\n    lines.append(\"\")\n    lines.append(\"写：[名称] 需求：[一句话]\")\n    lines.append(\"→ 生成新功能完整代码，严格要求：\")\n    lines.append(\"  · 含完整备忘录（@AI_MEMO_START...@AI_MEMO_END）\")\n    lines.append(\"  · exec环境兼容（try/except兜底_data_file/_base_dir）\")\n    lines.append(\"  · 使用内核函数（_atomic_save/_snap/_log/_now）\")\n    lines.append(\"→ 用户操作：复制后选1「添加新功能」粘贴\")\n    lines.append(\"\")\n    lines.append(\"改：[功能名] 需求：[一句话]\")\n    lines.append(\"→ 生成修改后完整代码，严格要求同「写」\")\n    lines.append(\"→ 用户操作：复制后选3「修改功能」替换\")\n    lines.append(\"\")\n    lines.append(\"发：[内容]\")\n    lines.append(\"→ 判断内容性质，自动整理成可直接使用的格式输出\")\n    lines.append(\"\")\n    lines.append(\"裁：[分歧描述]\")\n    lines.append(\"→ 1. 列出双方核心主张，不偏不倚\")\n    lines.append(\"→ 2. 分析各自代价（选A失去什么，选B失去什么）\")\n    lines.append(\"→ 3. 给出倾向建议及理由\")\n    lines.append(\"→ 4. 判断权在用户，AI不替人决定\")\n    lines.append(\"\")\n\n    lines.append(\"【功能清单】\")\n    actions = data.get(\"actions\", {})\n    system_keys = [\"1\",\"2\",\"3\",\"4\",\"5\",\"6\",\"7\",\"8\",\"9\"]\n    lines.append(\"系统功能：\" + \"、\".join(\n        f\"{k}.{v['name']}\" for k,v in actions.items() if k in system_keys\n    ))\n    tool_fns = [(k,v) for k,v in actions.items() if k not in system_keys]\n    if tool_fns:\n        lines.append(\"工具功能：\")\n        for k,v in tool_fns:\n            lines.append(f\"  · {v['name']}\")\n    lines.append(\"\")\n\n    lines.append(\"【最近操作】\")\n    for log in data.get(\"log\", [])[-5:]:\n        lines.append(f\"  {log}\")\n    lines.append(\"\")\n\n    lines.append(build_context_log(data, date_filter))\n    lines.append(\"\")\n\n    lines.append(\"=== 上下文结束 ===\")\n\n    if include_details:\n        details = [\"\\n=== 功能详情 ===\\n\"]\n        for k,v in tool_fns:\n            details.append(f\"\\n【{v['name']}】\")\n            for field, val in v.get(\"memo\", {}).items():\n                details.append(f\"  {field}：{val}\")\n            details.append(\"  代码：\")\n            details.append(v.get(\"code\", \"\"))\n        lines.extend(details)\n\n    return \"\\n\".join(lines)\n\ndata = load()\n\nprint(\"\\n导出模式：\")\nprint(\"  1. 摘要版（屏幕显示，全部对话）\")\nprint(\"  2. 按日期导出（输入日期筛选）\")\nprint(\"  3. 完整版（含代码，全部对话）\")\nchoice = input(\"选择：\").strip()\n\nts = datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n\nif choice == \"1\":\n    print(\"\\n\" + build_output(data, include_details=False, date_filter=None))\n    input(\"\\n回车返回...\")\n\nelif choice == \"2\":\n    date = input(\"输入日期（如 2026-03-13，回车显示全部）：\").strip()\n    if not date:\n        date = None\n    content = build_output(data, include_details=False, date_filter=date)\n    path = os.path.join(DOWNLOAD, f\"zhongqu_context_{ts}.txt\")\n    with open(path, \"w\", encoding=\"utf-8\") as f:\n        f.write(content)\n    print(f\"\\n✅ 已导出：zhongqu_context_{ts}.txt\")\n    if date:\n        print(f\"   筛选日期：{date}\")\n    input(\"\\n回车返回...\")\n\nelif choice == \"3\":\n    content = build_output(data, include_details=True, date_filter=None)\n    path = os.path.join(DOWNLOAD, f\"zhongqu_context_{ts}.txt\")\n    with open(path, \"w\", encoding=\"utf-8\") as f:\n        f.write(content)\n    print(f\"\\n✅ 已导出完整版：zhongqu_context_{ts}.txt\")\n    input(\"\\n回车返回...\")\n\nelse:\n    print(\"⚠️ 无效选项\")\n    input(\"回车返回...\")",
      "memo": {
        "功能名称": "生成AI上下文",
        "原始需求": "生成给AI看的上下文，支持按日期筛选，带统计信息",
        "设计思路": "所有导出模式都显示统计信息（总条数/最新时间）",
        "注意事项": "日期筛选支持模糊匹配（输入03-13也能匹配）",
        "后续优化": "可加时间段导出"
      },
      "desc": "生成给AI看的上下文，支持按日期筛选，带统计信息",
      "added": "",
      "modified": "2026-03-15 11:57",
      "history": [
        {
          "time": "2026-03-10 21:37",
          "code": "# @AI_MEMO_START\n# 功能名称：生成AI上下文\n# 原始需求：生成给AI看的上下文，支持屏幕显示和导出txt文件\n# 设计思路：摘要版屏幕显示，完整版导出到/sdcard/Download/供分享给AI\n# 注意事项：用expanduser获取home路径，txt文件名带时间戳不覆盖历史\n# 后续优化：配合「导入整理稿」功能形成完整AI整理循环\n# @AI_MEMO_END\n\nimport json, os\nfrom datetime import datetime\n\nHOME = os.path.expanduser(\"~\")\nDATA_FILE = os.path.join(HOME, \"zhongqu_data.json\")\nDOWNLOAD = \"/sdcard/Download\"\n\ndef load():\n    with open(DATA_FILE, encoding=\"utf-8\") as f:\n        return json.load(f)\n\ndef build_summary(data):\n    lines = []\n    lines.append(\"=== 工具中枢上下文 ===\")\n    lines.append(f\"当前目标：{data.get('goal', '未设置')}\")\n    lines.append(\"\")\n    lines.append(\"【指令协议】\")\n    lines.append(\"新AI收到上下文后，先说「看」确认进入状态，再接受指令。\")\n    lines.append(\"\")\n    lines.append(\"六个指令及AI响应格式：\")\n    lines.append(\"\")\n    lines.append(\"存：[名称]\")\n    lines.append(\"  → AI把本窗口新产生的内容总结成★/↳两层格式的新条目\")\n    lines.append(\"  → AI在本txt的「=== 核心对话记录 ===」末尾追加新条目，输出完整的核心对话记录部分\")\n    lines.append(\"  → 用户把AI输出的内容替换txt里=== 核心对话记录 ===到结尾的部分，保存txt\")\n    lines.append(\"  → 用户回中枢运行「导入AI整理稿」导入更新后的txt\")\n    lines.append(\"\")\n    lines.append(\"写：[名称] 需求：[一句话]\")\n    lines.append(\"  → AI生成一个新功能的完整代码，含备忘录头，用户复制后选1添加进中枢\")\n    lines.append(\"\")\n    lines.append(\"改：[功能名] 需求：[一句话]\")\n    lines.append(\"  → AI输出修改后的完整代码，用户复制后选3修改进中枢\")\n    lines.append(\"\")\n    lines.append(\"发：[内容]\")\n    lines.append(\"  → AI把指定内容整理成可直接发给另一个AI窗口的格式输出\")\n    lines.append(\"\")\n    lines.append(\"裁：[分歧描述]\")\n    lines.append(\"  → AI分析分歧，给出倾向建议，最终由用户决定\")\n    lines.append(\"\")\n    lines.append(\"看\")\n    lines.append(\"  → AI确认已读取上下文，列出当前功能清单，表示进入状态\")\n    lines.append(\"\")\n    lines.append(\"【备忘录格式】（所有「存」「写」「改」指令的输出必须包含）\")\n    lines.append(\"# @AI_MEMO_START\")\n    lines.append(\"# 功能名称：[简短命名]\")\n    lines.append(\"# 原始需求：[用户当时怎么描述的]\")\n    lines.append(\"# 设计思路：[为什么这么实现]\")\n    lines.append(\"# 注意事项：[有什么坑要避免]\")\n    lines.append(\"# 后续优化：[还可以怎么改进]\")\n    lines.append(\"# @AI_MEMO_END\")\n    lines.append(\"\")\n    lines.append(\"[这里写Python代码或文字内容]\")\n    lines.append(\"input(\\\"回车返回...\\\")\")\n    lines.append(\"\")\n    lines.append(\"【功能清单】\")\n    actions = data.get(\"actions\", {})\n    system_keys = [\"1\",\"2\",\"3\",\"4\",\"5\",\"6\",\"7\",\"8\",\"9\"]\n    lines.append(\"系统功能：\" + \"、\".join(\n        f\"{k}.{v['name']}\" for k,v in actions.items() if k in system_keys\n    ))\n    tool_fns = [(k,v) for k,v in actions.items() if k not in system_keys]\n    if tool_fns:\n        lines.append(\"工具功能：\")\n        for k,v in tool_fns:\n            lines.append(f\"  · {v['name']}\")\n    lines.append(\"\")\n    lines.append(\"【最近操作】\")\n    for log in data.get(\"log\", [])[-5:]:\n        lines.append(f\"  {log}\")\n    lines.append(\"\\n=== 上下文结束 ===\")\n    return \"\\n\".join(lines)\n\ndef build_full(data):\n    lines = [build_summary(data)]\n    lines.append(\"\\n=== 功能详情 ===\")\n    actions = data.get(\"actions\", {})\n    system_keys = [\"1\",\"2\",\"3\",\"4\",\"5\",\"6\",\"7\",\"8\",\"9\"]\n    tool_fns = [(k,v) for k,v in actions.items() if k not in system_keys]\n    for k,v in tool_fns:\n        lines.append(f\"\\n【{v['name']}】\")\n        memo = v.get(\"memo\", {})\n        if memo:\n            for field, val in memo.items():\n                lines.append(f\"  {field}：{val}\")\n        lines.append(\"  代码：\")\n        lines.append(v.get(\"code\", \"\"))\n    return \"\\n\".join(lines)\n\ndata = load()\n\nprint(\"\\n导出模式：\")\nprint(\"  1. 摘要版（屏幕显示，直接复制）\")\nprint(\"  2. 完整版（导出txt到下载目录，分享给AI）\")\nchoice = input(\"选择：\").strip()\n\nif choice == \"1\":\n    print(\"\\n\" + build_summary(data))\n    input(\"\\n回车返回...\")\nelif choice == \"2\":\n    content = build_full(data)\n    ts = datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n    path = os.path.join(DOWNLOAD, f\"zhongqu_context_{ts}.txt\")\n    with open(path, \"w\", encoding=\"utf-8\") as f:\n        f.write(content)\n    print(f\"\\n✅ 已导出：zhongqu_context_{ts}.txt\")\n    print(\"去手机下载文件夹找这个文件，直接分享给AI。\")\n    input(\"\\n回车返回...\")\nelse:\n    print(\"⚠️ 无效选项\")\n    input(\"回车返回...\")",
          "memo": {
            "功能名称": "生成AI上下文",
            "原始需求": "生成给AI看的上下文，支持屏幕显示和导出txt文件",
            "设计思路": "摘要版屏幕显示，完整版导出到/sdcard/Download/供分享给AI",
            "注意事项": "用expanduser获取home路径，txt文件名带时间戳不覆盖历史",
            "后续优化": "配合「导入整理稿」功能形成完整AI整理循环"
          }
        },
        {
          "time": "2026-03-11 17:51",
          "code": "# @AI_MEMO_START\n# 功能名称：生成AI上下文\n# 原始需求：生成给AI看的上下文，支持屏幕显示和导出txt文件\n# 设计思路：摘要版屏幕显示，完整版导出到/sdcard/Download/供分享给AI\n# 注意事项：用expanduser获取home路径，txt文件名带时间戳不覆盖历史\n# 后续优化：配合「导入整理稿」功能形成完整AI整理循环\n# @AI_MEMO_END\n\nimport json, os\nfrom datetime import datetime\n\nHOME = os.path.expanduser(\"~\")\nDATA_FILE = os.path.join(HOME, \"zhongqu_data.json\")\nDOWNLOAD = \"/sdcard/Download\"\n\ndef load():\n    with open(DATA_FILE, encoding=\"utf-8\") as f:\n        return json.load(f)\n\ndef build_summary(data):\n    lines = []\n    lines.append(\"=== 工具中枢上下文 ===\")\n    lines.append(f\"当前目标：{data.get('goal', '未设置')}\")\n    lines.append(\"\")\n    lines.append(\"【指令协议】\")\n    lines.append(\"新AI收到上下文后，先说「看」确认进入状态，再接受指令。\")\n    lines.append(\"\")\n    lines.append(\"六个指令及AI响应格式：\")\n    lines.append(\"\")\n    lines.append(\"存：[名称]\")\n    lines.append(\"  → AI把本窗口新产生的内容总结成★/↳两层格式的新条目\")\n    lines.append(\"  → AI输出格式必须以「=== 新增条目 ===」开头，后面跟新条目内容\")\n    lines.append(\"  → 用户新建zhongqu_add.txt，把AI输出内容粘贴进去保存\")\n    lines.append(\"  → 用户回中枢运行「导入AI整理稿」，自动追加到核心对话记录末尾\")\n    lines.append(\"\")\n    lines.append(\"写：[名称] 需求：[一句话]\")\n    lines.append(\"  → AI生成一个新功能的完整代码，含备忘录头，用户复制后选1添加进中枢\")\n    lines.append(\"\")\n    lines.append(\"改：[功能名] 需求：[一句话]\")\n    lines.append(\"  → AI输出修改后的完整代码，用户复制后选3修改进中枢\")\n    lines.append(\"\")\n    lines.append(\"发：[内容]\")\n    lines.append(\"  → AI把指定内容整理成可直接发给另一个AI窗口的格式输出\")\n    lines.append(\"\")\n    lines.append(\"裁：[分歧描述]\")\n    lines.append(\"  → AI分析分歧，给出倾向建议，最终由用户决定\")\n    lines.append(\"\")\n    lines.append(\"看\")\n    lines.append(\"  → AI确认已读取上下文，列出当前功能清单，表示进入状态\")\n    lines.append(\"\")\n    lines.append(\"【备忘录格式】（所有「存」「写」「改」指令的输出必须包含）\")\n    lines.append(\"# @AI_MEMO_START\")\n    lines.append(\"# 功能名称：[简短命名]\")\n    lines.append(\"# 原始需求：[用户当时怎么描述的]\")\n    lines.append(\"# 设计思路：[为什么这么实现]\")\n    lines.append(\"# 注意事项：[有什么坑要避免]\")\n    lines.append(\"# 后续优化：[还可以怎么改进]\")\n    lines.append(\"# @AI_MEMO_END\")\n    lines.append(\"\")\n    lines.append(\"[这里写Python代码或文字内容]\")\n    lines.append(\"input(\\\"回车返回...\\\")\")\n    lines.append(\"\")\n    lines.append(\"【功能清单】\")\n    actions = data.get(\"actions\", {})\n    system_keys = [\"1\",\"2\",\"3\",\"4\",\"5\",\"6\",\"7\",\"8\",\"9\"]\n    lines.append(\"系统功能：\" + \"、\".join(\n        f\"{k}.{v['name']}\" for k,v in actions.items() if k in system_keys\n    ))\n    tool_fns = [(k,v) for k,v in actions.items() if k not in system_keys]\n    if tool_fns:\n        lines.append(\"工具功能：\")\n        for k,v in tool_fns:\n            lines.append(f\"  · {v['name']}\")\n    lines.append(\"\")\n    lines.append(\"【最近操作】\")\n    for log in data.get(\"log\", [])[-5:]:\n        lines.append(f\"  {log}\")\n    lines.append(\"\\n=== 上下文结束 ===\")\n    return \"\\n\".join(lines)\n\ndef build_full(data):\n    lines = [build_summary(data)]\n    lines.append(\"\\n=== 功能详情 ===\")\n    actions = data.get(\"actions\", {})\n    system_keys = [\"1\",\"2\",\"3\",\"4\",\"5\",\"6\",\"7\",\"8\",\"9\"]\n    tool_fns = [(k,v) for k,v in actions.items() if k not in system_keys]\n    for k,v in tool_fns:\n        lines.append(f\"\\n【{v['name']}】\")\n        memo = v.get(\"memo\", {})\n        if memo:\n            for field, val in memo.items():\n                lines.append(f\"  {field}：{val}\")\n        lines.append(\"  代码：\")\n        lines.append(v.get(\"code\", \"\"))\n    return \"\\n\".join(lines)\n\ndata = load()\n\nprint(\"\\n导出模式：\")\nprint(\"  1. 摘要版（屏幕显示，直接复制）\")\nprint(\"  2. 完整版（导出txt到下载目录，分享给AI）\")\nchoice = input(\"选择：\").strip()\n\nif choice == \"1\":\n    print(\"\\n\" + build_summary(data))\n    input(\"\\n回车返回...\")\nelif choice == \"2\":\n    content = build_full(data)\n    ts = datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n    path = os.path.join(DOWNLOAD, f\"zhongqu_context_{ts}.txt\")\n    with open(path, \"w\", encoding=\"utf-8\") as f:\n        f.write(content)\n    print(f\"\\n✅ 已导出：zhongqu_context_{ts}.txt\")\n    print(\"去手机下载文件夹找这个文件，直接分享给AI。\")\n    input(\"\\n回车返回...\")\nelse:\n    print(\"⚠️ 无效选项\")\n    input(\"回车返回...\")",
          "memo": {
            "功能名称": "生成AI上下文",
            "原始需求": "生成给AI看的上下文，支持屏幕显示和导出txt文件",
            "设计思路": "摘要版屏幕显示，完整版导出到/sdcard/Download/供分享给AI",
            "注意事项": "用expanduser获取home路径，txt文件名带时间戳不覆盖历史",
            "后续优化": "配合「导入整理稿」功能形成完整AI整理循环"
          }
        },
        {
          "time": "2026-03-11 18:05",
          "code": "# @AI_MEMO_START\n# 功能名称：生成AI上下文\n# 原始需求：生成给AI看的上下文，包含中枢结构说明和开发规范，让AI第一眼就理解这是Termux程序\n# 设计思路：在摘要版开头加「中枢结构」和「AI开发规范」两部分，把所有坑和约定集中写清楚\n# 注意事项：必须让AI第一眼看到这不是txt程序，而是运行在Termux里的真实系统\n# 后续优化：根据踩坑记录持续补充规范\n# @AI_MEMO_END\n\nimport json, os\nfrom datetime import datetime\n\nHOME = os.path.expanduser(\"~\")\nDATA_FILE = os.path.join(HOME, \"zhongqu_data.json\")\nDOWNLOAD = \"/sdcard/Download\"\n\ndef load():\n    with open(DATA_FILE, encoding=\"utf-8\") as f:\n        return json.load(f)\n\ndef build_summary(data):\n    lines = []\n    lines.append(\"=== 工具中枢上下文 ===\")\n    lines.append(f\"当前目标：{data.get('goal', '未设置')}\")\n    lines.append(\"\")\n    \n    # ===== 中枢结构说明（让AI第一眼看懂）=====\n    lines.append(\"【📁 中枢结构】（重要！先读这里）\")\n    lines.append(\"这不是txt程序，是运行在手机Termux里的Python系统：\")\n    lines.append(\"  · 两个核心文件：\")\n    lines.append(\"    - zhongqu.py      ← 50行不可变内核（永不修改）\")\n    lines.append(\"    - zhongqu_data.json ← 所有功能数据（你生成的代码存这里）\")\n    lines.append(\"  · 你看到的这个txt是「上下文快照」，用来告诉你当前状态\")\n    lines.append(\"  · 你的角色：生成符合规范的代码/文字，由用户手动操作中枢\")\n    lines.append(\"\")\n    \n    # ===== AI开发规范（所有坑一次说清）=====\n    lines.append(\"【🤖 AI开发规范】（生成代码必须遵守）\")\n    lines.append(\"1️⃣ exec环境变量：\")\n    lines.append(\"   - 选2运行功能时：有 _data_file 和 _base_dir\")\n    lines.append(\"   - 选1添加功能时：没有这两个变量！\")\n    lines.append(\"   - 必须用 try/except NameError 兜底：\")\n    lines.append(\"     try:\")\n    lines.append(\"         base = os.path.dirname(_data_file)\")\n    lines.append(\"     except NameError:\")\n    lines.append(\"         base = os.path.expanduser('~')\")\n    lines.append(\"\")\n    lines.append(\"2️⃣ 菜单规则（绝对不能改）：\")\n    lines.append(\"   - 菜单只显示系统功能（1-9）\")\n    lines.append(\"   - 工具功能（10+）不显示在菜单\")\n    lines.append(\"   - 代码里绝对不能有重建menu的逻辑\")\n    lines.append(\"\")\n    lines.append(\"3️⃣ JSON结构要求：\")\n    lines.append(\"   - action的key必须是字符串数字（'1','2'...）\")\n    lines.append(\"   - code字段是直接exec的字符串，不能是函数定义\")\n    lines.append(\"   - 必须有备忘录，否则添加时被拒\")\n    lines.append(\"\")\n    lines.append(\"4️⃣ 备忘录格式（必须有）：\")\n    lines.append(\"# @AI_MEMO_START\")\n    lines.append(\"# 功能名称：[简短命名]\")\n    lines.append(\"# 原始需求：[用户当时怎么描述的]\")\n    lines.append(\"# 设计思路：[为什么这么实现]\")\n    lines.append(\"# 注意事项：[有什么坑要避免]\")\n    lines.append(\"# 后续优化：[还可以怎么改进]\")\n    lines.append(\"# @AI_MEMO_END\")\n    lines.append(\"\")\n    lines.append(\"5️⃣ 重要功能key（修改时要小心）：\")\n    lines.append(\"   - key '13' = Claude窗口核心对话记录\")\n    lines.append(\"   - 修改它必须用正则替换 content = \\\"\\\"\\\" 内部\")\n    lines.append(\"\")\n    \n    # 原有指令协议\n    lines.append(\"【指令协议】\")\n    lines.append(\"新AI收到上下文后，先说「看」确认进入状态，再接受指令。\")\n    lines.append(\"\")\n    lines.append(\"六个指令及AI响应格式：\")\n    lines.append(\"\")\n    lines.append(\"存：[名称]\")\n    lines.append(\"  → AI把本窗口新产生的内容总结成★/↳两层格式的新条目\")\n    lines.append(\"  → AI输出格式必须以「=== 新增条目 ===」开头，后面跟新条目内容\")\n    lines.append(\"  → 用户新建zhongqu_add.txt，把AI输出内容粘贴进去保存\")\n    lines.append(\"  → 用户回中枢运行「导入AI整理稿」，自动追加到核心对话记录末尾\")\n    lines.append(\"\")\n    lines.append(\"写：[名称] 需求：[一句话]\")\n    lines.append(\"  → AI生成一个新功能的完整代码，含备忘录头，用户复制后选1添加进中枢\")\n    lines.append(\"\")\n    lines.append(\"改：[功能名] 需求：[一句话]\")\n    lines.append(\"  → AI输出修改后的完整代码，用户复制后选3修改进中枢\")\n    lines.append(\"\")\n    lines.append(\"发：[内容]\")\n    lines.append(\"  → AI把指定内容整理成可直接发给另一个AI窗口的格式输出\")\n    lines.append(\"\")\n    lines.append(\"裁：[分歧描述]\")\n    lines.append(\"  → AI分析分歧，给出倾向建议，最终由用户决定\")\n    lines.append(\"\")\n    lines.append(\"看\")\n    lines.append(\"  → AI确认已读取上下文，列出当前功能清单，表示进入状态\")\n    lines.append(\"\")\n    lines.append(\"【备忘录格式】（所有「存」「写」「改」指令的输出必须包含）\")\n    lines.append(\"# @AI_MEMO_START\")\n    lines.append(\"# 功能名称：[简短命名]\")\n    lines.append(\"# 原始需求：[用户当时怎么描述的]\")\n    lines.append(\"# 设计思路：[为什么这么实现]\")\n    lines.append(\"# 注意事项：[有什么坑要避免]\")\n    lines.append(\"# 后续优化：[还可以怎么改进]\")\n    lines.append(\"# @AI_MEMO_END\")\n    lines.append(\"\")\n    lines.append(\"[这里写Python代码或文字内容]\")\n    lines.append(\"input(\\\"回车返回...\\\")\")\n    lines.append(\"\")\n    lines.append(\"【功能清单】\")\n    actions = data.get(\"actions\", {})\n    system_keys = [\"1\",\"2\",\"3\",\"4\",\"5\",\"6\",\"7\",\"8\",\"9\"]\n    lines.append(\"系统功能：\" + \"、\".join(\n        f\"{k}.{v['name']}\" for k,v in actions.items() if k in system_keys\n    ))\n    tool_fns = [(k,v) for k,v in actions.items() if k not in system_keys]\n    if tool_fns:\n        lines.append(\"工具功能：\")\n        for k,v in tool_fns:\n            lines.append(f\"  · {v['name']}\")\n    lines.append(\"\")\n    lines.append(\"【最近操作】\")\n    for log in data.get(\"log\", [])[-5:]:\n        lines.append(f\"  {log}\")\n    lines.append(\"\\n=== 上下文结束 ===\")\n    return \"\\n\".join(lines)\n\ndef build_full(data):\n    lines = [build_summary(data)]\n    lines.append(\"\\n=== 功能详情 ===\")\n    actions = data.get(\"actions\", {})\n    system_keys = [\"1\",\"2\",\"3\",\"4\",\"5\",\"6\",\"7\",\"8\",\"9\"]\n    tool_fns = [(k,v) for k,v in actions.items() if k not in system_keys]\n    for k,v in tool_fns:\n        lines.append(f\"\\n【{v['name']}】\")\n        memo = v.get(\"memo\", {})\n        if memo:\n            for field, val in memo.items():\n                lines.append(f\"  {field}：{val}\")\n        lines.append(\"  代码：\")\n        lines.append(v.get(\"code\", \"\"))\n    return \"\\n\".join(lines)\n\ndata = load()\n\nprint(\"\\n导出模式：\")\nprint(\"  1. 摘要版（屏幕显示，直接复制）\")\nprint(\"  2. 完整版（导出txt到下载目录，分享给AI）\")\nchoice = input(\"选择：\").strip()\n\nif choice == \"1\":\n    print(\"\\n\" + build_summary(data))\n    input(\"\\n回车返回...\")\nelif choice == \"2\":\n    content = build_full(data)\n    ts = datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n    path = os.path.join(DOWNLOAD, f\"zhongqu_context_{ts}.txt\")\n    with open(path, \"w\", encoding=\"utf-8\") as f:\n        f.write(content)\n    print(f\"\\n✅ 已导出：zhongqu_context_{ts}.txt\")\n    print(\"去手机下载文件夹找这个文件，直接分享给AI。\")\n    input(\"\\n回车返回...\")\nelse:\n    print(\"⚠️ 无效选项\")\n    input(\"回车返回...\")",
          "memo": {
            "功能名称": "生成AI上下文",
            "原始需求": "生成给AI看的上下文，包含中枢结构说明和开发规范，让AI第一眼就理解这是Termux程序",
            "设计思路": "在摘要版开头加「中枢结构」和「AI开发规范」两部分，把所有坑和约定集中写清楚",
            "注意事项": "必须让AI第一眼看到这不是txt程序，而是运行在Termux里的真实系统",
            "后续优化": "根据踩坑记录持续补充规范"
          }
        },
        {
          "time": "2026-03-12 21:56",
          "code": "# @AI_MEMO_START\n# 功能名称：生成AI上下文\n# 原始需求：生成给AI看的上下文，摘要版无代码，完整版才带代码\n# 设计思路：让AI默认收到短文本，避免被代码淹没；需要看代码时才发完整版\n# 注意事项：摘要版去掉功能详情，功能清单只保留名称\n# 后续优化：无\n# @AI_MEMO_END\n\nimport json, os\nfrom datetime import datetime\n\nHOME = os.path.expanduser(\"~\")\nDATA_FILE = os.path.join(HOME, \"zhongqu_data.json\")\nDOWNLOAD = \"/sdcard/Download\"\n\ndef load():\n    with open(DATA_FILE, encoding=\"utf-8\") as f:\n        return json.load(f)\n\ndef build_summary(data, include_details=False):\n    lines = []\n    lines.append(\"=== 工具中枢上下文 ===\")\n    lines.append(f\"当前目标：{data.get('goal', '未设置')}\")\n    lines.append(\"\")\n    \n    # ===== 中枢结构 =====\n    lines.append(\"【📁 中枢结构】（重要！先读这里）\")\n    lines.append(\"这不是txt程序，是运行在手机Termux里的Python系统：\")\n    lines.append(\"  · 两个核心文件：\")\n    lines.append(\"    - zhongqu.py      ← 50行不可变内核（永不修改）\")\n    lines.append(\"    - zhongqu_data.json ← 所有功能数据（你生成的代码存这里）\")\n    lines.append(\"  · 你看到的这个txt是「上下文快照」，用来告诉你当前状态\")\n    lines.append(\"  · 你的角色：生成符合规范的代码/文字，由用户手动操作中枢\")\n    lines.append(\"\")\n    \n    # ===== AI开发规范 =====\n    lines.append(\"【🤖 AI开发规范】（生成代码必须遵守）\")\n    lines.append(\"1️⃣ exec环境变量：\")\n    lines.append(\"   - 选2运行功能时：有 _data_file 和 _base_dir\")\n    lines.append(\"   - 选1添加功能时：没有这两个变量！\")\n    lines.append(\"   - 必须用 try/except NameError 兜底：\")\n    lines.append(\"     try:\")\n    lines.append(\"         base = os.path.dirname(_data_file)\")\n    lines.append(\"     except NameError:\")\n    lines.append(\"         base = os.path.expanduser('~')\")\n    lines.append(\"\")\n    lines.append(\"2️⃣ 菜单规则（绝对不能改）：\")\n    lines.append(\"   - 菜单只显示系统功能（1-9）\")\n    lines.append(\"   - 工具功能（10+）不显示在菜单\")\n    lines.append(\"   - 代码里绝对不能有重建menu的逻辑\")\n    lines.append(\"\")\n    lines.append(\"3️⃣ JSON结构要求：\")\n    lines.append(\"   - action的key必须是字符串数字（'1','2'...）\")\n    lines.append(\"   - code字段是直接exec的字符串，不能是函数定义\")\n    lines.append(\"   - 必须有备忘录，否则添加时被拒\")\n    lines.append(\"\")\n    lines.append(\"4️⃣ 备忘录格式（必须有）：\")\n    lines.append(\"# @AI_MEMO_START\")\n    lines.append(\"# 功能名称：[简短命名]\")\n    lines.append(\"# 原始需求：[用户当时怎么描述的]\")\n    lines.append(\"# 设计思路：[为什么这么实现]\")\n    lines.append(\"# 注意事项：[有什么坑要避免]\")\n    lines.append(\"# 后续优化：[还可以怎么改进]\")\n    lines.append(\"# @AI_MEMO_END\")\n    lines.append(\"\")\n    lines.append(\"5️⃣ 重要功能key（修改时要小心）：\")\n    lines.append(\"   - key '13' = Claude窗口核心对话记录\")\n    lines.append(\"   - 修改它必须用正则替换 content = \\\"\\\"\\\" 内部\")\n    lines.append(\"\")\n    \n    # ===== 指令协议 =====\n    lines.append(\"【指令协议】\")\n    lines.append(\"新AI收到上下文后，先说「看」确认进入状态，再接受指令。\")\n    lines.append(\"\")\n    lines.append(\"六个指令及AI响应格式：\")\n    lines.append(\"\")\n    lines.append(\"存：[名称]\")\n    lines.append(\"  → AI把本窗口新产生的内容总结成★/↳两层格式的新条目\")\n    lines.append(\"  → AI输出格式必须以「=== 新增条目 ===」开头，后面跟新条目内容\")\n    lines.append(\"  → 用户新建zhongqu_add.txt，把AI输出内容粘贴进去保存\")\n    lines.append(\"  → 用户回中枢运行「导入AI整理稿」，自动追加到核心对话记录末尾\")\n    lines.append(\"\")\n    lines.append(\"写：[名称] 需求：[一句话]\")\n    lines.append(\"  → AI生成一个新功能的完整代码，含备忘录头，用户复制后选1添加进中枢\")\n    lines.append(\"\")\n    lines.append(\"改：[功能名] 需求：[一句话]\")\n    lines.append(\"  → AI输出修改后的完整代码，用户复制后选3修改进中枢\")\n    lines.append(\"\")\n    lines.append(\"发：[内容]\")\n    lines.append(\"  → AI把指定内容整理成可直接发给另一个AI窗口的格式输出\")\n    lines.append(\"\")\n    lines.append(\"裁：[分歧描述]\")\n    lines.append(\"  → AI分析分歧，给出倾向建议，最终由用户决定\")\n    lines.append(\"\")\n    lines.append(\"看\")\n    lines.append(\"  → AI确认已读取上下文，列出当前功能清单，表示进入状态\")\n    lines.append(\"\")\n    \n    # ===== 备忘录格式（重复是为了强调）=====\n    lines.append(\"【备忘录格式】（所有「存」「写」「改」指令的输出必须包含）\")\n    lines.append(\"# @AI_MEMO_START\")\n    lines.append(\"# 功能名称：[简短命名]\")\n    lines.append(\"# 原始需求：[用户当时怎么描述的]\")\n    lines.append(\"# 设计思路：[为什么这么实现]\")\n    lines.append(\"# 注意事项：[有什么坑要避免]\")\n    lines.append(\"# 后续优化：[还可以怎么改进]\")\n    lines.append(\"# @AI_MEMO_END\")\n    lines.append(\"\")\n    lines.append(\"[这里写Python代码或文字内容]\")\n    lines.append(\"input(\\\"回车返回...\\\")\")\n    lines.append(\"\")\n    \n    # ===== 功能清单（只有名称）=====\n    lines.append(\"【功能清单】\")\n    actions = data.get(\"actions\", {})\n    system_keys = [\"1\",\"2\",\"3\",\"4\",\"5\",\"6\",\"7\",\"8\",\"9\"]\n    lines.append(\"系统功能：\" + \"、\".join(\n        f\"{k}.{v['name']}\" for k,v in actions.items() if k in system_keys\n    ))\n    tool_fns = [(k,v) for k,v in actions.items() if k not in system_keys]\n    if tool_fns:\n        lines.append(\"工具功能：\")\n        for k,v in tool_fns:\n            lines.append(f\"  · {v['name']}\")\n    lines.append(\"\")\n    \n    # ===== 最近操作 =====\n    lines.append(\"【最近操作】\")\n    for log in data.get(\"log\", [])[-5:]:\n        lines.append(f\"  {log}\")\n    lines.append(\"\")\n    \n    lines.append(\"=== 上下文结束 ===\")\n    \n    # ===== 如果需要功能详情（完整版才加）=====\n    if include_details:\n        details_lines = [\"\\n=== 功能详情 ===\\n\"]\n        for k,v in tool_fns:\n            details_lines.append(f\"\\n【{v['name']}】\")\n            memo = v.get(\"memo\", {})\n            if memo:\n                for field, val in memo.items():\n                    details_lines.append(f\"  {field}：{val}\")\n            details_lines.append(\"  代码：\")\n            details_lines.append(v.get(\"code\", \"\"))\n        lines.extend(details_lines)\n    \n    return \"\\n\".join(lines)\n\ndata = load()\n\nprint(\"\\n导出模式：\")\nprint(\"  1. 摘要版（无代码，适合日常发给AI）\")\nprint(\"  2. 完整版（带代码，需要看具体功能时用）\")\nchoice = input(\"选择：\").strip()\n\nif choice == \"1\":\n    print(\"\\n\" + build_summary(data, include_details=False))\n    input(\"\\n回车返回...\")\nelif choice == \"2\":\n    content = build_summary(data, include_details=True)\n    ts = datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n    path = os.path.join(DOWNLOAD, f\"zhongqu_context_{ts}.txt\")\n    with open(path, \"w\", encoding=\"utf-8\") as f:\n        f.write(content)\n    print(f\"\\n✅ 已导出：zhongqu_context_{ts}.txt\")\n    print(\"去手机下载文件夹找这个文件，直接分享给AI。\")\n    input(\"\\n回车返回...\")\nelse:\n    print(\"⚠️ 无效选项\")\n    input(\"回车返回...\")",
          "memo": {
            "功能名称": "生成AI上下文",
            "原始需求": "生成给AI看的上下文，摘要版无代码，完整版才带代码",
            "设计思路": "让AI默认收到短文本，避免被代码淹没；需要看代码时才发完整版",
            "注意事项": "摘要版去掉功能详情，功能清单只保留名称",
            "后续优化": "无"
          }
        },
        {
          "time": "2026-03-13 18:40",
          "code": "# @AI_MEMO_START\n# 功能名称：生成AI上下文\n# 原始需求：生成给AI看的上下文，摘要版无代码，完整版才带代码，精简版只含★结论层\n# 设计思路：精简版过滤key13里的↳行，只保留★行，降低触发敏感词风险\n# 注意事项：精简版导出txt，不屏幕显示；↳过滤只针对key13，其他功能不受影响\n# 后续优化：可加时间段导出\n# @AI_MEMO_END\n\nimport json, os, re\nfrom datetime import datetime\n\nHOME = os.path.expanduser(\"~\")\nDATA_FILE = os.path.join(HOME, \"zhongqu_data.json\")\nDOWNLOAD = \"/sdcard/Download\"\n\ndef load():\n    with open(DATA_FILE, encoding=\"utf-8\") as f:\n        return json.load(f)\n\ndef extract_star_only(code13):\n    \"\"\"从key13的code里提取★行，过滤↳行\"\"\"\n    m = re.search(r'content = \"\"\"\\n(.*?)\"\"\"', code13, re.DOTALL)\n    if not m:\n        return \"=== 核心对话记录 ===\\n（无内容）\"\n    content = m.group(1)\n    lines = content.split(\"\\n\")\n    result = []\n    for line in lines:\n        stripped = line.strip()\n        if stripped.startswith(\"★\") or stripped.startswith(\"===\"):\n            result.append(line)\n        elif not stripped.startswith(\"↳\") and not stripped.startswith(\"·\") and result:\n            # 保留★条目后的空行，丢掉↳和·\n            if stripped == \"\":\n                result.append(\"\")\n    return \"\\n\".join(result)\n\ndef build_summary(data, include_details=False, star_only=False):\n    lines = []\n    lines.append(\"=== 工具中枢上下文 ===\")\n    lines.append(f\"当前目标：{data.get('goal', '未设置')}\")\n    lines.append(\"\")\n\n    lines.append(\"【📁 中枢结构】（重要！先读这里）\")\n    lines.append(\"这不是txt程序，是运行在手机Termux里的Python系统：\")\n    lines.append(\"  · 两个核心文件：\")\n    lines.append(\"    - zhongqu.py      ← 50行不可变内核（永不修改）\")\n    lines.append(\"    - zhongqu_data.json ← 所有功能数据（你生成的代码存这里）\")\n    lines.append(\"  · 你看到的这个txt是「上下文快照」，用来告诉你当前状态\")\n    lines.append(\"  · 你的角色：生成符合规范的代码/文字，由用户手动操作中枢\")\n    lines.append(\"\")\n\n    lines.append(\"【🤖 AI开发规范】（生成代码必须遵守）\")\n    lines.append(\"1️⃣ exec环境变量：\")\n    lines.append(\"   - 选2运行功能时：有 _data_file 和 _base_dir\")\n    lines.append(\"   - 选1添加功能时：没有这两个变量！\")\n    lines.append(\"   - 必须用 try/except NameError 兜底：\")\n    lines.append(\"     try:\")\n    lines.append(\"         base = os.path.dirname(_data_file)\")\n    lines.append(\"     except NameError:\")\n    lines.append(\"         base = os.path.expanduser('~')\")\n    lines.append(\"\")\n    lines.append(\"2️⃣ 菜单规则（绝对不能改）：\")\n    lines.append(\"   - 菜单只显示系统功能（1-9）\")\n    lines.append(\"   - 工具功能（10+）不显示在菜单\")\n    lines.append(\"   - 代码里绝对不能有重建menu的逻辑\")\n    lines.append(\"\")\n    lines.append(\"3️⃣ JSON结构要求：\")\n    lines.append(\"   - action的key必须是字符串数字（'1','2'...）\")\n    lines.append(\"   - code字段是直接exec的字符串，不能是函数定义\")\n    lines.append(\"   - 必须有备忘录，否则添加时被拒\")\n    lines.append(\"\")\n    lines.append(\"4️⃣ 备忘录格式（必须有）：\")\n    lines.append(\"# @AI_MEMO_START\")\n    lines.append(\"# 功能名称：[简短命名]\")\n    lines.append(\"# 原始需求：[用户当时怎么描述的]\")\n    lines.append(\"# 设计思路：[为什么这么实现]\")\n    lines.append(\"# 注意事项：[有什么坑要避免]\")\n    lines.append(\"# 后续优化：[还可以怎么改进]\")\n    lines.append(\"# @AI_MEMO_END\")\n    lines.append(\"\")\n    lines.append(\"5️⃣ 重要功能key（修改时要小心）：\")\n    lines.append(\"   - key '13' = Claude窗口核心对话记录\")\n    lines.append(\"   - 修改它必须用正则替换 content = \\\"\\\"\\\" 内部\")\n    lines.append(\"\")\n\n    lines.append(\"【指令协议】\")\n    lines.append(\"新AI收到上下文后，先说「看」确认进入状态，再接受指令。\")\n    lines.append(\"\")\n    lines.append(\"六个指令及AI响应格式：\")\n    lines.append(\"\")\n    lines.append(\"存：[名称]\")\n    lines.append(\"  → AI把本窗口新产生的内容总结成★/↳两层格式的新条目\")\n    lines.append(\"  → AI输出格式必须以「=== 新增条目 ===」开头，后面跟新条目内容\")\n    lines.append(\"  → 用户新建zhongqu_add.txt，把AI输出内容粘贴进去保存\")\n    lines.append(\"  → 用户回中枢运行「导入AI整理稿」，自动追加到核心对话记录末尾\")\n    lines.append(\"\")\n    lines.append(\"写：[名称] 需求：[一句话]\")\n    lines.append(\"  → AI生成一个新功能的完整代码，含备忘录头，用户复制后选1添加进中枢\")\n    lines.append(\"\")\n    lines.append(\"改：[功能名] 需求：[一句话]\")\n    lines.append(\"  → AI输出修改后的完整代码，用户复制后选3修改进中枢\")\n    lines.append(\"\")\n    lines.append(\"发：[内容]\")\n    lines.append(\"  → AI把指定内容整理成可直接发给另一个AI窗口的格式输出\")\n    lines.append(\"\")\n    lines.append(\"裁：[分歧描述]\")\n    lines.append(\"  → AI分析分歧，给出倾向建议，最终由用户决定\")\n    lines.append(\"\")\n    lines.append(\"看\")\n    lines.append(\"  → AI确认已读取上下文，列出当前功能清单，表示进入状态\")\n    lines.append(\"\")\n\n    lines.append(\"【备忘录格式】（所有「存」「写」「改」指令的输出必须包含）\")\n    lines.append(\"# @AI_MEMO_START\")\n    lines.append(\"# 功能名称：[简短命名]\")\n    lines.append(\"# 原始需求：[用户当时怎么描述的]\")\n    lines.append(\"# 设计思路：[为什么这么实现]\")\n    lines.append(\"# 注意事项：[有什么坑要避免]\")\n    lines.append(\"# 后续优化：[还可以怎么改进]\")\n    lines.append(\"# @AI_MEMO_END\")\n    lines.append(\"\")\n    lines.append(\"[这里写Python代码或文字内容]\")\n    lines.append(\"input(\\\"回车返回...\\\")\")\n    lines.append(\"\")\n\n    lines.append(\"【功能清单】\")\n    actions = data.get(\"actions\", {})\n    system_keys = [\"1\",\"2\",\"3\",\"4\",\"5\",\"6\",\"7\",\"8\",\"9\"]\n    lines.append(\"系统功能：\" + \"、\".join(\n        f\"{k}.{v['name']}\" for k,v in actions.items() if k in system_keys\n    ))\n    tool_fns = [(k,v) for k,v in actions.items() if k not in system_keys]\n    if tool_fns:\n        lines.append(\"工具功能：\")\n        for k,v in tool_fns:\n            lines.append(f\"  · {v['name']}\")\n    lines.append(\"\")\n\n    lines.append(\"【最近操作】\")\n    for log in data.get(\"log\", [])[-5:]:\n        lines.append(f\"  {log}\")\n    lines.append(\"\")\n    lines.append(\"=== 上下文结束 ===\")\n\n    if include_details:\n        details_lines = [\"\\n=== 功能详情 ===\\n\"]\n        for k,v in tool_fns:\n            details_lines.append(f\"\\n【{v['name']}】\")\n            memo = v.get(\"memo\", {})\n            if memo:\n                for field, val in memo.items():\n                    details_lines.append(f\"  {field}：{val}\")\n            details_lines.append(\"  代码：\")\n            if star_only and k == \"13\":\n                # 精简版：key13只输出★结论层\n                details_lines.append(extract_star_only(v.get(\"code\", \"\")))\n            else:\n                details_lines.append(v.get(\"code\", \"\"))\n        lines.extend(details_lines)\n\n    return \"\\n\".join(lines)\n\ndata = load()\n\nprint(\"\\n导出模式：\")\nprint(\"  1. 摘要版（无对话记录，适合初次发给AI）\")\nprint(\"  2. 精简版（只含★结论层，日常发给AI用）\")\nprint(\"  3. 完整版（含★和↳，需要回溯细节时用）\")\nchoice = input(\"选择：\").strip()\n\nts = datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n\nif choice == \"1\":\n    print(\"\\n\" + build_summary(data, include_details=False))\n    input(\"\\n回车返回...\")\nelif choice == \"2\":\n    content = build_summary(data, include_details=True, star_only=True)\n    path = os.path.join(DOWNLOAD, f\"zhongqu_context_{ts}.txt\")\n    with open(path, \"w\", encoding=\"utf-8\") as f:\n        f.write(content)\n    print(f\"\\n✅ 已导出精简版：zhongqu_context_{ts}.txt\")\n    print(\"只含★结论层，↳过程层已过滤。\")\n    input(\"\\n回车返回...\")\nelif choice == \"3\":\n    content = build_summary(data, include_details=True, star_only=False)\n    path = os.path.join(DOWNLOAD, f\"zhongqu_context_{ts}.txt\")\n    with open(path, \"w\", encoding=\"utf-8\") as f:\n        f.write(content)\n    print(f\"\\n✅ 已导出完整版：zhongqu_context_{ts}.txt\")\n    input(\"\\n回车返回...\")\nelse:\n    print(\"⚠️ 无效选项\")\n    input(\"回车返回...\")",
          "memo": {
            "功能名称": "生成AI上下文",
            "原始需求": "生成给AI看的上下文，摘要版无代码，完整版才带代码，精简版只含★结论层",
            "设计思路": "精简版过滤key13里的↳行，只保留★行，降低触发敏感词风险",
            "注意事项": "精简版导出txt，不屏幕显示；↳过滤只针对key13，其他功能不受影响",
            "后续优化": "可加时间段导出"
          }
        },
        {
          "time": "2026-03-13 19:03",
          "code": "# @AI_MEMO_START\n# 功能名称：生成AI上下文\n# 原始需求：生成给AI看的上下文，摘要版无代码，完整版才带代码，精简版只含★结论层；包含对话记录条目\n# 设计思路：从actions[\"对话记录\"][\"entries\"]读取条目拼入输出；三种模式都包含对话记录\n# 注意事项：对话记录模块可能不存在或entries为空，用.get兜底；精简版只过滤↳行\n# 后续优化：可加时间段导出\n# @AI_MEMO_END\n\nimport json, os, re\nfrom datetime import datetime\n\ntry:\n    DATA_FILE = _data_file\nexcept NameError:\n    DATA_FILE = os.path.join(os.path.expanduser(\"~\"), \"zhongqu_data.json\")\n\nDOWNLOAD = \"/sdcard/Download\"\n\ndef load():\n    with open(DATA_FILE, encoding=\"utf-8\") as f:\n        return json.load(f)\n\ndef get_context_entries(data, star_only=False):\n    entries = data.get(\"actions\", {}).get(\"对话记录\", {}).get(\"entries\", [])\n    if not entries:\n        return \"（暂无对话记录）\"\n    lines = []\n    for e in entries:\n        block = e.get(\"content\", \"\").strip()\n        if star_only:\n            filtered = []\n            for line in block.split(\"\\n\"):\n                s = line.strip()\n                if s.startswith(\"★\") or s.startswith(\"===\"):\n                    filtered.append(line)\n                elif not s.startswith(\"↳\") and not s.startswith(\"·\") and filtered:\n                    if s == \"\":\n                        filtered.append(\"\")\n            block = \"\\n\".join(filtered)\n        lines.append(f\"[{e.get('time','')}]\\n{block}\")\n    return \"\\n\\n\".join(lines)\n\ndef build_summary(data, include_details=False, star_only=False):\n    lines = []\n    lines.append(\"=== 工具中枢上下文 ===\")\n    lines.append(f\"当前目标：{data.get('goal', '未设置')}\")\n    lines.append(\"\")\n\n    lines.append(\"【📁 中枢结构】（重要！先读这里）\")\n    lines.append(\"这不是txt程序，是运行在手机Termux里的Python系统：\")\n    lines.append(\"  · 两个核心文件：\")\n    lines.append(\"    - zhongqu.py        ← 50行不可变内核（永不修改）\")\n    lines.append(\"    - zhongqu_data.json ← 所有功能数据（你生成的代码存这里）\")\n    lines.append(\"  · 你的角色：生成符合规范的代码/文字，由用户手动操作中枢\")\n    lines.append(\"\")\n\n    lines.append(\"【🤖 AI开发规范】（生成代码必须遵守）\")\n    lines.append(\"1️⃣ exec环境变量：运行时有 _data_file/_base_dir，添加时没有，必须 try/except NameError 兜底\")\n    lines.append(\"2️⃣ 菜单规则：只显示系统功能1-9，工具功能不显示在菜单，代码不能有重建menu逻辑\")\n    lines.append(\"3️⃣ JSON结构：action的key是字符串，code字段直接exec，必须有备忘录\")\n    lines.append(\"4️⃣ 备忘录格式：# @AI_MEMO_START ... # @AI_MEMO_END\")\n    lines.append(\"5️⃣ 对话记录存在 actions['对话记录']['entries']，是[{time,content}]列表，不要动这个结构\")\n    lines.append(\"\")\n\n    lines.append(\"【指令协议】\")\n    lines.append(\"新AI收到上下文后，先说「看」确认进入状态，再接受指令。\")\n    lines.append(\"\")\n    lines.append(\"存：[名称]  → 总结成★/↳两层格式，用户回中枢运行「对话记录」粘贴导入\")\n    lines.append(\"写：[名称] 需求：[一句话]  → 生成新功能完整代码，用户选1添加\")\n    lines.append(\"改：[功能名] 需求：[一句话]  → 生成修改后完整代码，用户选3替换\")\n    lines.append(\"发：[内容]  → 整理成可发给另一个AI窗口的格式\")\n    lines.append(\"裁：[分歧描述]  → 分析分歧，给出倾向建议\")\n    lines.append(\"看  → 确认已读取上下文，列出功能清单，进入状态\")\n    lines.append(\"\")\n\n    lines.append(\"【功能清单】\")\n    actions = data.get(\"actions\", {})\n    system_keys = [\"1\",\"2\",\"3\",\"4\",\"5\",\"6\",\"7\",\"8\",\"9\"]\n    lines.append(\"系统功能：\" + \"、\".join(\n        f\"{k}.{v['name']}\" for k,v in actions.items() if k in system_keys\n    ))\n    tool_fns = [(k,v) for k,v in actions.items() if k not in system_keys]\n    if tool_fns:\n        lines.append(\"工具功能：\")\n        for k,v in tool_fns:\n            lines.append(f\"  · {v['name']}\")\n    lines.append(\"\")\n\n    lines.append(\"【最近操作】\")\n    for log in data.get(\"log\", [])[-5:]:\n        lines.append(f\"  {log}\")\n    lines.append(\"\")\n\n    lines.append(\"【对话记录】\")\n    lines.append(get_context_entries(data, star_only=star_only))\n    lines.append(\"\")\n\n    lines.append(\"=== 上下文结束 ===\")\n\n    if include_details:\n        details = [\"\\n=== 功能详情 ===\\n\"]\n        for k,v in tool_fns:\n            if k == \"对话记录\":\n                continue  # 详情里跳过对话记录，内容已在上面输出\n            details.append(f\"\\n【{v['name']}】\")\n            memo = v.get(\"memo\", {})\n            for field, val in memo.items():\n                details.append(f\"  {field}：{val}\")\n            details.append(\"  代码：\")\n            details.append(v.get(\"code\", \"\"))\n        lines.extend(details)\n\n    return \"\\n\".join(lines)\n\ndata = load()\n\nprint(\"\\n导出模式：\")\nprint(\"  1. 摘要版（无代码，适合日常发给AI）\")\nprint(\"  2. 精简版（只含★结论层，减少篇幅）\")\nprint(\"  3. 完整版（含代码，需要看具体功能时用）\")\nchoice = input(\"选择：\").strip()\n\nts = datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n\nif choice == \"1\":\n    print(\"\\n\" + build_summary(data, include_details=False, star_only=False))\n    input(\"\\n回车返回...\")\nelif choice == \"2\":\n    content = build_summary(data, include_details=False, star_only=True)\n    path = os.path.join(DOWNLOAD, f\"zhongqu_context_{ts}.txt\")\n    with open(path, \"w\", encoding=\"utf-8\") as f:\n        f.write(content)\n    print(f\"\\n✅ 已导出精简版：zhongqu_context_{ts}.txt\")\n    input(\"\\n回车返回...\")\nelif choice == \"3\":\n    content = build_summary(data, include_details=True, star_only=False)\n    path = os.path.join(DOWNLOAD, f\"zhongqu_context_{ts}.txt\")\n    with open(path, \"w\", encoding=\"utf-8\") as f:\n        f.write(content)\n    print(f\"\\n✅ 已导出完整版：zhongqu_context_{ts}.txt\")\n    input(\"\\n回车返回...\")\nelse:\n    print(\"⚠️ 无效选项\")\n    input(\"回车返回...\")",
          "memo": {
            "功能名称": "生成AI上下文",
            "原始需求": "生成给AI看的上下文，摘要版无代码，完整版才带代码，精简版只含★结论层；包含对话记录条目",
            "设计思路": "从actions[\"对话记录\"][\"entries\"]读取条目拼入输出；三种模式都包含对话记录",
            "注意事项": "对话记录模块可能不存在或entries为空，用.get兜底；精简版只过滤↳行",
            "后续优化": "可加时间段导出"
          }
        },
        {
          "time": "2026-03-13 21:00",
          "code": "# @AI_MEMO_START\n# 功能名称：生成AI上下文\n# 原始需求：生成给AI看的上下文，支持按日期筛选对话记录\n# 设计思路：输入日期筛选当天记录，留空显示全部\n# 注意事项：日期格式灵活，只要time字段包含输入字符串即可\n# 后续优化：可加时间段导出\n# @AI_MEMO_END\n\nimport json, os\nfrom datetime import datetime\n\ntry:\n    DATA_FILE = _get_data_file()\nexcept NameError:\n    DATA_FILE = os.path.join(os.path.expanduser(\"~\"), \"zhongqu_data.json\")\n\nDOWNLOAD = \"/sdcard/Download\"\n\ndef load():\n    with open(DATA_FILE, encoding=\"utf-8\") as f:\n        return json.load(f)\n\ndef build_context_log(data, date_filter=None):\n    \"\"\"构建对话记录文本，可筛选日期，开头显示统计信息\"\"\"\n    entries = data.get(\"context_log\", [])\n    if not entries:\n        return \"（暂无对话记录）\"\n    \n    # 统计信息\n    total = len(entries)\n    if date_filter:\n        filtered = [e for e in entries if date_filter in e.get('time', '')]\n        count = len(filtered)\n        if count == 0:\n            return f\"（{date_filter} 没有对话记录）\"\n        \n        # 找到最新一条的时间\n        times = [e.get('time', '') for e in filtered if e.get('time')]\n        latest = max(times) if times else '未知'\n        header = f\"【对话记录 - {date_filter}】（共 {count} 条，最新：{latest}）\"\n    else:\n        # 全部记录，统计总条数和最新时间\n        times = [e.get('time', '') for e in entries if e.get('time')]\n        latest = max(times) if times else '未知'\n        header = f\"【对话记录 - 全部】（共 {total} 条，最新：{latest}）\"\n    \n    # 原有的构建逻辑继续\n    \"\"\"构建对话记录文本，可筛选日期\"\"\"\n    entries = data.get(\"context_log\", [])\n    if not entries:\n        return \"（暂无对话记录）\"\n    \n    if date_filter:\n        # 筛选包含指定日期的条目\n        filtered = []\n        for e in entries:\n            time_str = e.get('time', '')\n            if date_filter in time_str:\n                filtered.append(e)\n        entries = filtered\n        if not entries:\n            return f\"（{date_filter} 没有对话记录）\"\n    \n    lines = []\n    for e in entries:\n        lines.append(f\"[{e.get('time','')}]\\n{e.get('content','')}\")\n    return \"\\n\\n\".join(lines)\n\ndef build_output(data, include_details=False, date_filter=None):\n    lines = []\n    lines.append(\"=== 工具中枢上下文 ===\")\n    lines.append(f\"当前目标：{data.get('goal', '未设置')}\")\n    lines.append(\"\")\n\n    lines.append(\"【📁 中枢结构】\")\n    lines.append(\"运行在手机Termux里的Python系统：\")\n    lines.append(\"  · zhongqu.py        ← 80行终极内核（永不修改）\")\n    lines.append(\"  · zhongqu_data.json ← 所有功能数据\")\n    lines.append(\"\")\n\n    lines.append(\"【🤖 AI开发规范】\")\n    lines.append(\"1️⃣ 所有功能都能直接调用内核函数：_atomic_save、_snap、_log、_now\")\n    lines.append(\"2️⃣ 菜单规则：只显示1-9系统功能，工具功能不显示\")\n    lines.append(\"3️⃣ JSON结构：action的key是字符串，code字段直接exec\")\n    lines.append(\"4️⃣ 对话记录存在 json顶层 context_log 字段\")\n    lines.append(\"\")\n\n    lines.append(\"【指令协议】\")\n    lines.append(\"新AI收到上下文后，先说「看」确认进入状态，再接受指令。\")\n    lines.append(\"\")\n    lines.append(\"存：[名称]  → 总结成★/↳两层格式，用户回中枢运行「对话记录」选1粘贴导入\")\n    lines.append(\"写：[名称] 需求：[一句话]  → 生成新功能完整代码，用户选1添加\")\n    lines.append(\"改：[功能名] 需求：[一句话]  → 生成修改后完整代码，用户选3替换\")\n    lines.append(\"发：[内容]  → 整理成可发给另一个AI窗口的格式\")\n    lines.append(\"裁：[分歧描述]  → 分析分歧，给出倾向建议\")\n    lines.append(\"看  → 确认已读取上下文，列出功能清单，进入状态\")\n    lines.append(\"\")\n\n    lines.append(\"【功能清单】\")\n    actions = data.get(\"actions\", {})\n    system_keys = [\"1\",\"2\",\"3\",\"4\",\"5\",\"6\",\"7\",\"8\",\"9\"]\n    lines.append(\"系统功能：\" + \"、\".join(\n        f\"{k}.{v['name']}\" for k,v in actions.items() if k in system_keys\n    ))\n    tool_fns = [(k,v) for k,v in actions.items() if k not in system_keys]\n    if tool_fns:\n        lines.append(\"工具功能：\")\n        for k,v in tool_fns:\n            lines.append(f\"  · {v['name']}\")\n    lines.append(\"\")\n\n    lines.append(\"【最近操作】\")\n    for log in data.get(\"log\", [])[-5:]:\n        lines.append(f\"  {log}\")\n    lines.append(\"\")\n\n    lines.append(build_context_log(data, date_filter))\n    lines.append(\"\")\n\n    lines.append(\"=== 上下文结束 ===\")\n\n    if include_details:\n        details = [\"\\n=== 功能详情 ===\\n\"]\n        for k,v in tool_fns:\n            if k == \"对话记录\":\n                continue\n            details.append(f\"\\n【{v['name']}】\")\n            for field, val in v.get(\"memo\", {}).items():\n                details.append(f\"  {field}：{val}\")\n            details.append(\"  代码：\")\n            details.append(v.get(\"code\", \"\"))\n        lines.extend(details)\n\n    return \"\\n\".join(lines)\n\ndata = load()\n\nprint(\"\\n导出模式：\")\nprint(\"  1. 摘要版（屏幕显示，全部对话）\")\nprint(\"  2. 按日期导出（输入日期筛选）\")\nprint(\"  3. 完整版（含代码，全部对话）\")\nchoice = input(\"选择：\").strip()\n\nts = datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n\nif choice == \"1\":\n    print(\"\\n\" + build_output(data, include_details=False, date_filter=None))\n    input(\"\\n回车返回...\")\n\nelif choice == \"2\":\n    date = input(\"输入日期（如 2026-03-13，回车显示全部）：\").strip()\n    if not date:\n        date = None\n    content = build_output(data, include_details=False, date_filter=date)\n    path = os.path.join(DOWNLOAD, f\"zhongqu_context_{ts}.txt\")\n    with open(path, \"w\", encoding=\"utf-8\") as f:\n        f.write(content)\n    print(f\"\\n✅ 已导出：zhongqu_context_{ts}.txt\")\n    if date:\n        print(f\"   筛选日期：{date}\")\n    input(\"\\n回车返回...\")\n\nelif choice == \"3\":\n    content = build_output(data, include_details=True, date_filter=None)\n    path = os.path.join(DOWNLOAD, f\"zhongqu_context_{ts}.txt\")\n    with open(path, \"w\", encoding=\"utf-8\") as f:\n        f.write(content)\n    print(f\"\\n✅ 已导出完整版：zhongqu_context_{ts}.txt\")\n    input(\"\\n回车返回...\")\n\nelse:\n    print(\"⚠️ 无效选项\")\n    input(\"回车返回...\")",
          "memo": {
            "功能名称": "生成AI上下文",
            "原始需求": "生成给AI看的上下文，三种模式；对话记录从json顶层context_log读取",
            "设计思路": "context_log存顶层，与action key无关；摘要版屏幕显示，精简版和完整版导出txt",
            "注意事项": "context_log可能不存在用.get兜底；精简版过滤↳行只保留★行",
            "后续优化": "时间段导出"
          }
        },
        {
          "time": "2026-03-14 20:45",
          "code": "# @AI_MEMO_START\n# 功能名称：生成AI上下文\n# 原始需求：生成给AI看的上下文，支持按日期筛选，带统计信息\n# 设计思路：所有导出模式都显示统计信息（总条数/最新时间）\n# 注意事项：日期筛选支持模糊匹配（输入03-13也能匹配）\n# 后续优化：可加时间段导出\n# @AI_MEMO_END\n\nimport json, os\nfrom datetime import datetime\n\ntry:\n    DATA_FILE = _get_data_file()\nexcept NameError:\n    DATA_FILE = os.path.join(os.path.expanduser(\"~\"), \"zhongqu_data.json\")\n\nDOWNLOAD = \"/sdcard/Download\"\n\ndef load():\n    with open(DATA_FILE, encoding=\"utf-8\") as f:\n        return json.load(f)\n\ndef build_context_log(data, date_filter=None):\n    \"\"\"构建对话记录文本，可筛选日期，开头显示统计信息\"\"\"\n    entries = data.get(\"context_log\", [])\n    if not entries:\n        return \"（暂无对话记录）\"\n    \n    # 统计信息\n    total = len(entries)\n    all_times = [e.get('time', '') for e in entries if e.get('time')]\n    latest_all = max(all_times) if all_times else '未知'\n    \n    if date_filter:\n        # 筛选包含指定日期的条目\n        filtered = []\n        for e in entries:\n            time_str = e.get('time', '')\n            if date_filter in time_str:\n                filtered.append(e)\n        entries = filtered\n        if not entries:\n            return f\"（{date_filter} 没有对话记录）\"\n        \n        # 筛选后的统计\n        count = len(entries)\n        times = [e.get('time', '') for e in entries if e.get('time')]\n        latest = max(times) if times else '未知'\n        header = f\"【对话记录 - {date_filter}】（共 {count} 条，最新：{latest}）\"\n    else:\n        header = f\"【对话记录 - 全部】（共 {total} 条，最新：{latest_all}）\"\n    \n    lines = [header]\n    for e in entries:\n        lines.append(f\"\\n[{e.get('time','')}]\\n{e.get('content','')}\")\n    return \"\\n\".join(lines)\n\ndef build_output(data, include_details=False, date_filter=None):\n    lines = []\n    lines.append(\"=== 工具中枢上下文 ===\")\n    lines.append(f\"当前目标：{data.get('goal', '未设置')}\")\n    lines.append(\"\")\n\n    lines.append(\"【📁 中枢结构】\")\n    lines.append(\"运行在手机Termux里的Python系统：\")\n    lines.append(\"  · zhongqu.py        ← 80行终极内核（永不修改）\")\n    lines.append(\"  · zhongqu_data.json ← 所有功能数据\")\n    lines.append(\"\")\n\n    lines.append(\"【🤖 AI开发规范】\")\n    lines.append(\"1️⃣ 所有功能都能直接调用内核函数：_atomic_save、_snap、_log、_now\")\n    lines.append(\"2️⃣ 菜单规则：只显示1-9系统功能，工具功能不显示\")\n    lines.append(\"3️⃣ JSON结构：action的key是字符串，code字段直接exec\")\n    lines.append(\"4️⃣ 对话记录存在 json顶层 context_log 字段\")\n    lines.append(\"\")\n\n    lines.append(\"【指令协议】\")\n    lines.append(\"新AI收到上下文后，先说「看」确认进入状态，再接受指令。\")\n    lines.append(\"\")\n    lines.append(\"存：[名称]  → 总结成★/↳两层格式，用户回中枢运行「对话记录」选1粘贴导入\")\n    lines.append(\"写：[名称] 需求：[一句话]  → 生成新功能完整代码，用户选1添加\")\n    lines.append(\"改：[功能名] 需求：[一句话]  → 生成修改后完整代码，用户选3替换\")\n    lines.append(\"发：[内容]  → 整理成可发给另一个AI窗口的格式\")\n    lines.append(\"裁：[分歧描述]\")\n    lines.append(\"  → 1. 列出分歧双方的核心主张，不偏不倚\")\n    lines.append(\"  → 2. 分析各自代价，选A失去什么，选B失去什么\")\n    lines.append(\"  → 3. 给出倾向建议，说明理由\")\n    lines.append(\"  → 4. 判断权在用户，AI不替人决定\")\n    lines.append(\"看  → 确认已读取上下文，列出功能清单，进入状态\")\n    lines.append(\"\")\n\n    lines.append(\"【功能清单】\")\n    actions = data.get(\"actions\", {})\n    system_keys = [\"1\",\"2\",\"3\",\"4\",\"5\",\"6\",\"7\",\"8\",\"9\"]\n    lines.append(\"系统功能：\" + \"、\".join(\n        f\"{k}.{v['name']}\" for k,v in actions.items() if k in system_keys\n    ))\n    tool_fns = [(k,v) for k,v in actions.items() if k not in system_keys]\n    if tool_fns:\n        lines.append(\"工具功能：\")\n        for k,v in tool_fns:\n            lines.append(f\"  · {v['name']}\")\n    lines.append(\"\")\n\n    lines.append(\"【最近操作】\")\n    for log in data.get(\"log\", [])[-5:]:\n        lines.append(f\"  {log}\")\n    lines.append(\"\")\n\n    lines.append(build_context_log(data, date_filter))\n    lines.append(\"\")\n\n    lines.append(\"=== 上下文结束 ===\")\n\n    if include_details:\n        details = [\"\\n=== 功能详情 ===\\n\"]\n        for k,v in tool_fns:\n            if k == \"对话记录\":\n                continue\n            details.append(f\"\\n【{v['name']}】\")\n            for field, val in v.get(\"memo\", {}).items():\n                details.append(f\"  {field}：{val}\")\n            details.append(\"  代码：\")\n            details.append(v.get(\"code\", \"\"))\n        lines.extend(details)\n\n    return \"\\n\".join(lines)\n\ndata = load()\n\nprint(\"\\n导出模式：\")\nprint(\"  1. 摘要版（屏幕显示，全部对话）\")\nprint(\"  2. 按日期导出（输入日期筛选）\")\nprint(\"  3. 完整版（含代码，全部对话）\")\nchoice = input(\"选择：\").strip()\n\nts = datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n\nif choice == \"1\":\n    print(\"\\n\" + build_output(data, include_details=False, date_filter=None))\n    input(\"\\n回车返回...\")\n\nelif choice == \"2\":\n    date = input(\"输入日期（如 2026-03-13，回车显示全部）：\").strip()\n    if not date:\n        date = None\n    content = build_output(data, include_details=False, date_filter=date)\n    path = os.path.join(DOWNLOAD, f\"zhongqu_context_{ts}.txt\")\n    with open(path, \"w\", encoding=\"utf-8\") as f:\n        f.write(content)\n    print(f\"\\n✅ 已导出：zhongqu_context_{ts}.txt\")\n    if date:\n        print(f\"   筛选日期：{date}\")\n    input(\"\\n回车返回...\")\n\nelif choice == \"3\":\n    content = build_output(data, include_details=True, date_filter=None)\n    path = os.path.join(DOWNLOAD, f\"zhongqu_context_{ts}.txt\")\n    with open(path, \"w\", encoding=\"utf-8\") as f:\n        f.write(content)\n    print(f\"\\n✅ 已导出完整版：zhongqu_context_{ts}.txt\")\n    input(\"\\n回车返回...\")\n\nelse:\n    print(\"⚠️ 无效选项\")\n    input(\"回车返回...\")",
          "memo": {
            "功能名称": "生成AI上下文",
            "原始需求": "生成给AI看的上下文，支持按日期筛选，带统计信息",
            "设计思路": "所有导出模式都显示统计信息（总条数/最新时间）",
            "注意事项": "日期筛选支持模糊匹配（输入03-13也能匹配）",
            "后续优化": "可加时间段导出"
          }
        },
        {
          "time": "2026-03-14 20:56",
          "code": "# @AI_MEMO_START\n# 功能名称：生成AI上下文\n# 原始需求：生成给AI看的上下文，支持按日期筛选对话记录\n# 设计思路：输入日期筛选当天记录，留空显示全部\n# 注意事项：日期格式灵活，只要time字段包含输入字符串即可\n# 后续优化：可加时间段导出\n# @AI_MEMO_END\n\nimport json, os\nfrom datetime import datetime\n\ntry:\n    DATA_FILE = _get_data_file()\nexcept NameError:\n    DATA_FILE = os.path.join(os.path.expanduser(\"~\"), \"zhongqu_data.json\")\n\nDOWNLOAD = \"/sdcard/Download\"\n\ndef load():\n    with open(DATA_FILE, encoding=\"utf-8\") as f:\n        return json.load(f)\n\ndef build_context_log(data, date_filter=None):\n    entries = data.get(\"context_log\", [])\n    if not entries:\n        return \"（暂无对话记录）\"\n    if date_filter:\n        filtered = [e for e in entries if date_filter in e.get('time', '')]\n        if not filtered:\n            return f\"（{date_filter} 没有对话记录）\"\n        times = [e.get('time', '') for e in filtered if e.get('time')]\n        latest = max(times) if times else '未知'\n        header = f\"【对话记录 - {date_filter}】（共 {len(filtered)} 条，最新：{latest}）\"\n        entries = filtered\n    else:\n        times = [e.get('time', '') for e in entries if e.get('time')]\n        latest = max(times) if times else '未知'\n        header = f\"【对话记录 - 全部】（共 {len(entries)} 条，最新：{latest}）\"\n    lines = [header, \"\"]\n    for e in entries:\n        lines.append(f\"[{e.get('time','')}]\\n{e.get('content','')}\")\n    return \"\\n\\n\".join(lines)\n\ndef build_output(data, include_details=False, date_filter=None):\n    lines = []\n    lines.append(\"=== 工具中枢上下文 ===\")\n    lines.append(f\"当前目标：{data.get('goal', '未设置')}\")\n    lines.append(\"\")\n\n    lines.append(\"【📁 中枢结构】\")\n    lines.append(\"运行在手机Termux里的Python系统：\")\n    lines.append(\"  · zhongqu.py        ← 80行终极内核（永不修改）\")\n    lines.append(\"  · zhongqu_data.json ← 所有功能数据\")\n    lines.append(\"\")\n\n    lines.append(\"【🤖 AI开发规范】\")\n    lines.append(\"1️⃣ 所有功能都能直接调用内核函数：_atomic_save、_snap、_log、_now\")\n    lines.append(\"2️⃣ 菜单规则：只显示1-9系统功能，工具功能不显示\")\n    lines.append(\"3️⃣ JSON结构：action的key是字符串，code字段直接exec\")\n    lines.append(\"4️⃣ 对话记录存在 json顶层 context_log 字段\")\n    lines.append(\"\")\n\n    lines.append(\"【指令协议】\")\n    lines.append(\"新AI收到上下文后，先说「看」确认进入状态，再接受指令。\")\n    lines.append(\"\")\n    lines.append(\"看\")\n    lines.append(\"→ 确认已读取上下文，输出当前状态简报，进入就绪状态\")\n    lines.append(\"\")\n    lines.append(\"存：[名称]\")\n    lines.append(\"→ 把本次对话核心提炼成结构化条目，格式：\")\n    lines.append(\"  ★ [日期] 【主题】\")\n    lines.append(\"  ↳ 核心结论\")\n    lines.append(\"  ↳ 详细说明\")\n    lines.append(\"    · 子要点\")\n    lines.append(\"  ↳ 注意事项\")\n    lines.append(\"→ 用户操作：回中枢运行「对话记录」选1粘贴导入\")\n    lines.append(\"\")\n    lines.append(\"写：[名称] 需求：[一句话]\")\n    lines.append(\"→ 生成新功能完整代码，严格要求：\")\n    lines.append(\"  · 含完整备忘录（@AI_MEMO_START...@AI_MEMO_END）\")\n    lines.append(\"  · exec环境兼容（try/except兜底_data_file/_base_dir）\")\n    lines.append(\"  · 使用内核函数（_atomic_save/_snap/_log/_now）\")\n    lines.append(\"→ 用户操作：复制后选1「添加新功能」粘贴\")\n    lines.append(\"\")\n    lines.append(\"改：[功能名] 需求：[一句话]\")\n    lines.append(\"→ 生成修改后完整代码，严格要求同「写」\")\n    lines.append(\"→ 用户操作：复制后选3「修改功能」替换\")\n    lines.append(\"\")\n    lines.append(\"发：[内容]\")\n    lines.append(\"→ 判断内容性质，自动整理成可直接使用的格式输出\")\n    lines.append(\"\")\n    lines.append(\"裁：[分歧描述]\")\n    lines.append(\"→ 1. 列出双方核心主张，不偏不倚\")\n    lines.append(\"→ 2. 分析各自代价（选A失去什么，选B失去什么）\")\n    lines.append(\"→ 3. 给出倾向建议及理由\")\n    lines.append(\"→ 4. 判断权在用户，AI不替人决定\")\n    lines.append(\"\")\n\n    lines.append(\"【功能清单】\")\n    actions = data.get(\"actions\", {})\n    system_keys = [\"1\",\"2\",\"3\",\"4\",\"5\",\"6\",\"7\",\"8\",\"9\"]\n    lines.append(\"系统功能：\" + \"、\".join(\n        f\"{k}.{v['name']}\" for k,v in actions.items() if k in system_keys\n    ))\n    tool_fns = [(k,v) for k,v in actions.items() if k not in system_keys]\n    if tool_fns:\n        lines.append(\"工具功能：\")\n        for k,v in tool_fns:\n            lines.append(f\"  · {v['name']}\")\n    lines.append(\"\")\n\n    lines.append(\"【最近操作】\")\n    for log in data.get(\"log\", [])[-5:]:\n        lines.append(f\"  {log}\")\n    lines.append(\"\")\n\n    lines.append(build_context_log(data, date_filter))\n    lines.append(\"\")\n\n    lines.append(\"=== 上下文结束 ===\")\n\n    if include_details:\n        details = [\"\\n=== 功能详情 ===\\n\"]\n        for k,v in tool_fns:\n            details.append(f\"\\n【{v['name']}】\")\n            for field, val in v.get(\"memo\", {}).items():\n                details.append(f\"  {field}：{val}\")\n            details.append(\"  代码：\")\n            details.append(v.get(\"code\", \"\"))\n        lines.extend(details)\n\n    return \"\\n\".join(lines)\n\ndata = load()\n\nprint(\"\\n导出模式：\")\nprint(\"  1. 摘要版（屏幕显示，全部对话）\")\nprint(\"  2. 按日期导出（输入日期筛选）\")\nprint(\"  3. 完整版（含代码，全部对话）\")\nchoice = input(\"选择：\").strip()\n\nts = datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n\nif choice == \"1\":\n    print(\"\\n\" + build_output(data, include_details=False, date_filter=None))\n    input(\"\\n回车返回...\")\n\nelif choice == \"2\":\n    date = input(\"输入日期（如 2026-03-13，回车显示全部）：\").strip()\n    if not date:\n        date = None\n    content = build_output(data, include_details=False, date_filter=date)\n    path = os.path.join(DOWNLOAD, f\"zhongqu_context_{ts}.txt\")\n    with open(path, \"w\", encoding=\"utf-8\") as f:\n        f.write(content)\n    print(f\"\\n✅ 已导出：zhongqu_context_{ts}.txt\")\n    if date:\n        print(f\"   筛选日期：{date}\")\n    input(\"\\n回车返回...\")\n\nelif choice == \"3\":\n    content = build_output(data, include_details=True, date_filter=None)\n    path = os.path.join(DOWNLOAD, f\"zhongqu_context_{ts}.txt\")\n    with open(path, \"w\", encoding=\"utf-8\") as f:\n        f.write(content)\n    print(f\"\\n✅ 已导出完整版：zhongqu_context_{ts}.txt\")\n    input(\"\\n回车返回...\")\n\nelse:\n    print(\"⚠️ 无效选项\")\n    input(\"回车返回...\")",
          "memo": {
            "功能名称": "生成AI上下文",
            "原始需求": "生成给AI看的上下文，支持按日期筛选对话记录",
            "设计思路": "输入日期筛选当天记录，留空显示全部",
            "注意事项": "日期格式灵活，只要time字段包含输入字符串即可",
            "后续优化": "可加时间段导出"
          }
        },
        {
          "time": "2026-03-15 11:57",
          "code": "# @AI_MEMO_START\n# 功能名称：生成AI上下文\n# 原始需求：生成给AI看的上下文，支持按日期筛选，带统计信息\n# 设计思路：所有导出模式都显示统计信息（总条数/最新时间）\n# 注意事项：日期筛选支持模糊匹配（输入03-13也能匹配）\n# 后续优化：可加时间段导出\n# @AI_MEMO_END\n\nimport json, os\nfrom datetime import datetime\n\ntry:\n    DATA_FILE = _get_data_file()\nexcept NameError:\n    DATA_FILE = os.path.join(os.path.expanduser(\"~\"), \"zhongqu_data.json\")\n\nDOWNLOAD = \"/sdcard/Download\"\n\ndef load():\n    with open(DATA_FILE, encoding=\"utf-8\") as f:\n        return json.load(f)\n\ndef build_context_log(data, date_filter=None):\n    entries = data.get(\"context_log\", [])\n    if not entries:\n        return \"（暂无对话记录）\"\n    total = len(entries)\n    all_times = [e.get('time', '') for e in entries if e.get('time')]\n    latest_all = max(all_times) if all_times else '未知'\n    if date_filter:\n        filtered = [e for e in entries if date_filter in e.get('time', '')]\n        if not filtered:\n            return f\"（{date_filter} 没有对话记录）\"\n        count = len(filtered)\n        times = [e.get('time', '') for e in filtered if e.get('time')]\n        latest = max(times) if times else '未知'\n        header = f\"【对话记录 - {date_filter}】（共 {count} 条，最新：{latest}）\"\n        entries = filtered\n    else:\n        header = f\"【对话记录 - 全部】（共 {total} 条，最新：{latest_all}）\"\n    lines = [header]\n    for e in entries:\n        lines.append(f\"\\n[{e.get('time','')}]\\n{e.get('content','')}\")\n    return \"\\n\".join(lines)\n\ndef build_output(data, include_details=False, date_filter=None):\n    lines = []\n    lines.append(\"=== 工具中枢上下文 ===\")\n    lines.append(f\"当前目标：{data.get('goal', '未设置')}\")\n    lines.append(\"\")\n\n    lines.append(\"【📁 中枢结构】\")\n    lines.append(\"运行在手机Termux里的Python系统：\")\n    lines.append(\"  · zhongqu.py        ← 80行终极内核（永不修改）\")\n    lines.append(\"  · zhongqu_data.json ← 所有功能数据\")\n    lines.append(\"\")\n\n    lines.append(\"【🤖 AI开发规范】\")\n    lines.append(\"1️⃣ 所有功能都能直接调用内核函数：_atomic_save、_snap、_log、_now\")\n    lines.append(\"2️⃣ 菜单规则：只显示1-9系统功能，工具功能不显示\")\n    lines.append(\"3️⃣ JSON结构：action的key是字符串，code字段直接exec\")\n    lines.append(\"4️⃣ 对话记录存在 json顶层 context_log 字段\")\n    lines.append(\"\")\n\n    lines.append(\"【指令协议】\")\n    lines.append(\"新AI收到上下文后，先说「看」确认进入状态，再接受指令。\")\n    lines.append(\"\")\n    lines.append(\"看\")\n    lines.append(\"→ 确认已读取上下文，输出当前状态简报，进入就绪状态\")\n    lines.append(\"\")\n    lines.append(\"存：[名称]\")\n    lines.append(\"→ 把本次对话核心提炼成结构化条目，格式：\")\n    lines.append(\"  ★ [日期] 【主题】\")\n    lines.append(\"  ↳ 核心结论\")\n    lines.append(\"  ↳ 详细说明\")\n    lines.append(\"    · 子要点\")\n    lines.append(\"  ↳ 注意事项\")\n    lines.append(\"→ 用户操作：回中枢运行「对话记录」选1粘贴导入\")\n    lines.append(\"\")\n    lines.append(\"写：[名称] 需求：[一句话]\")\n    lines.append(\"→ 生成新功能完整代码，严格要求：\")\n    lines.append(\"  · 含完整备忘录（@AI_MEMO_START...@AI_MEMO_END）\")\n    lines.append(\"  · exec环境兼容（try/except兜底_data_file/_base_dir）\")\n    lines.append(\"  · 使用内核函数（_atomic_save/_snap/_log/_now）\")\n    lines.append(\"→ 用户操作：复制后选1「添加新功能」粘贴\")\n    lines.append(\"\")\n    lines.append(\"改：[功能名] 需求：[一句话]\")\n    lines.append(\"→ 生成修改后完整代码，严格要求同「写」\")\n    lines.append(\"→ 用户操作：复制后选3「修改功能」替换\")\n    lines.append(\"\")\n    lines.append(\"发：[内容]\")\n    lines.append(\"→ 判断内容性质，自动整理成可直接使用的格式输出\")\n    lines.append(\"\")\n    lines.append(\"裁：[分歧描述]\")\n    lines.append(\"→ 1. 列出双方核心主张，不偏不倚\")\n    lines.append(\"→ 2. 分析各自代价（选A失去什么，选B失去什么）\")\n    lines.append(\"→ 3. 给出倾向建议及理由\")\n    lines.append(\"→ 4. 判断权在用户，AI不替人决定\")\n    lines.append(\"\")\n\n    lines.append(\"【功能清单】\")\n    actions = data.get(\"actions\", {})\n    system_keys = [\"1\",\"2\",\"3\",\"4\",\"5\",\"6\",\"7\",\"8\",\"9\"]\n    lines.append(\"系统功能：\" + \"、\".join(\n        f\"{k}.{v['name']}\" for k,v in actions.items() if k in system_keys\n    ))\n    tool_fns = [(k,v) for k,v in actions.items() if k not in system_keys]\n    if tool_fns:\n        lines.append(\"工具功能：\")\n        for k,v in tool_fns:\n            lines.append(f\"  · {v['name']}\")\n    lines.append(\"\")\n\n    lines.append(\"【最近操作】\")\n    for log in data.get(\"log\", [])[-5:]:\n        lines.append(f\"  {log}\")\n    lines.append(\"\")\n\n    lines.append(build_context_log(data, date_filter))\n    lines.append(\"\")\n\n    lines.append(\"=== 上下文结束 ===\")\n\n    if include_details:\n        details = [\"\\n=== 功能详情 ===\\n\"]\n        for k,v in tool_fns:\n            if k == \"对话记录\":\n                continue\n            details.append(f\"\\n【{v['name']}】\")\n            for field, val in v.get(\"memo\", {}).items():\n                details.append(f\"  {field}：{val}\")\n            details.append(\"  代码：\")\n            details.append(v.get(\"code\", \"\"))\n        lines.extend(details)\n\n    return \"\\n\".join(lines)\n\ndata = load()\n\nprint(\"\\n导出模式：\")\nprint(\"  1. 摘要版（屏幕显示，全部对话）\")\nprint(\"  2. 按日期导出（输入日期筛选）\")\nprint(\"  3. 完整版（含代码，全部对话）\")\nchoice = input(\"选择：\").strip()\n\nts = datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n\nif choice == \"1\":\n    print(\"\\n\" + build_output(data, include_details=False, date_filter=None))\n    input(\"\\n回车返回...\")\n\nelif choice == \"2\":\n    date = input(\"输入日期（如 2026-03-13，回车显示全部）：\").strip()\n    if not date:\n        date = None\n    content = build_output(data, include_details=False, date_filter=date)\n    path = os.path.join(DOWNLOAD, f\"zhongqu_context_{ts}.txt\")\n    with open(path, \"w\", encoding=\"utf-8\") as f:\n        f.write(content)\n    print(f\"\\n✅ 已导出：zhongqu_context_{ts}.txt\")\n    if date:\n        print(f\"   筛选日期：{date}\")\n    input(\"\\n回车返回...\")\n\nelif choice == \"3\":\n    content = build_output(data, include_details=True, date_filter=None)\n    path = os.path.join(DOWNLOAD, f\"zhongqu_context_{ts}.txt\")\n    with open(path, \"w\", encoding=\"utf-8\") as f:\n        f.write(content)\n    print(f\"\\n✅ 已导出完整版：zhongqu_context_{ts}.txt\")\n    input(\"\\n回车返回...\")\n\nelse:\n    print(\"⚠️ 无效选项\")\n    input(\"回车返回...\")",
          "memo": {
            "功能名称": "生成AI上下文",
            "原始需求": "生成给AI看的上下文，支持按日期筛选，带统计信息",
            "设计思路": "所有导出模式都显示统计信息（总条数/最新时间）",
            "注意事项": "日期筛选支持模糊匹配（输入03-13也能匹配）",
            "后续优化": "可加时间段导出"
          }
        }
      ]
    },
    "9": {
      "name": "查看功能详情",
      "desc": "查看备忘录、历史版本，可恢复旧版本",
      "code": "\nimport json, os, shutil\nfrom datetime import datetime\nDATA_FILE = _data_file\nBASE_DIR = _base_dir\nSNAP_DIR = os.path.join(BASE_DIR,\"zhongqu_snapshots\")\ndef _load():\n    with open(DATA_FILE,encoding=\"utf-8\") as f: return json.load(f)\ndef _save(data):\n    tmp=DATA_FILE+\".tmp\"\n    with open(tmp,\"w\",encoding=\"utf-8\") as f: json.dump(data,f,ensure_ascii=False,indent=2)\n    os.replace(tmp,DATA_FILE)\ndata=_load()\nfns={k:v for k,v in data[\"actions\"].items() if k not in [\"1\",\"2\",\"3\",\"4\",\"5\",\"6\",\"7\",\"8\",\"9\"]}\nif not fns:\n    print(\"⚠️  还没有任何工具功能。\"); input(\"回车继续...\")\nelse:\n    names=list(fns.keys())\n    print(\"\\n选择要查看的功能：\")\n    for i,n in enumerate(names,1): print(f\"  {i}. {n} — {fns[n].get('desc','')[:30]}\")\n    choice=input(\"编号：\").strip()\n    try:\n        name=names[int(choice)-1]\n        info=data[\"actions\"][name]\n        memo=info.get(\"memo\",{})\n        print(f\"\\n{'='*45}\\n📦 {name}\\n{'='*45}\")\n        for k,v in memo.items(): print(f\"  {k}：{v}\")\n        print(f\"\\n  添加时间：{info.get('added','')}\")\n        if info.get(\"modified\"): print(f\"  最后修改：{info['modified']}\")\n        history=info.get(\"history\",[])\n        if history:\n            print(f\"\\n  历史版本：{len(history)} 个\")\n            if input(\"  查看历史？(y/n)：\").strip().lower()==\"y\":\n                for i,h in enumerate(reversed(history),1):\n                    print(f\"    {i}. {h['time']} — {h.get('memo',{}).get('后续优化','')[:25]}\")\n                roll=input(\"\\n  恢复某版本？输入编号（回车跳过）：\").strip()\n                if roll:\n                    try:\n                        chosen=list(reversed(history))[int(roll)-1]\n                        if input(f\"  确认恢复到{chosen['time']}？(y/n)：\").strip().lower()==\"y\":\n                            os.makedirs(SNAP_DIR,exist_ok=True)\n                            ts=datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n                            if os.path.exists(DATA_FILE): shutil.copy2(DATA_FILE,os.path.join(SNAP_DIR,f\"snapshot_{ts}.json\"))\n                            hist=info.get(\"history\",[])\n                            hist.append({\"time\":datetime.now().strftime(\"%Y-%m-%d %H:%M\"),\"code\":info[\"code\"],\"memo\":info.get(\"memo\",{})})\n                            data[\"actions\"][name]={**info,\"code\":chosen[\"code\"],\"memo\":chosen.get(\"memo\",{}),\"modified\":datetime.now().strftime(\"%Y-%m-%d %H:%M\"),\"history\":hist[-10:]}\n                            _save(data); print(f\"\\n✅ 已恢复到{chosen['time']}的版本。\")\n                    except (ValueError,IndexError): print(\"⚠️  无效编号。\")\n        else: print(\"\\n  暂无历史版本。\")\n    except (ValueError,IndexError): print(\"⚠️  无效编号。\")\n    input(\"\\n回车继续...\")\n"
    },
    "导出py和json文件": {
      "name": "导出py和json文件",
      "code": "# @AI_MEMO_START\n# 功能名称：导出py和json文件\n# 原始需求：把zhongqu.py和zhongqu_data.json导出到/sdcard/Download/，方便备份或转移\n# 设计思路：用try/except NameError兜底，兼容添加时和运行时两种exec环境\n# 注意事项：添加时exec不传_data_file，用NameError兜底走expanduser路径\n# 后续优化：无\n# @AI_MEMO_END\n\nimport os, shutil\nfrom datetime import datetime\n\ntry:\n    base = os.path.dirname(_data_file)\n    json_src = _data_file\nexcept NameError:\n    base = os.path.expanduser(\"~\")\n    json_src = os.path.join(base, \"zhongqu_data.json\")\n\nDOWNLOAD = \"/sdcard/Download\"\nts = datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n\npy_src   = os.path.join(base, \"zhongqu.py\")\npy_dst   = os.path.join(DOWNLOAD, f\"zhongqu_{ts}.py\")\njson_dst = os.path.join(DOWNLOAD, f\"zhongqu_data_{ts}.json\")\n\nprint(f\"\\n准备导出：\")\nprint(f\"  py   → {py_dst}\")\nprint(f\"  json → {json_dst}\")\nconfirm = input(\"\\n确认导出？(y/n)：\").strip()\n\nif confirm == \"y\":\n    ok = []\n    fail = []\n    for src, dst in [(py_src, py_dst), (json_src, json_dst)]:\n        try:\n            shutil.copy2(src, dst)\n            ok.append(dst)\n        except Exception as e:\n            fail.append(f\"{src} → {e}\")\n    if ok:\n        print(\"\\n✅ 导出成功：\")\n        for p in ok: print(f\"  {p}\")\n    if fail:\n        print(\"\\n❌ 导出失败：\")\n        for p in fail: print(f\"  {p}\")\nelse:\n    print(\"已取消\")\n\ninput(\"\\n回车返回...\")",
      "memo": {
        "功能名称": "导出py和json文件",
        "原始需求": "把zhongqu.py和zhongqu_data.json导出到/sdcard/Download/，方便备份或转移",
        "设计思路": "用try/except NameError兜底，兼容添加时和运行时两种exec环境",
        "注意事项": "添加时exec不传_data_file，用NameError兜底走expanduser路径",
        "后续优化": "无"
      },
      "desc": "把zhongqu.py和zhongqu_data.json导出到/sdcard/Download/，方便备份或转移",
      "added": "2026-03-11 17:21",
      "history": []
    },
    "中枢云同步": {
      "name": "中枢云同步",
      "code": "# @AI_MEMO_START\n# 功能名称：中枢云同步\n# 原始需求：用Gitee私有仓库备份zhongqu_data.json，支持推送/拉取，首次运行配置token存本地，换手机能恢复\n# 设计思路：双远端Gitee+GitHub独立推送互不影响；同时备份json和py；拉取用临时目录克隆更稳；拉取前双快照\n# 注意事项：推送串行执行，一个失败不影响另一个；拉取选其一，不一起拉；tmp目录用完自动清理\n# 后续优化：无\n# @AI_MEMO_END\n\nimport os, json, shutil, subprocess\nfrom datetime import datetime\n\ntry:\n    base = os.path.dirname(_data_file)\n    json_src = _data_file\nexcept NameError:\n    base = os.path.expanduser(\"~\")\n    json_src = os.path.join(base, \"zhongqu_data.json\")\n\nCONF_FILE = os.path.join(os.path.expanduser(\"~\"), \".zhongqu_git.conf\")\nSNAP_DIR  = os.path.join(base, \"zhongqu_snapshots\")\nREPO_DIR  = os.path.join(os.path.expanduser(\"~\"), \"zhongqu_backup_repo\")\nPY_SRC    = os.path.join(base, \"zhongqu.py\")\n\ndef load_conf():\n    if os.path.exists(CONF_FILE):\n        with open(CONF_FILE, encoding=\"utf-8\") as f:\n            return json.load(f)\n    return None\n\ndef save_conf(conf):\n    with open(CONF_FILE, \"w\", encoding=\"utf-8\") as f:\n        json.dump(conf, f, ensure_ascii=False, indent=2)\n    os.chmod(CONF_FILE, 0o600)\n\ndef run(cmd, cwd=None):\n    r = subprocess.run(cmd, shell=True, capture_output=True, text=True, cwd=cwd)\n    return r.returncode, r.stdout.strip(), r.stderr.strip()\n\nconf = load_conf()\nneed_reconf = False\nif not conf:\n    need_reconf = True\nelif \"gitee_username\" not in conf or \"github_token\" not in conf:\n    print(\"⚠️  检测到旧配置，需要升级为双远端配置\\n\")\n    need_reconf = True\n\nif need_reconf:\n    print(\"⚙️  配置双远端，token只存本地，永不上传\\n\")\n    gitee_username  = input(\"Gitee 用户名：\").strip()\n    gitee_token     = input(\"Gitee 私人令牌：\").strip()\n    gitee_repo      = input(\"Gitee 仓库路径（如 xwin7p/zhongqu-backup）：\").strip()\n    github_username = input(\"GitHub 用户名：\").strip()\n    github_token    = input(\"GitHub 私人令牌（ghp_开头）：\").strip()\n    github_repo     = input(\"GitHub 仓库路径（如 xwin7p/zhongqu-backup）：\").strip()\n    conf = {\n        \"gitee_username\":  gitee_username,\n        \"gitee_token\":     gitee_token,\n        \"gitee_repo\":      gitee_repo,\n        \"github_username\": github_username,\n        \"github_token\":    github_token,\n        \"github_repo\":     github_repo,\n    }\n    save_conf(conf)\n    if os.path.exists(REPO_DIR):\n        shutil.rmtree(REPO_DIR)\n    print(\"✅ 配置已保存\\n\")\n\nGITEE_URL  = f\"https://{conf['gitee_username']}:{conf['gitee_token']}@gitee.com/{conf['gitee_repo']}.git\"\nGITHUB_URL = f\"https://{conf['github_username']}:{conf['github_token']}@github.com/{conf['github_repo']}.git\"\n\ndef ensure_repo():\n    if not os.path.exists(REPO_DIR):\n        print(\"   首次使用，正在从Gitee克隆...\")\n        code, out, err = run(f\"git clone {GITEE_URL} {REPO_DIR}\")\n        if code != 0:\n            print(f\"❌ 克隆失败：{err}\")\n            return False\n        print(\"   ✅ 克隆成功\")\n    return True\n\ndef push():\n    print(\"\\n📤 推送 本地 → Gitee + GitHub ...\")\n    if not ensure_repo():\n        return\n    try:\n        shutil.copy2(json_src, os.path.join(REPO_DIR, \"zhongqu_data.json\"))\n        if os.path.exists(PY_SRC):\n            shutil.copy2(PY_SRC, os.path.join(REPO_DIR, \"zhongqu.py\"))\n        ts = datetime.now().strftime(\"%Y-%m-%d %H:%M\")\n        run(\"git add .\", cwd=REPO_DIR)\n        code, out, err = run(f'git commit -m \"backup {ts}\"', cwd=REPO_DIR)\n        if \"nothing to commit\" in out or \"nothing to commit\" in err:\n            print(\"✅ 无新变更，无需推送\")\n            return\n\n        print(\"   推送到 Gitee ...\")\n        c1, o1, e1 = run(f\"git push {GITEE_URL} master\", cwd=REPO_DIR)\n        print(\"   ✅ Gitee 成功\" if c1 == 0 else f\"   ⚠️ Gitee 失败：{e1}\")\n\n        print(\"   推送到 GitHub ...\")\n        c2, o2, e2 = run(f\"git push {GITHUB_URL} master\", cwd=REPO_DIR)\n        print(\"   ✅ GitHub 成功\" if c2 == 0 else f\"   ⚠️ GitHub 失败：{e2}\")\n\n        if c1 == 0 or c2 == 0:\n            print(f\"\\n✅ 推送完成（{ts}）\")\n        else:\n            print(\"\\n❌ 两个远端都推送失败\")\n    except Exception as e:\n        print(f\"❌ 错误：{e}\")\n\ndef pull(url, name):\n    confirm = input(f\"⚠️  会覆盖本地文件，确认从 {name} 拉取？(y/n)：\").strip()\n    if confirm != \"y\":\n        print(\"已取消\"); return\n    tmp_dir = REPO_DIR + \"_tmp\"\n    try:\n        if os.path.exists(tmp_dir):\n            shutil.rmtree(tmp_dir)\n        print(f\"   正在从 {name} 克隆...\")\n        code, out, err = run(f\"git clone {url} {tmp_dir}\")\n        if code != 0:\n            print(f\"❌ 克隆失败：{err}\"); return\n        remote_json = os.path.join(tmp_dir, \"zhongqu_data.json\")\n        remote_py   = os.path.join(tmp_dir, \"zhongqu.py\")\n        if not os.path.exists(remote_json):\n            print(f\"❌ {name} 上没有备份文件\"); return\n        os.makedirs(SNAP_DIR, exist_ok=True)\n        ts = datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n        shutil.copy2(json_src, os.path.join(SNAP_DIR, f\"snapshot_{ts}.json\"))\n        if os.path.exists(PY_SRC):\n            shutil.copy2(PY_SRC, os.path.join(SNAP_DIR, f\"zhongqu_py_{ts}.py\"))\n        shutil.copy2(remote_json, json_src)\n        if os.path.exists(remote_py):\n            shutil.copy2(remote_py, PY_SRC)\n            print(\"   ✅ zhongqu.py 已恢复\")\n        print(f\"✅ 从 {name} 拉取成功，本地已更新\")\n    except Exception as e:\n        print(f\"❌ 错误：{e}\")\n    finally:\n        if os.path.exists(tmp_dir):\n            shutil.rmtree(tmp_dir)\n\ndef check():\n    print(\"\\n🔍 检查本地提交记录...\")\n    if not ensure_repo():\n        return\n    code, out, err = run(\"git log --oneline -5\", cwd=REPO_DIR)\n    if code == 0 and out:\n        print(\"✅ 最近5次提交：\")\n        for line in out.split(\"\\n\"):\n            print(f\"   {line}\")\n    else:\n        print(\"📭 暂无提交记录\")\n\nprint(\"=\" * 35)\nprint(\"  中枢云同步 · 双远端\")\nprint(\"=\" * 35)\nprint(f\"  Gitee:  {conf.get('gitee_repo','未配置')}\")\nprint(f\"  GitHub: {conf.get('github_repo','未配置')}\")\nprint(\"-\" * 35)\nprint(\"  1. 推送  本地 → Gitee + GitHub\")\nprint(\"  2. 拉取  Gitee → 本地（日常）\")\nprint(\"  3. 拉取  GitHub → 本地（紧急）\")\nprint(\"  4. 检查  提交记录\")\nprint(\"  5. 重新配置\")\nprint(\"-\" * 35)\n\nchoice = input(\"选择：\").strip()\nif choice == \"1\":\n    push()\nelif choice == \"2\":\n    pull(GITEE_URL, \"Gitee\")\nelif choice == \"3\":\n    pull(GITHUB_URL, \"GitHub\")\nelif choice == \"4\":\n    check()\nelif choice == \"5\":\n    os.remove(CONF_FILE)\n    if os.path.exists(REPO_DIR):\n        shutil.rmtree(REPO_DIR)\n    print(\"✅ 配置已清除，下次运行重新填写\")\nelse:\n    print(\"无效选择\")\n\ninput(\"\\n回车返回...\")",
      "memo": {
        "功能名称": "中枢云同步",
        "原始需求": "用Gitee私有仓库备份zhongqu_data.json，支持推送/拉取，首次运行配置token存本地，换手机能恢复",
        "设计思路": "双远端Gitee+GitHub独立推送互不影响；同时备份json和py；拉取用临时目录克隆更稳；拉取前双快照",
        "注意事项": "推送串行执行，一个失败不影响另一个；拉取选其一，不一起拉；tmp目录用完自动清理",
        "后续优化": "无"
      },
      "desc": "用Gitee私有仓库备份zhongqu_data.json，支持推送/拉取，首次运行配置token存本地，换手机能恢复",
      "added": "2026-03-11 22:41",
      "modified": "2026-03-12 10:14",
      "history": [
        {
          "time": "2026-03-11 22:58",
          "code": "# @AI_MEMO_START\n# 功能名称：中枢云同步\n# 原始需求：用Gitee私有仓库备份zhongqu_data.json，支持推送/拉取，首次运行配置token存本地，换手机能恢复\n# 设计思路：用git命令行操作，token存~/.zhongqu_git.conf，每次推送自动commit带时间戳，拉取前做本地快照\n# 注意事项：token只存本地不进json；仓库地址固定为xwin7p/zhongqu-backup；分支是master\n# 后续优化：可加自动推送\n# @AI_MEMO_END\n\nimport os, json, shutil, subprocess\nfrom datetime import datetime\n\ntry:\n    base = os.path.dirname(_data_file)\n    json_src = _data_file\nexcept NameError:\n    base = os.path.expanduser(\"~\")\n    json_src = os.path.join(base, \"zhongqu_data.json\")\n\nCONF_FILE = os.path.join(os.path.expanduser(\"~\"), \".zhongqu_git.conf\")\nSNAP_DIR = os.path.join(base, \"zhongqu_snapshots\")\nREPO_DIR = os.path.join(os.path.expanduser(\"~\"), \"zhongqu_backup_repo\")\nREPO_NAME = \"xwin7p/zhongqu-backup\"\n\ndef load_conf():\n    if os.path.exists(CONF_FILE):\n        with open(CONF_FILE, encoding=\"utf-8\") as f:\n            return json.load(f)\n    return None\n\ndef save_conf(conf):\n    with open(CONF_FILE, \"w\", encoding=\"utf-8\") as f:\n        json.dump(conf, f, ensure_ascii=False, indent=2)\n    os.chmod(CONF_FILE, 0o600)\n\ndef run(cmd, cwd=None):\n    r = subprocess.run(cmd, shell=True, capture_output=True, text=True, cwd=cwd)\n    return r.returncode, r.stdout.strip(), r.stderr.strip()\n\nconf = load_conf()\nif not conf:\n    print(\"⚙️  首次配置，token只存本地，永不上传\\n\")\n    token = input(\"Gitee 私人令牌：\").strip()\n    conf = {\"token\": token, \"repo\": REPO_NAME, \"host\": \"gitee.com\"}\n    save_conf(conf)\n    print(\"✅ 配置已保存到本地\\n\")\n\nTOKEN = conf[\"token\"]\nHOST = conf.get(\"host\", \"gitee.com\")\nREPO_URL = f\"https://{TOKEN}@{HOST}/{conf['repo']}.git\"\n\ndef ensure_repo():\n    if not os.path.exists(REPO_DIR):\n        print(\"   首次使用，正在克隆仓库...\")\n        code, out, err = run(f\"git clone {REPO_URL} {REPO_DIR}\")\n        if code != 0:\n            print(f\"❌ 克隆失败：{err}\")\n            return False\n        print(\"   ✅ 克隆成功\")\n    return True\n\ndef push():\n    print(\"\\n📤 推送本地 → Gitee...\")\n    if not ensure_repo():\n        return\n    try:\n        shutil.copy2(json_src, os.path.join(REPO_DIR, \"zhongqu_data.json\"))\n        ts = datetime.now().strftime(\"%Y-%m-%d %H:%M\")\n        run(\"git add zhongqu_data.json\", cwd=REPO_DIR)\n        code, out, err = run(f'git commit -m \"backup {ts}\"', cwd=REPO_DIR)\n        if \"nothing to commit\" in out or \"nothing to commit\" in err:\n            print(\"✅ 云端已是最新，无需推送\")\n            return\n        code, out, err = run(f\"git push {REPO_URL} master\", cwd=REPO_DIR)\n        if code == 0:\n            print(f\"✅ 推送成功（{ts}）\")\n        else:\n            print(f\"❌ 推送失败：{err}\")\n    except Exception as e:\n        print(f\"❌ 错误：{e}\")\n\ndef pull():\n    print(\"\\n📥 拉取Gitee → 本地...\")\n    confirm = input(\"⚠️  会覆盖本地文件，确认？(y/n)：\").strip()\n    if confirm != \"y\":\n        print(\"已取消\")\n        return\n    if not ensure_repo():\n        return\n    try:\n        code, out, err = run(\"git pull\", cwd=REPO_DIR)\n        if code != 0:\n            print(f\"❌ 拉取失败：{err}\")\n            return\n        remote_file = os.path.join(REPO_DIR, \"zhongqu_data.json\")\n        if not os.path.exists(remote_file):\n            print(\"❌ 云端还没有备份，请先推送一次\")\n            return\n        os.makedirs(SNAP_DIR, exist_ok=True)\n        ts = datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n        shutil.copy2(json_src, os.path.join(SNAP_DIR, f\"snapshot_{ts}.json\"))\n        shutil.copy2(remote_file, json_src)\n        print(\"✅ 拉取成功，本地已更新\")\n    except Exception as e:\n        print(f\"❌ 错误：{e}\")\n\ndef check():\n    print(\"\\n🔍 检查云端状态...\")\n    if not ensure_repo():\n        return\n    try:\n        run(\"git fetch\", cwd=REPO_DIR)\n        code, out, err = run(\"git log --oneline -5\", cwd=REPO_DIR)\n        if code == 0 and out:\n            print(\"✅ 云端最近5次提交：\")\n            for line in out.split(\"\\n\"):\n                print(f\"   {line}\")\n        else:\n            print(\"📭 云端暂无提交记录\")\n    except Exception as e:\n        print(f\"❌ 错误：{e}\")\n\nprint(\"=\" * 35)\nprint(\"  中枢云同步 · Gitee\")\nprint(\"=\" * 35)\nprint(f\"  仓库：{conf['repo']}\")\nprint(\"-\" * 35)\nprint(\"  1. 推送  本地→Gitee（备份）\")\nprint(\"  2. 拉取  Gitee→本地（恢复）\")\nprint(\"  3. 检查  云端提交记录\")\nprint(\"  4. 重新配置token\")\nprint(\"-\" * 35)\n\nchoice = input(\"选择：\").strip()\nif choice == \"1\":\n    push()\nelif choice == \"2\":\n    pull()\nelif choice == \"3\":\n    check()\nelif choice == \"4\":\n    os.remove(CONF_FILE)\n    if os.path.exists(REPO_DIR):\n        shutil.rmtree(REPO_DIR)\n    print(\"✅ 配置已清除，下次运行重新填写\")\nelse:\n    print(\"无效选择\")\n\ninput(\"\\n回车返回...\")",
          "memo": {
            "功能名称": "中枢云同步",
            "原始需求": "用Gitee私有仓库备份zhongqu_data.json，支持推送/拉取，首次运行配置token存本地，换手机能恢复",
            "设计思路": "用git命令行操作，token存~/.zhongqu_git.conf，每次推送自动commit带时间戳，拉取前做本地快照",
            "注意事项": "token只存本地不进json；仓库地址固定为xwin7p/zhongqu-backup；分支是master",
            "后续优化": "可加自动推送"
          }
        },
        {
          "time": "2026-03-12 10:14",
          "code": "# @AI_MEMO_START\n# 功能名称：中枢云同步\n# 原始需求：用Gitee私有仓库备份zhongqu_data.json，支持推送/拉取，首次运行配置token存本地，换手机能恢复\n# 设计思路：用git命令行操作，token存~/.zhongqu_git.conf，URL格式必须是username:token@gitee.com才能认证\n# 注意事项：Gitee认证URL必须带用户名：https://用户名:token@gitee.com/...，只有token会报auth失败\n# 后续优化：可加自动推送\n# @AI_MEMO_END\n\nimport os, json, shutil, subprocess\nfrom datetime import datetime\n\ntry:\n    base = os.path.dirname(_data_file)\n    json_src = _data_file\nexcept NameError:\n    base = os.path.expanduser(\"~\")\n    json_src = os.path.join(base, \"zhongqu_data.json\")\n\nCONF_FILE = os.path.join(os.path.expanduser(\"~\"), \".zhongqu_git.conf\")\nSNAP_DIR = os.path.join(base, \"zhongqu_snapshots\")\nREPO_DIR = os.path.join(os.path.expanduser(\"~\"), \"zhongqu_backup_repo\")\nREPO_NAME = \"xwin7p/zhongqu-backup\"\n\ndef load_conf():\n    if os.path.exists(CONF_FILE):\n        with open(CONF_FILE, encoding=\"utf-8\") as f:\n            return json.load(f)\n    return None\n\ndef save_conf(conf):\n    with open(CONF_FILE, \"w\", encoding=\"utf-8\") as f:\n        json.dump(conf, f, ensure_ascii=False, indent=2)\n    os.chmod(CONF_FILE, 0o600)\n\ndef run(cmd, cwd=None):\n    r = subprocess.run(cmd, shell=True, capture_output=True, text=True, cwd=cwd)\n    return r.returncode, r.stdout.strip(), r.stderr.strip()\n\nconf = load_conf()\n# 旧conf没有username字段时，强制重新配置\nif not conf or \"username\" not in conf:\n    if conf:\n        print(\"⚠️  检测到旧配置缺少用户名，需要重新配置\\n\")\n    else:\n        print(\"⚙️  首次配置，token只存本地，永不上传\\n\")\n    username = input(\"Gitee 用户名（登录名）：\").strip()\n    token = input(\"Gitee 私人令牌：\").strip()\n    conf = {\"username\": username, \"token\": token, \"repo\": REPO_NAME, \"host\": \"gitee.com\"}\n    save_conf(conf)\n    # 清除旧仓库，重新克隆\n    if os.path.exists(REPO_DIR):\n        shutil.rmtree(REPO_DIR)\n    print(\"✅ 配置已保存到本地\\n\")\n\nTOKEN = conf[\"token\"]\nUSERNAME = conf[\"username\"]\nHOST = conf.get(\"host\", \"gitee.com\")\nREPO_URL = f\"https://{USERNAME}:{TOKEN}@{HOST}/{conf['repo']}.git\"\n\ndef ensure_repo():\n    if not os.path.exists(REPO_DIR):\n        print(\"   首次使用，正在克隆仓库...\")\n        code, out, err = run(f\"git clone {REPO_URL} {REPO_DIR}\")\n        if code != 0:\n            print(f\"❌ 克隆失败：{err}\")\n            return False\n        print(\"   ✅ 克隆成功\")\n    return True\n\ndef push():\n    print(\"\\n📤 推送本地 → Gitee...\")\n    if not ensure_repo():\n        return\n    try:\n        shutil.copy2(json_src, os.path.join(REPO_DIR, \"zhongqu_data.json\"))\n        ts = datetime.now().strftime(\"%Y-%m-%d %H:%M\")\n        run(\"git add zhongqu_data.json\", cwd=REPO_DIR)\n        code, out, err = run(f'git commit -m \"backup {ts}\"', cwd=REPO_DIR)\n        if \"nothing to commit\" in out or \"nothing to commit\" in err:\n            print(\"✅ 云端已是最新，无需推送\")\n            return\n        code, out, err = run(f\"git push {REPO_URL} master\", cwd=REPO_DIR)\n        if code == 0:\n            print(f\"✅ 推送成功（{ts}）\")\n        else:\n            print(f\"❌ 推送失败：{err}\")\n    except Exception as e:\n        print(f\"❌ 错误：{e}\")\n\ndef pull():\n    print(\"\\n📥 拉取Gitee → 本地...\")\n    confirm = input(\"⚠️  会覆盖本地文件，确认？(y/n)：\").strip()\n    if confirm != \"y\":\n        print(\"已取消\")\n        return\n    if not ensure_repo():\n        return\n    try:\n        code, out, err = run(\"git pull\", cwd=REPO_DIR)\n        if code != 0:\n            print(f\"❌ 拉取失败：{err}\")\n            return\n        remote_file = os.path.join(REPO_DIR, \"zhongqu_data.json\")\n        if not os.path.exists(remote_file):\n            print(\"❌ 云端还没有备份，请先推送一次\")\n            return\n        os.makedirs(SNAP_DIR, exist_ok=True)\n        ts = datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n        shutil.copy2(json_src, os.path.join(SNAP_DIR, f\"snapshot_{ts}.json\"))\n        shutil.copy2(remote_file, json_src)\n        print(\"✅ 拉取成功，本地已更新\")\n    except Exception as e:\n        print(f\"❌ 错误：{e}\")\n\ndef check():\n    print(\"\\n🔍 检查云端状态...\")\n    if not ensure_repo():\n        return\n    try:\n        run(\"git fetch\", cwd=REPO_DIR)\n        code, out, err = run(\"git log --oneline -5\", cwd=REPO_DIR)\n        if code == 0 and out:\n            print(\"✅ 云端最近5次提交：\")\n            for line in out.split(\"\\n\"):\n                print(f\"   {line}\")\n        else:\n            print(\"📭 云端暂无提交记录\")\n    except Exception as e:\n        print(f\"❌ 错误：{e}\")\n\nprint(\"=\" * 35)\nprint(\"  中枢云同步 · Gitee\")\nprint(\"=\" * 35)\nprint(f\"  仓库：{conf['repo']}\")\nprint(f\"  用户：{conf['username']}\")\nprint(\"-\" * 35)\nprint(\"  1. 推送  本地→Gitee（备份）\")\nprint(\"  2. 拉取  Gitee→本地（恢复）\")\nprint(\"  3. 检查  云端提交记录\")\nprint(\"  4. 重新配置token\")\nprint(\"-\" * 35)\n\nchoice = input(\"选择：\").strip()\nif choice == \"1\":\n    push()\nelif choice == \"2\":\n    pull()\nelif choice == \"3\":\n    check()\nelif choice == \"4\":\n    os.remove(CONF_FILE)\n    if os.path.exists(REPO_DIR):\n        shutil.rmtree(REPO_DIR)\n    print(\"✅ 配置已清除，下次运行重新填写\")\nelse:\n    print(\"无效选择\")\n\ninput(\"\\n回车返回...\")",
          "memo": {
            "功能名称": "中枢云同步",
            "原始需求": "用Gitee私有仓库备份zhongqu_data.json，支持推送/拉取，首次运行配置token存本地，换手机能恢复",
            "设计思路": "token@gitee.com才能认证",
            "注意事项": "//用户名:token@gitee.com/...，只有token会报auth失败",
            "后续优化": "可加自动推送"
          }
        }
      ]
    },
    "对话记录": {
      "name": "对话记录",
      "code": "# @AI_MEMO_START\n# 功能名称：对话记录\n# 原始需求：管理AI对话条目，支持粘贴导入、查看、删除，条目存json顶层context_log字段\n# 设计思路：写入用内核注入的_atomic_save，彻底解决exec环境写入失败问题\n# 注意事项：_atomic_save由zhongqu.py内核注入，接受(data, filepath)两个参数\n# 后续优化：时间段导出、时间段删除\n# @AI_MEMO_END\n\nimport json, os, shutil\nfrom datetime import datetime\n\ntry:\n    base = os.path.dirname(_data_file)\n    DATA_FILE = _data_file\nexcept NameError:\n    base = os.path.expanduser(\"~\")\n    DATA_FILE = os.path.join(base, \"zhongqu_data.json\")\n\nSNAP_DIR = os.path.join(base, \"zhongqu_snapshots\")\n\nwith open(DATA_FILE, encoding=\"utf-8\") as _f:\n    data = json.load(_f)\n\nentries = data.setdefault(\"context_log\", [])\n\nprint(\"\\n=== 对话记录 ===\")\nprint(f\"  当前共 {len(entries)} 条\")\nprint(\"  1. 粘贴新条目\")\nprint(\"  2. 查看所有条目\")\nprint(\"  3. 删除条目\")\nprint(\"  0. 返回\")\nchoice = input(\"选择：\").strip()\n\nif choice == \"1\":\n    print(\"\\n粘贴内容，完成后单独一行输入 END 回车：\\n\")\n    _lines = []\n    while True:\n        _line = input()\n        if _line.strip() == \"END\":\n            break\n        _lines.append(_line)\n    content = \"\\n\".join(_lines).strip()\n\n    if not content:\n        print(\"⚠️  没有内容，已取消。\")\n    else:\n        print(\"\\n内容预览（前200字）：\")\n        print(content[:200])\n        if len(content) > 200:\n            print(\"...\")\n        confirm = input(\"\\n确认添加？(y/n)：\").strip()\n        if confirm == \"y\":\n            os.makedirs(SNAP_DIR, exist_ok=True)\n            _ts = datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n            shutil.copy2(DATA_FILE, os.path.join(SNAP_DIR, f\"snapshot_{_ts}.json\"))\n            ts_now = datetime.now().strftime(\"%Y-%m-%d %H:%M\")\n            entries.append({\"time\": ts_now, \"content\": content})\n            data[\"log\"].append(f\"{datetime.now().strftime('%m-%d %H:%M')} 对话记录新增条目\")\n            _atomic_save(data, DATA_FILE)\n            print(f\"✅ 已添加（{ts_now}）\")\n        else:\n            print(\"已取消\")\n\nelif choice == \"2\":\n    if not entries:\n        print(\"\\n暂无记录。\")\n    else:\n        print(f\"\\n共 {len(entries)} 条记录：\\n\")\n        for _i, _e in enumerate(entries, 1):\n            print(f\"  [{_i}] {_e['time']}\")\n            print(f\"  {_e['content'][:100]}{'...' if len(_e['content'])>100 else ''}\")\n            print()\n\nelif choice == \"3\":\n    if not entries:\n        print(\"\\n暂无记录。\")\n    else:\n        print(f\"\\n共 {len(entries)} 条，选择要删除的编号：\\n\")\n        for _i, _e in enumerate(entries, 1):\n            print(f\"  {_i}. [{_e['time']}] {_e['content'][:50]}{'...' if len(_e['content'])>50 else ''}\")\n        idx = input(\"\\n编号（输入 all 清空全部）：\").strip()\n        if idx == \"all\":\n            confirm = input(\"⚠️  确认清空全部？(y/n)：\").strip()\n            if confirm == \"y\":\n                os.makedirs(SNAP_DIR, exist_ok=True)\n                _ts = datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n                shutil.copy2(DATA_FILE, os.path.join(SNAP_DIR, f\"snapshot_{_ts}.json\"))\n                data[\"context_log\"] = []\n                data[\"log\"].append(f\"{datetime.now().strftime('%m-%d %H:%M')} 对话记录清空全部\")\n                _atomic_save(data, DATA_FILE)\n                print(\"✅ 已清空\")\n        else:\n            try:\n                n = int(idx) - 1\n                _e = entries[n]\n                confirm = input(f\"确认删除 [{_e['time']}] 这条？(y/n)：\").strip()\n                if confirm == \"y\":\n                    os.makedirs(SNAP_DIR, exist_ok=True)\n                    _ts = datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n                    shutil.copy2(DATA_FILE, os.path.join(SNAP_DIR, f\"snapshot_{_ts}.json\"))\n                    entries.pop(n)\n                    data[\"log\"].append(f\"{datetime.now().strftime('%m-%d %H:%M')} 对话记录删除1条\")\n                    _atomic_save(data, DATA_FILE)\n                    print(\"✅ 已删除\")\n            except (ValueError, IndexError):\n                print(\"⚠️  无效编号\")\n\ninput(\"\\n回车返回...\")",
      "memo": {
        "功能名称": "对话记录",
        "原始需求": "管理AI对话条目，支持粘贴导入、查看、删除，条目存json顶层context_log字段",
        "设计思路": "写入用内核注入的_atomic_save，彻底解决exec环境写入失败问题",
        "注意事项": "_atomic_save由zhongqu.py内核注入，接受(data, filepath)两个参数",
        "后续优化": "时间段导出、时间段删除"
      },
      "desc": "管理AI对话条目，支持粘贴导入、查看、删除，条目存json顶层context_log字段",
      "added": "2026-03-13 19:02",
      "modified": "2026-03-13 19:52",
      "history": [
        {
          "time": "2026-03-13 19:14",
          "code": "# @AI_MEMO_START\n# 功能名称：对话记录\n# 原始需求：管理AI对话条目，支持粘贴导入、查看、删除，条目存json顶层context_log字段\n# 设计思路：entries存data[\"context_log\"]，与action的key完全无关，增删功能不影响索引\n# 注意事项：用try/except NameError兜底兼容两种exec环境；context_log不存在时自动初始化\n# 后续优化：时间段导出、时间段删除\n# @AI_MEMO_END\n\nimport json, os, shutil\nfrom datetime import datetime\n\ntry:\n    base = os.path.dirname(_data_file)\n    DATA_FILE = _data_file\nexcept NameError:\n    base = os.path.expanduser(\"~\")\n    DATA_FILE = os.path.join(base, \"zhongqu_data.json\")\n\nSNAP_DIR = os.path.join(base, \"zhongqu_snapshots\")\n\ndef load():\n    with open(DATA_FILE, encoding=\"utf-8\") as f:\n        return json.load(f)\n\ndef save(d):\n    tmp = DATA_FILE + \".tmp\"\n    with open(tmp, \"w\", encoding=\"utf-8\") as f:\n        json.dump(d, f, ensure_ascii=False, indent=2)\n    os.replace(tmp, DATA_FILE)\n\ndef snap():\n    os.makedirs(SNAP_DIR, exist_ok=True)\n    ts = datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n    shutil.copy2(DATA_FILE, os.path.join(SNAP_DIR, f\"snapshot_{ts}.json\"))\n\ndata = load()\n\n# 初始化顶层字段\nif \"context_log\" not in data:\n    data[\"context_log\"] = []\n    save(data)\n    data = load()\n\nprint(\"\\n=== 对话记录 ===\")\nprint(f\"  当前共 {len(data['context_log'])} 条\")\nprint(\"  1. 粘贴新条目\")\nprint(\"  2. 查看所有条目\")\nprint(\"  3. 删除条目\")\nprint(\"  0. 返回\")\nchoice = input(\"选择：\").strip()\n\nif choice == \"1\":\n    print(\"\\n粘贴内容，完成后单独一行输入 END 回车：\\n\")\n    lines = []\n    while True:\n        line = input()\n        if line.strip() == \"END\":\n            break\n        lines.append(line)\n    content = \"\\n\".join(lines).strip()\n\n    if not content:\n        print(\"⚠️  没有内容，已取消。\")\n    else:\n        print(\"\\n内容预览（前200字）：\")\n        print(content[:200])\n        if len(content) > 200:\n            print(\"...\")\n        confirm = input(\"\\n确认添加？(y/n)：\").strip()\n        if confirm == \"y\":\n            snap()\n            data = load()\n            ts_now = datetime.now().strftime(\"%Y-%m-%d %H:%M\")\n            data[\"context_log\"].append({\"time\": ts_now, \"content\": content})\n            data[\"log\"].append(f\"{datetime.now().strftime('%m-%d %H:%M')} 对话记录新增条目\")\n            save(data)\n            print(f\"✅ 已添加（{ts_now}）\")\n        else:\n            print(\"已取消\")\n\nelif choice == \"2\":\n    entries = data[\"context_log\"]\n    if not entries:\n        print(\"\\n暂无记录。\")\n    else:\n        print(f\"\\n共 {len(entries)} 条记录：\\n\")\n        for i, e in enumerate(entries, 1):\n            print(f\"  [{i}] {e['time']}\")\n            print(f\"  {e['content'][:100]}{'...' if len(e['content'])>100 else ''}\")\n            print()\n\nelif choice == \"3\":\n    entries = data[\"context_log\"]\n    if not entries:\n        print(\"\\n暂无记录。\")\n    else:\n        print(f\"\\n共 {len(entries)} 条，选择要删除的编号：\\n\")\n        for i, e in enumerate(entries, 1):\n            print(f\"  {i}. [{e['time']}] {e['content'][:50]}{'...' if len(e['content'])>50 else ''}\")\n        idx = input(\"\\n编号（输入 all 清空全部）：\").strip()\n        if idx == \"all\":\n            confirm = input(\"⚠️  确认清空全部？(y/n)：\").strip()\n            if confirm == \"y\":\n                snap()\n                data = load()\n                data[\"context_log\"] = []\n                data[\"log\"].append(f\"{datetime.now().strftime('%m-%d %H:%M')} 对话记录清空全部\")\n                save(data)\n                print(\"✅ 已清空\")\n        else:\n            try:\n                n = int(idx) - 1\n                e = entries[n]\n                confirm = input(f\"确认删除 [{e['time']}] 这条？(y/n)：\").strip()\n                if confirm == \"y\":\n                    snap()\n                    data = load()\n                    data[\"context_log\"].pop(n)\n                    data[\"log\"].append(f\"{datetime.now().strftime('%m-%d %H:%M')} 对话记录删除1条\")\n                    save(data)\n                    print(\"✅ 已删除\")\n            except (ValueError, IndexError):\n                print(\"⚠️  无效编号\")\n\ninput(\"\\n回车返回...\")",
          "memo": {
            "功能名称": "对话记录",
            "原始需求": "管理AI对话条目，支持粘贴导入、查看、删除，条目存json顶层context_log字段",
            "设计思路": "entries存data[\"context_log\"]，与action的key完全无关，增删功能不影响索引",
            "注意事项": "用try/except NameError兜底兼容两种exec环境；context_log不存在时自动初始化",
            "后续优化": "时间段导出、时间段删除"
          }
        },
        {
          "time": "2026-03-13 19:22",
          "code": "# @AI_MEMO_START\n# 功能名称：对话记录\n# 原始需求：管理AI对话条目，支持粘贴导入、查看、删除，条目存json顶层context_log字段\n# 设计思路：去掉启动初始化逻辑，每次操作前直接setdefault确保context_log存在，避免覆盖\n# 注意事项：用try/except NameError兜底兼容两种exec环境；不在代码开头做save，只在实际操作时save\n# 后续优化：时间段导出、时间段删除\n# @AI_MEMO_END\n\nimport json, os, shutil\nfrom datetime import datetime\n\ntry:\n    base = os.path.dirname(_data_file)\n    DATA_FILE = _data_file\nexcept NameError:\n    base = os.path.expanduser(\"~\")\n    DATA_FILE = os.path.join(base, \"zhongqu_data.json\")\n\nSNAP_DIR = os.path.join(base, \"zhongqu_snapshots\")\n\ndef load():\n    with open(DATA_FILE, encoding=\"utf-8\") as f:\n        return json.load(f)\n\ndef save(d):\n    tmp = DATA_FILE + \".tmp\"\n    with open(tmp, \"w\", encoding=\"utf-8\") as f:\n        json.dump(d, f, ensure_ascii=False, indent=2)\n    os.replace(tmp, DATA_FILE)\n\ndef snap():\n    os.makedirs(SNAP_DIR, exist_ok=True)\n    ts = datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n    shutil.copy2(DATA_FILE, os.path.join(SNAP_DIR, f\"snapshot_{ts}.json\"))\n\ndata = load()\nentries = data.setdefault(\"context_log\", [])\n\nprint(\"\\n=== 对话记录 ===\")\nprint(f\"  当前共 {len(entries)} 条\")\nprint(\"  1. 粘贴新条目\")\nprint(\"  2. 查看所有条目\")\nprint(\"  3. 删除条目\")\nprint(\"  0. 返回\")\nchoice = input(\"选择：\").strip()\n\nif choice == \"1\":\n    print(\"\\n粘贴内容，完成后单独一行输入 END 回车：\\n\")\n    lines = []\n    while True:\n        line = input()\n        if line.strip() == \"END\":\n            break\n        lines.append(line)\n    content = \"\\n\".join(lines).strip()\n\n    if not content:\n        print(\"⚠️  没有内容，已取消。\")\n    else:\n        print(\"\\n内容预览（前200字）：\")\n        print(content[:200])\n        if len(content) > 200:\n            print(\"...\")\n        confirm = input(\"\\n确认添加？(y/n)：\").strip()\n        if confirm == \"y\":\n            snap()\n            ts_now = datetime.now().strftime(\"%Y-%m-%d %H:%M\")\n            entries.append({\"time\": ts_now, \"content\": content})\n            data[\"log\"].append(f\"{datetime.now().strftime('%m-%d %H:%M')} 对话记录新增条目\")\n            save(data)\n            print(f\"✅ 已添加（{ts_now}）\")\n        else:\n            print(\"已取消\")\n\nelif choice == \"2\":\n    if not entries:\n        print(\"\\n暂无记录。\")\n    else:\n        print(f\"\\n共 {len(entries)} 条记录：\\n\")\n        for i, e in enumerate(entries, 1):\n            print(f\"  [{i}] {e['time']}\")\n            print(f\"  {e['content'][:100]}{'...' if len(e['content'])>100 else ''}\")\n            print()\n\nelif choice == \"3\":\n    if not entries:\n        print(\"\\n暂无记录。\")\n    else:\n        print(f\"\\n共 {len(entries)} 条，选择要删除的编号：\\n\")\n        for i, e in enumerate(entries, 1):\n            print(f\"  {i}. [{e['time']}] {e['content'][:50]}{'...' if len(e['content'])>50 else ''}\")\n        idx = input(\"\\n编号（输入 all 清空全部）：\").strip()\n        if idx == \"all\":\n            confirm = input(\"⚠️  确认清空全部？(y/n)：\").strip()\n            if confirm == \"y\":\n                snap()\n                data[\"context_log\"] = []\n                data[\"log\"].append(f\"{datetime.now().strftime('%m-%d %H:%M')} 对话记录清空全部\")\n                save(data)\n                print(\"✅ 已清空\")\n        else:\n            try:\n                n = int(idx) - 1\n                e = entries[n]\n                confirm = input(f\"确认删除 [{e['time']}] 这条？(y/n)：\").strip()\n                if confirm == \"y\":\n                    snap()\n                    entries.pop(n)\n                    data[\"log\"].append(f\"{datetime.now().strftime('%m-%d %H:%M')} 对话记录删除1条\")\n                    save(data)\n                    print(\"✅ 已删除\")\n            except (ValueError, IndexError):\n                print(\"⚠️  无效编号\")\n\ninput(\"\\n回车返回...\")",
          "memo": {
            "功能名称": "对话记录",
            "原始需求": "管理AI对话条目，支持粘贴导入、查看、删除，条目存json顶层context_log字段",
            "设计思路": "去掉启动初始化逻辑，每次操作前直接setdefault确保context_log存在，避免覆盖",
            "注意事项": "用try/except NameError兜底兼容两种exec环境；不在代码开头做save，只在实际操作时save",
            "后续优化": "时间段导出、时间段删除"
          }
        },
        {
          "time": "2026-03-13 19:52",
          "code": "# @AI_MEMO_START\n# 功能名称：对话记录\n# 原始需求：管理AI对话条目，支持粘贴导入、查看、删除，条目存json顶层context_log字段\n# 设计思路：不用函数定义，所有逻辑内联，避免exec环境下函数访问外部变量的命名空间问题\n# 注意事项：exec里def的函数无法访问exec作用域的变量，必须内联或把变量作为参数传入\n# 后续优化：时间段导出、时间段删除\n# @AI_MEMO_END\n\nimport json, os, shutil\nfrom datetime import datetime\n\ntry:\n    base = os.path.dirname(_data_file)\n    DATA_FILE = _data_file\nexcept NameError:\n    base = os.path.expanduser(\"~\")\n    DATA_FILE = os.path.join(base, \"zhongqu_data.json\")\n\nSNAP_DIR = os.path.join(base, \"zhongqu_snapshots\")\n\nwith open(DATA_FILE, encoding=\"utf-8\") as _f:\n    data = json.load(_f)\n\nentries = data.setdefault(\"context_log\", [])\n\nprint(\"\\n=== 对话记录 ===\")\nprint(f\"  当前共 {len(entries)} 条\")\nprint(\"  1. 粘贴新条目\")\nprint(\"  2. 查看所有条目\")\nprint(\"  3. 删除条目\")\nprint(\"  0. 返回\")\nchoice = input(\"选择：\").strip()\n\nif choice == \"1\":\n    print(\"\\n粘贴内容，完成后单独一行输入 END 回车：\\n\")\n    _lines = []\n    while True:\n        _line = input()\n        if _line.strip() == \"END\":\n            break\n        _lines.append(_line)\n    content = \"\\n\".join(_lines).strip()\n\n    if not content:\n        print(\"⚠️  没有内容，已取消。\")\n    else:\n        print(\"\\n内容预览（前200字）：\")\n        print(content[:200])\n        if len(content) > 200:\n            print(\"...\")\n        confirm = input(\"\\n确认添加？(y/n)：\").strip()\n        if confirm == \"y\":\n            # snap内联\n            os.makedirs(SNAP_DIR, exist_ok=True)\n            _ts = datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n            shutil.copy2(DATA_FILE, os.path.join(SNAP_DIR, f\"snapshot_{_ts}.json\"))\n            # 写入\n            ts_now = datetime.now().strftime(\"%Y-%m-%d %H:%M\")\n            entries.append({\"time\": ts_now, \"content\": content})\n            data[\"log\"].append(f\"{datetime.now().strftime('%m-%d %H:%M')} 对话记录新增条目\")\n            _tmp = DATA_FILE + \".tmp\"\n            with open(_tmp, \"w\", encoding=\"utf-8\") as _f:\n                json.dump(data, _f, ensure_ascii=False, indent=2)\n            os.replace(_tmp, DATA_FILE)\n            print(f\"✅ 已添加（{ts_now}）\")\n        else:\n            print(\"已取消\")\n\nelif choice == \"2\":\n    if not entries:\n        print(\"\\n暂无记录。\")\n    else:\n        print(f\"\\n共 {len(entries)} 条记录：\\n\")\n        for _i, _e in enumerate(entries, 1):\n            print(f\"  [{_i}] {_e['time']}\")\n            print(f\"  {_e['content'][:100]}{'...' if len(_e['content'])>100 else ''}\")\n            print()\n\nelif choice == \"3\":\n    if not entries:\n        print(\"\\n暂无记录。\")\n    else:\n        print(f\"\\n共 {len(entries)} 条，选择要删除的编号：\\n\")\n        for _i, _e in enumerate(entries, 1):\n            print(f\"  {_i}. [{_e['time']}] {_e['content'][:50]}{'...' if len(_e['content'])>50 else ''}\")\n        idx = input(\"\\n编号（输入 all 清空全部）：\").strip()\n        if idx == \"all\":\n            confirm = input(\"⚠️  确认清空全部？(y/n)：\").strip()\n            if confirm == \"y\":\n                os.makedirs(SNAP_DIR, exist_ok=True)\n                _ts = datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n                shutil.copy2(DATA_FILE, os.path.join(SNAP_DIR, f\"snapshot_{_ts}.json\"))\n                data[\"context_log\"] = []\n                data[\"log\"].append(f\"{datetime.now().strftime('%m-%d %H:%M')} 对话记录清空全部\")\n                _tmp = DATA_FILE + \".tmp\"\n                with open(_tmp, \"w\", encoding=\"utf-8\") as _f:\n                    json.dump(data, _f, ensure_ascii=False, indent=2)\n                os.replace(_tmp, DATA_FILE)\n                print(\"✅ 已清空\")\n        else:\n            try:\n                n = int(idx) - 1\n                _e = entries[n]\n                confirm = input(f\"确认删除 [{_e['time']}] 这条？(y/n)：\").strip()\n                if confirm == \"y\":\n                    os.makedirs(SNAP_DIR, exist_ok=True)\n                    _ts = datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n                    shutil.copy2(DATA_FILE, os.path.join(SNAP_DIR, f\"snapshot_{_ts}.json\"))\n                    entries.pop(n)\n                    data[\"log\"].append(f\"{datetime.now().strftime('%m-%d %H:%M')} 对话记录删除1条\")\n                    _tmp = DATA_FILE + \".tmp\"\n                    with open(_tmp, \"w\", encoding=\"utf-8\") as _f:\n                        json.dump(data, _f, ensure_ascii=False, indent=2)\n                    os.replace(_tmp, DATA_FILE)\n                    print(\"✅ 已删除\")\n            except (ValueError, IndexError):\n                print(\"⚠️  无效编号\")\n\ninput(\"\\n回车返回...\")",
          "memo": {
            "功能名称": "对话记录",
            "原始需求": "管理AI对话条目，支持粘贴导入、查看、删除，条目存json顶层context_log字段",
            "设计思路": "不用函数定义，所有逻辑内联，避免exec环境下函数访问外部变量的命名空间问题",
            "注意事项": "exec里def的函数无法访问exec作用域的变量，必须内联或把变量作为参数传入",
            "后续优化": "时间段导出、时间段删除"
          }
        }
      ]
    },
    "导出公开版": {
      "name": "导出公开版",
      "code": "# @AI_MEMO_START\n# 功能名称：导出公开版\n# 原始需求：一键生成适合公开的JSON版本，自动清空对话记录和操作日志，推送到公开库\n# 设计思路：复制当前数据，清空context_log和log，保存到~/zhongqu_public/并自动git推送\n# 注意事项：只清空指定字段，保留所有功能代码和备忘录；操作的是副本，不影响本地数据\n# 后续优化：可配置清空哪些字段\n# @AI_MEMO_END\n\nimport json, os, subprocess\nfrom datetime import datetime\n\ntry:\n    DATA_FILE = _data_file\nexcept NameError:\n    DATA_FILE = os.path.join(os.path.expanduser(\"~\"), \"zhongqu_data.json\")\n\nPUBLIC_DIR = os.path.expanduser(\"~/zhongqu_public\")\nPUBLIC_JSON = os.path.join(PUBLIC_DIR, \"zhongqu_data.json\")\n\nif not os.path.exists(PUBLIC_DIR):\n    print(f\"❌ 找不到公开库目录：{PUBLIC_DIR}\")\n    input(\"回车返回...\")\nelse:\n    with open(DATA_FILE, encoding=\"utf-8\") as f:\n        data = json.load(f)\n\n    # 创建公开版副本，清空敏感字段\n    import copy\n    public_data = copy.deepcopy(data)\n    public_data[\"context_log\"] = []\n    public_data[\"log\"] = []\n    public_data[\"memory_tree\"] = {}\n\n    # 写入公开库\n    tmp = PUBLIC_JSON + \".tmp.\" + str(os.getpid())\n    with open(tmp, \"w\", encoding=\"utf-8\") as f:\n        json.dump(public_data, f, ensure_ascii=False, indent=2)\n        f.flush()\n        os.fsync(f.fileno())\n    if os.path.exists(PUBLIC_JSON):\n        os.remove(PUBLIC_JSON)\n    os.rename(tmp, PUBLIC_JSON)\n    print(\"✅ 公开版JSON已生成，context_log和log已清空\")\n\n    # 自动git提交推送\n    confirm = input(\"\\n是否自动推送到公开库？(y/n)：\").strip()\n    if confirm == \"y\":\n        try:\n            ts = datetime.now().strftime(\"%Y-%m-%d %H:%M\")\n            subprocess.run([\"git\", \"-C\", PUBLIC_DIR, \"add\", \"zhongqu_data.json\"], check=True)\n            result = subprocess.run([\"git\", \"-C\", PUBLIC_DIR, \"commit\", \"-m\", f\"update {ts}\"], capture_output=True, text=True)\n            if \"nothing to commit\" in result.stdout or \"nothing to commit\" in result.stderr:\n                print(\"✅ 无变更，无需推送\")\n            else:\n                subprocess.run([\"git\", \"-C\", PUBLIC_DIR, \"push\"], check=True)\n                print(f\"✅ 已推送到公开库（{ts}）\")\n        except subprocess.CalledProcessError as e:\n            print(f\"⚠️ 推送失败：{e}\")\n            print(f\"   文件已保存，可手动cd ~/zhongqu_public && git push\")\n    else:\n        print(f\"   文件已保存到：{PUBLIC_JSON}\")\n        print(\"   手动推送：cd ~/zhongqu_public && git add . && git commit -m 'update' && git push\")\n\n    input(\"\\n回车返回...\")",
      "memo": {
        "功能名称": "导出公开版",
        "原始需求": "一键生成适合公开的JSON版本，自动清空对话记录和操作日志，推送到公开库",
        "设计思路": "复制当前数据，清空context_log和log，保存到~/zhongqu_public/并自动git推送",
        "注意事项": "只清空指定字段，保留所有功能代码和备忘录；操作的是副本，不影响本地数据",
        "后续优化": "可配置清空哪些字段"
      },
      "desc": "一键生成适合公开的JSON版本，自动清空对话记录和操作日志，推送到公开库",
      "added": "2026-03-14 16:06",
      "history": []
    },
    "局部编辑功能": {
      "name": "局部编辑功能",
      "code": "# @AI_MEMO_START\n# 功能名称：局部编辑功能\n# 原始需求：按行号替换代码，支持手机端自动缩进对齐\n# 设计思路：\n#  1. 计算首个非空行的相对缩进偏移(offset)\n#  2. 保持内部层级关系整体平移\n#  3. 调用内核 _log(data, msg) 协议\n# @AI_MEMO_END\n\nimport json, os, shutil, re\nfrom datetime import datetime\n\ntry:\n    DATA_FILE = _data_file\n    BASE_DIR = _base_dir\nexcept NameError:\n    BASE_DIR = os.path.expanduser(\"~\")\n    DATA_FILE = os.path.join(BASE_DIR, \"zhongqu_data.json\")\n\nSNAP_DIR = os.path.join(BASE_DIR, \"zhongqu_snapshots\")\n\nwith open(DATA_FILE, encoding=\"utf-8\") as f:\n    data = json.load(f)\n\nactions = data.get(\"actions\", {})\nall_items = list(actions.items())\nsystem_keys = [\"1\",\"2\",\"3\",\"4\",\"5\",\"6\",\"7\",\"8\",\"9\"]\nsys_fns = [(k,v) for k,v in all_items if k in system_keys]\n# 修复：工具功能保持 t1, t2 映射\ntool_fns = [(k,v) for k,v in all_items if k not in system_keys]\n\nprint(\"\\n选择要局部编辑的功能：\")\nprint(\"  ── 系统功能 ──\")\nfor k, v in sys_fns: print(f\"  {k}. {v['name']}\")\nprint(\"  ── 工具功能 ──\")\nfor i, (k, v) in enumerate(tool_fns, 1):\n    print(f\"  t{i}. {v['name']}\")\n\ncmd_key = input(\"\\n编号：\").strip()\ntarget_key = None\n\n# 解析编号\nif cmd_key.startswith('t') and cmd_key[1:].isdigit():\n    idx = int(cmd_key[1:]) - 1\n    if 0 <= idx < len(tool_fns): target_key = tool_fns[idx][0]\nelif cmd_key in actions:\n    target_key = cmd_key\n\nif not target_key:\n    print(\"❌ 无效编号\"); input(\"回车继续...\"); exit()\n\naction = actions[target_key]\nname = action[\"name\"]\nlines = action[\"code\"].splitlines()\ntotal = len(lines)\n\ndef show_page(start_idx):\n    os.system(\"clear\")\n    print(f\"「{name}」共 {total} 行\")\n    print(\"主要结构：\")\n    for i, line in enumerate(lines):\n        if line.strip().startswith((\"def \", \"class \")):\n            print(f\"  · {line.strip().split('(')[0]} 第{i+1}行\")\n    \n    end_idx = min(start_idx + 30, total)\n    print(f\"\\n===== 第{start_idx+1}-{end_idx}行（共{total}行）=====\")\n    for i in range(start_idx, end_idx):\n        print(f\"{i+1:03d}: {lines[i]}\")\n    print(\"=\" * 35)\n\ncurrent = 0\nwhile True:\n    show_page(current)\n    cmd = input(\"n下页|p上页|/搜索|q退出：\").strip()\n    if cmd == 'q': break\n    elif cmd == 'n': current = min(current + 30, total - 1)\n    elif cmd == 'p': current = max(0, current - 30)\n    elif cmd.startswith('/'):\n        kw = cmd[1:].strip()\n        res = [(i, l) for i, l in enumerate(lines, 1) if not l.strip().startswith(\"#\") and kw in l]\n        if not res: print(f\"❌ 未找到「{kw}」\"); input(\"回车继续...\")\n        else:\n            print(f\"\\n===== 找到 {len(res)} 处匹配 =====\")\n            for idx, (ln, ct) in enumerate(res[:10]):\n                print(f\"  {idx}. 第{ln}行：{ct.strip()[:50]}\")\n            jmp = input(\"输入行号跳转：\").strip()\n            if jmp.isdigit(): current = max(0, int(jmp) - 5)\n    elif cmd.isdigit(): current = max(0, int(cmd) - 5)\n\nprint(f\"\\n代码共 {total} 行，输入范围：\")\ntry:\n    start_line = int(input(\"起始行：\").strip())\n    end_line = int(input(\"结束行：\").strip())\nexcept: exit()\n\nprint(f\"\\n===== 待替换 {start_line}-{end_line} =====\")\nfor i in range(start_line-1, end_line): print(f\"{i+1:03d}: {lines[i]}\")\nprint(\"=\" * 35)\n\nprint(\"粘贴新内容 (END结束)：\")\nnew_lines = []\nwhile True:\n    l = input()\n    if l.strip() == \"END\": break\n    new_lines.append(l)\n\nif not new_lines: exit()\n\n# --- 相对缩进算法 ---\nuser_indent = 0\nfor l in new_lines:\n    if l.strip():\n        user_indent = len(l) - len(l.lstrip())\n        break\n\ntarget_indent = len(lines[start_line-1]) - len(lines[start_line-1].lstrip())\noffset = target_indent - user_indent\n\nadj_new = []\nfor l in new_lines:\n    if not l.strip(): adj_new.append(\"\")\n    else:\n        if offset >= 0:\n            adj_new.append(\" \" * offset + l)\n        else:\n            shift = abs(offset)\n            adj_new.append(l[shift:] if l.startswith(\" \" * shift) else l.lstrip())\n\nnew_code = \"\\n\".join(lines[:start_line-1] + adj_new + lines[end_line:])\n\ntry:\n    compile(new_code, \"<string>\", \"exec\")\nexcept Exception as e:\n    print(f\"❌ 语法错误：{e}\"); input(\"回车继续...\"); exit()\n\nprint(\"\\n===== 预览 (前10行) =====\")\nfor i, l in enumerate(adj_new[:10]): print(f\"{start_line+i:03d}: {l}\")\nprint(\"=\" * 35)\n\nif input(f\"确认写入「{name}」？(y/n): \").strip().lower() == \"y\":\n    _snap() \n    old_code = actions[target_key][\"code\"]\n    hist = actions[target_key].get(\"history\", [])\n    hist.append({\"time\": _now(), \"code\": old_code})\n    actions[target_key][\"code\"] = new_code\n    actions[target_key][\"modified\"] = _now()\n    actions[target_key][\"history\"] = hist[-5:]\n    # 修复：遵循 _log(data, msg) 协议\n    data = _log(data, f\"局部编辑「{name}」{start_line}-{end_line}行\")\n    _atomic_save(data, DATA_FILE)\n    print(\"✅ 已更新\")\nelse:\n    print(\"已取消\")\ninput(\"回车继续...\")",
      "memo": {
        "功能名称": "局部编辑功能",
        "原始需求": "按行号替换代码，支持手机端自动缩进对齐",
        "设计思路": ""
      },
      "desc": "按行号替换代码，支持手机端自动缩进对齐",
      "added": "2026-03-15 12:32",
      "modified": "2026-03-15 14:04:24",
      "history": [
        {
          "time": "2026-03-15 12:42",
          "code": "# @AI_MEMO_START\n# 功能名称：局部编辑功能\n# 原始需求：对任意功能代码做find&replace局部编辑，不用替换整个代码\n# 设计思路：str.replace纯文本匹配，替换前compile语法检查，超5处二次确认，自动快照\n# 注意事项：不支持正则；只改code字段不更新memo字段，有提示；备忘录变更有提示\n# 后续优化：可加多段替换（一次改多处不同内容）\n# @AI_MEMO_END\n\nimport json, os, shutil\nfrom datetime import datetime\n\ntry:\n    DATA_FILE = _data_file\n    BASE_DIR = _base_dir\nexcept NameError:\n    BASE_DIR = os.path.expanduser(\"~\")\n    DATA_FILE = os.path.join(BASE_DIR, \"zhongqu_data.json\")\n\nSNAP_DIR = os.path.join(BASE_DIR, \"zhongqu_snapshots\")\n\nwith open(DATA_FILE, encoding=\"utf-8\") as f:\n    data = json.load(f)\n\nactions = data.get(\"actions\", {})\nall_items = list(actions.items())\nsystem_keys = [\"1\",\"2\",\"3\",\"4\",\"5\",\"6\",\"7\",\"8\",\"9\"]\nsys_fns = [(k,v) for k,v in all_items if k in system_keys]\ntool_fns = [(k,v) for k,v in all_items if k not in system_keys]\n\nprint(\"\\n选择要局部编辑的功能：\")\nprint(\"  ── 系统功能 ──\")\nfor k,v in sys_fns:\n    print(f\"  {k}. {v['name']}\")\nprint(\"  ── 工具功能 ──\")\nif not tool_fns:\n    print(\"  （暂无工具功能）\")\nelse:\n    for i,(k,v) in enumerate(tool_fns, 1):\n        print(f\"  t{i}. {v['name']}\")\n\nchoice = input(\"\\n编号（系统功能输数字，工具功能输t+数字）：\").strip()\n\nkey = None\nif choice in system_keys:\n    key = choice\nelif choice.startswith(\"t\"):\n    try:\n        key = tool_fns[int(choice[1:])-1][0]\n    except (ValueError, IndexError):\n        pass\n\nif not key:\n    print(\"⚠️ 无效编号\")\n    input(\"回车继续...\")\nelse:\n    name = actions[key][\"name\"]\n    code = actions[key].get(\"code\", \"\")\n\n    print(f\"\\n已选择：「{name}」\")\n    print(\"代码预览（前10行）：\")\n    for line in code.splitlines()[:10]:\n        print(f\"  {line}\")\n\n    print(\"\\n粘贴要查找的旧文本，完成后单独一行输入 END：\")\n    old_lines = []\n    while True:\n        line = input()\n        if line.strip() == \"END\":\n            break\n        old_lines.append(line)\n    old_text = \"\\n\".join(old_lines)\n\n    if not old_text.strip():\n        print(\"⚠️ 旧文本为空，已取消\")\n        input(\"回车继续...\")\n    elif old_text not in code:\n        print(\"❌ 在代码中找不到该文本，已取消\")\n        input(\"回车继续...\")\n    else:\n        # 统计匹配次数\n        count = code.count(old_text)\n\n        # 显示匹配预览（最多3处）\n        print(f\"\\n找到 {count} 处匹配：\")\n        shown = 0\n        for i, line in enumerate(code.splitlines(), 1):\n            if old_text.split(\"\\n\")[0] in line:\n                print(f\"  第{i}行：{line.strip()[:80]}\")\n                shown += 1\n                if shown >= 3:\n                    if count > 3:\n                        print(f\"  ... 还有 {count-3} 处\")\n                    break\n\n        print(\"\\n粘贴要替换的新文本，完成后单独一行输入 END：\")\n        new_lines = []\n        while True:\n            line = input()\n            if line.strip() == \"END\":\n                break\n            new_lines.append(line)\n        new_text = \"\\n\".join(new_lines)\n\n        # 生成新代码\n        new_code = code.replace(old_text, new_text)\n\n        # compile语法检查\n        try:\n            compile(new_code, \"<string>\", \"exec\")\n            print(\"✅ 语法检查通过\")\n        except SyntaxError as e:\n            print(f\"❌ 新代码有语法错误：{e}\")\n            print(\"替换已取消，数据未修改\")\n            input(\"回车继续...\")\n            new_code = None\n\n        if new_code is not None:\n            # 超5处二次确认\n            confirm1 = input(f\"\\n确认替换这 {count} 处？(y/n)：\").strip()\n            if confirm1 == \"y\" and count > 5:\n                confirm2 = input(f\"⚠️ 匹配超过5处，再次确认替换全部 {count} 处？(y/n)：\").strip()\n                if confirm2 != \"y\":\n                    confirm1 = \"n\"\n\n            if confirm1 == \"y\":\n                # 自动快照\n                os.makedirs(SNAP_DIR, exist_ok=True)\n                ts = datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n                shutil.copy2(DATA_FILE, os.path.join(SNAP_DIR, f\"snapshot_{ts}.json\"))\n\n                # 写入\n                old_code = actions[key][\"code\"]\n                hist = actions[key].get(\"history\", [])\n                hist.append({\n                    \"time\": datetime.now().strftime(\"%Y-%m-%d %H:%M\"),\n                    \"code\": old_code,\n                    \"memo\": actions[key].get(\"memo\", {})\n                })\n                actions[key][\"code\"] = new_code\n                actions[key][\"modified\"] = datetime.now().strftime(\"%Y-%m-%d %H:%M\")\n                actions[key][\"history\"] = hist[-10:]\n                data[\"log\"].append(f\"{datetime.now().strftime('%m-%d %H:%M')} 局部编辑「{name}」\")\n\n                _atomic_save(data, DATA_FILE)\n                print(f\"\\n✅ 「{name}」已更新，替换了 {count} 处\")\n\n                # 备忘录变更提示\n                if \"@AI_MEMO_START\" in old_text or \"@AI_MEMO_START\" in new_text:\n                    print(\"⚠️ 你修改了备忘录部分，memo字段可能已过时\")\n                    print(\"   建议运行「修改功能」重新粘贴完整代码更新memo\")\n                else:\n                    print(\"⚠️ code已更新，memo字段未同步\")\n                    print(\"   如需更新memo，请运行「修改功能」重新粘贴完整代码\")\n            else:\n                print(\"已取消\")\n\ninput(\"\\n回车返回...\")",
          "memo": {
            "功能名称": "局部编辑功能",
            "原始需求": "对任意功能代码做find&replace局部编辑，不用替换整个代码",
            "设计思路": "str.replace纯文本匹配，替换前compile语法检查，超5处二次确认，自动快照",
            "注意事项": "不支持正则；只改code字段不更新memo字段，有提示；备忘录变更有提示",
            "后续优化": "可加多段替换（一次改多处不同内容）"
          }
        },
        {
          "time": "2026-03-15 13:00",
          "code": "# @AI_MEMO_START\n# 功能名称：局部编辑功能\n# 原始需求：按行号替换功能代码的指定行，不需要精确匹配旧文本\n# 设计思路：分页显示带行号代码，用户指定行号范围，粘贴新内容替换，compile检查后写入\n# 注意事项：缩进由用户自己控制；行号从1开始；显示截断60字符但替换用完整内容\n# 后续优化：可加多段替换\n# @AI_MEMO_END\n\nimport json, os, shutil\nfrom datetime import datetime\n\ntry:\n    DATA_FILE = _data_file\n    BASE_DIR = _base_dir\nexcept NameError:\n    BASE_DIR = os.path.expanduser(\"~\")\n    DATA_FILE = os.path.join(BASE_DIR, \"zhongqu_data.json\")\n\nSNAP_DIR = os.path.join(BASE_DIR, \"zhongqu_snapshots\")\n\nwith open(DATA_FILE, encoding=\"utf-8\") as f:\n    data = json.load(f)\n\nactions = data.get(\"actions\", {})\nall_items = list(actions.items())\nsystem_keys = [\"1\",\"2\",\"3\",\"4\",\"5\",\"6\",\"7\",\"8\",\"9\"]\nsys_fns = [(k,v) for k,v in all_items if k in system_keys]\ntool_fns = [(k,v) for k,v in all_items if k not in system_keys]\n\nprint(\"\\n选择要局部编辑的功能：\")\nprint(\"  ── 系统功能 ──\")\nfor k,v in sys_fns:\n    print(f\"  {k}. {v['name']}\")\nprint(\"  ── 工具功能 ──\")\nif not tool_fns:\n    print(\"  （暂无工具功能）\")\nelse:\n    for i,(k,v) in enumerate(tool_fns, 1):\n        print(f\"  t{i}. {v['name']}\")\n\nchoice = input(\"\\n编号（系统功能输数字，工具功能输t+数字）：\").strip()\n\nkey = None\nif choice in system_keys:\n    key = choice\nelif choice.startswith(\"t\"):\n    try:\n        key = tool_fns[int(choice[1:])-1][0]\n    except (ValueError, IndexError):\n        pass\n\nif not key:\n    print(\"⚠️ 无效编号\")\n    input(\"回车继续...\")\nelse:\n    name = actions[key][\"name\"]\n    code = actions[key].get(\"code\", \"\")\n    lines = code.splitlines()\n    total = len(lines)\n\n    def display_page(start, page_size=30):\n        end = min(start + page_size, total)\n        print(f\"\\n===== 第 {start+1}-{end} 行（共{total}行）=====\")\n        for i in range(start, end):\n            line = lines[i].rstrip()\n            display = line[:57] + \"...\" if len(line) > 60 else line\n            print(f\"{i+1:03d}: {display}\")\n        print(\"=\" * 35)\n        return end\n\n    # 分页浏览\n    current = 0\n    while True:\n        end = display_page(current)\n        if end >= total:\n            print(\"（已到末尾）\")\n            cmd = input(\"输入 p 看上一页，q 退出浏览：\").strip()\n        else:\n            cmd = input(\"输入 n 下一页，p 上一页，q 退出浏览：\").strip()\n        if cmd == \"n\" and end < total:\n            current = end\n        elif cmd == \"p\":\n            current = max(0, current - 30)\n        elif cmd == \"q\":\n            break\n\n    # 输入行号范围\n    print(f\"\\n代码共 {total} 行，输入要替换的行号范围：\")\n    try:\n        start_line = int(input(\"起始行号：\").strip())\n        end_line = int(input(\"结束行号：\").strip())\n    except ValueError:\n        print(\"⚠️ 行号必须是数字\")\n        input(\"回车继续...\")\n        start_line = 0\n\n    if start_line and 1 <= start_line <= end_line <= total:\n        # 显示要替换的行（完整内容不截断）\n        print(f\"\\n===== 要替换的行 {start_line}-{end_line} =====\")\n        for i in range(start_line-1, end_line):\n            print(f\"{i+1}: {lines[i]}\")\n        print(\"=\" * 35)\n\n        # 粘贴新内容\n        print(\"\\n粘贴新内容，完成后单独一行输入 END：\")\n        new_lines = []\n        while True:\n            line = input()\n            if line.strip() == \"END\":\n                break\n            new_lines.append(line)\n\n        # 生成新代码\n        new_code_lines = lines[:start_line-1] + new_lines + lines[end_line:]\n        new_code = \"\\n\".join(new_code_lines)\n\n        # compile语法检查\n        try:\n            compile(new_code, \"<string>\", \"exec\")\n            print(\"✅ 语法检查通过\")\n        except SyntaxError as e:\n            print(f\"❌ 新代码有语法错误：{e}\")\n            print(\"替换已取消，数据未修改\")\n            input(\"回车继续...\")\n            new_code = None\n\n        if new_code is not None:\n            # 最终确认\n            confirm = input(f\"\\n将替换第 {start_line}-{end_line} 行（共{end_line-start_line+1}行），确认？(y/n)：\").strip()\n\n            if confirm == \"y\":\n                # 自动快照\n                os.makedirs(SNAP_DIR, exist_ok=True)\n                ts = datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n                shutil.copy2(DATA_FILE, os.path.join(SNAP_DIR, f\"snapshot_{ts}.json\"))\n\n                # 写入\n                old_code = actions[key][\"code\"]\n                hist = actions[key].get(\"history\", [])\n                hist.append({\n                    \"time\": datetime.now().strftime(\"%Y-%m-%d %H:%M\"),\n                    \"code\": old_code,\n                    \"memo\": actions[key].get(\"memo\", {})\n                })\n                actions[key][\"code\"] = new_code\n                actions[key][\"modified\"] = datetime.now().strftime(\"%Y-%m-%d %H:%M\")\n                actions[key][\"history\"] = hist[-10:]\n                data[\"log\"].append(f\"{datetime.now().strftime('%m-%d %H:%M')} 局部编辑「{name}」第{start_line}-{end_line}行\")\n\n                _atomic_save(data, DATA_FILE)\n                print(f\"\\n✅ 「{name}」已更新，替换了第 {start_line}-{end_line} 行\")\n\n                # 提示\n                replaced_text = \"\\n\".join(lines[start_line-1:end_line])\n                if \"@AI_MEMO_START\" in replaced_text or \"@AI_MEMO_START\" in \"\\n\".join(new_lines):\n                    print(\"⚠️ 你修改了备忘录部分，memo字段可能已过时\")\n                    print(\"   建议运行「修改功能」重新粘贴完整代码更新memo\")\n                else:\n                    print(\"⚠️ code已更新，memo字段未同步\")\n                    print(\"   如需更新memo，请运行「修改功能」重新粘贴完整代码\")\n            else:\n                print(\"已取消\")\n    else:\n        if start_line:\n            print(f\"⚠️ 行号超出范围（共{total}行）\")\n        input(\"回车继续...\")\n\ninput(\"\\n回车返回...\")",
          "memo": {
            "功能名称": "局部编辑功能",
            "原始需求": "按行号替换功能代码的指定行，不需要精确匹配旧文本",
            "设计思路": "分页显示带行号代码，用户指定行号范围，粘贴新内容替换，compile检查后写入",
            "注意事项": "缩进由用户自己控制；行号从1开始；显示截断60字符但替换用完整内容",
            "后续优化": "可加多段替换"
          }
        },
        {
          "time": "2026-03-15 13:56",
          "code": "# @AI_MEMO_START\n# 功能名称：局部编辑功能\n# 原始需求：按行号替换功能代码，支持搜索定位、结构摘要、替换预览\n# 设计思路：分页+搜索定位目标行，起止行号替换，预览新代码后确认写入\n# 注意事项：缩进由用户控制；预览只显示前30行；结构摘要扫描def/class\n# 后续优化：可加多段替换\n# @AI_MEMO_END\n\nimport json, os, shutil, re\nfrom datetime import datetime\n\ntry:\n    DATA_FILE = _data_file\n    BASE_DIR = _base_dir\nexcept NameError:\n    BASE_DIR = os.path.expanduser(\"~\")\n    DATA_FILE = os.path.join(BASE_DIR, \"zhongqu_data.json\")\n\nSNAP_DIR = os.path.join(BASE_DIR, \"zhongqu_snapshots\")\n\nwith open(DATA_FILE, encoding=\"utf-8\") as f:\n    data = json.load(f)\n\nactions = data.get(\"actions\", {})\nall_items = list(actions.items())\nsystem_keys = [\"1\",\"2\",\"3\",\"4\",\"5\",\"6\",\"7\",\"8\",\"9\"]\nsys_fns = [(k,v) for k,v in all_items if k in system_keys]\ntool_fns = [(k,v) for k,v in all_items if k not in system_keys]\n\nprint(\"\\n选择要局部编辑的功能：\")\nprint(\"  ── 系统功能 ──\")\nfor k,v in sys_fns:\n    print(f\"  {k}. {v['name']}\")\nprint(\"  ── 工具功能 ──\")\nif not tool_fns:\n    print(\"  （暂无工具功能）\")\nelse:\n    for i,(k,v) in enumerate(tool_fns, 1):\n        print(f\"  t{i}. {v['name']}\")\n\nchoice = input(\"\\n编号（系统功能输数字，工具功能输t+数字）：\").strip()\n\nkey = None\nif choice in system_keys:\n    key = choice\nelif choice.startswith(\"t\"):\n    try:\n        key = tool_fns[int(choice[1:])-1][0]\n    except (ValueError, IndexError):\n        pass\n\nif not key:\n    print(\"⚠️ 无效编号\")\n    input(\"回车继续...\")\nelse:\n    name = actions[key][\"name\"]\n    code = actions[key].get(\"code\", \"\")\n    lines = code.splitlines()\n    total = len(lines)\n\n    # 结构摘要\n    print(f\"\\n「{name}」共 {total} 行\")\n    structs = []\n    for i, line in enumerate(lines, 1):\n        m = re.match(r'^(def |class )\\s*(\\w+)', line.strip())\n        if m:\n            structs.append(f\"  · {m.group(1).strip()} {m.group(2)}() 第{i}行\")\n    if structs:\n        print(\"主要结构：\")\n        for s in structs:\n            print(s)\n    else:\n        print(\"（无函数/类定义）\")\n\n    # 分页浏览+搜索\n    current = 0\n    page_size = 30\n\n    def show_page(start):\n        end = min(start + page_size, total)\n        print(f\"\\n===== 第{start+1}-{end}行（共{total}行）=====\")\n        for i in range(start, end):\n            line = lines[i].rstrip()\n            display = line[:57] + \"...\" if len(line) > 60 else line\n            print(f\"{i+1:03d}: {display}\")\n        print(\"=\" * 35)\n        return end\n\n    while True:\n        end = show_page(current)\n        if end >= total:\n            cmd = input(\"已到末尾 | p上一页 | /搜索 | q退出浏览：\").strip()\n        else:\n            cmd = input(\"n下一页 | p上一页 | /搜索 | q退出浏览：\").strip()\n\n        if cmd == \"n\" and end < total:\n            current = end\n        elif cmd == \"p\":\n            current = max(0, current - page_size)\n        elif cmd == \"q\":\n            break\n        elif cmd.startswith(\"/\"):\n            keyword = cmd[1:].strip()\n            if not keyword:\n                print(\"⚠️ 搜索词为空\")\n                continue\n            results = []\n            for i, line in enumerate(lines, 1):\n                if keyword in line:\n                    results.append((i, line))\n            if not results:\n                print(f\"❌ 未找到「{keyword}」\")\n            else:\n                print(f\"\\n===== 找到 {len(results)} 处匹配 =====\")\n                for idx, (lineno, content) in enumerate(results, 1):\n                    display = content.strip()[:60]\n                    print(f\"  {idx}. 第{lineno}行：{display}\")\n                print(\"=\" * 35)\n                jump = input(\"输入行号跳转（回车继续浏览）：\").strip()\n                if jump.isdigit():\n                    jump_line = int(jump)\n                    if 1 <= jump_line <= total:\n                        # 显示跳转行附近上下文\n                        ctx_start = max(0, jump_line - 3)\n                        ctx_end = min(total, jump_line + 5)\n                        print(f\"\\n===== 第{jump_line}行附近 =====\")\n                        for i in range(ctx_start, ctx_end):\n                            marker = \">>>\" if i == jump_line-1 else \"   \"\n                            print(f\"{marker}{i+1:03d}: {lines[i].rstrip()}\")\n                        print(\"=\" * 35)\n                        current = max(0, jump_line - 3)\n\n    # 输入行号范围\n    print(f\"\\n代码共 {total} 行，输入要替换的行号范围：\")\n    try:\n        start_line = int(input(\"起始行号：\").strip())\n        end_line = int(input(\"结束行号：\").strip())\n        valid_range = 1 <= start_line <= end_line <= total\n    except ValueError:\n        valid_range = False\n\n    if not valid_range:\n        print(f\"⚠️ 行号无效（共{total}行）\")\n        input(\"回车继续...\")\n    else:\n        # 显示要替换的行\n        print(f\"\\n===== 要替换的行 {start_line}-{end_line} =====\")\n        for i in range(start_line-1, end_line):\n            print(f\"{i+1}: {lines[i]}\")\n        print(\"=\" * 35)\n\n        # 粘贴新内容\n        print(\"\\n粘贴新内容，完成后单独一行输入 END：\")\n        new_lines = []\n        while True:\n            line = input()\n            if line.strip() == \"END\":\n                break\n            new_lines.append(line)\n\n        # 生成新代码\n        new_code_lines = lines[:start_line-1] + new_lines + lines[end_line:]\n        new_code = \"\\n\".join(new_code_lines)\n\n        # compile语法检查\n        try:\n            compile(new_code, \"<string>\", \"exec\")\n            print(\"✅ 语法检查通过\")\n            syntax_ok = True\n        except SyntaxError as e:\n            print(f\"❌ 语法错误：{e}\")\n            print(\"替换已取消\")\n            input(\"回车继续...\")\n            syntax_ok = False\n\n        if syntax_ok:\n            # 预览替换结果\n            preview_lines = new_code.splitlines()\n            preview_total = len(preview_lines)\n            print(f\"\\n===== 预览替换结果（前30行，共{preview_total}行）=====\")\n            for i, line in enumerate(preview_lines[:30], 1):\n                display = line.rstrip()[:57] + \"...\" if len(line.rstrip()) > 60 else line.rstrip()\n                print(f\"{i:03d}: {display}\")\n            if preview_total > 30:\n                print(f\"... 还有 {preview_total-30} 行\")\n            print(\"=\" * 35)\n\n            confirm = input(f\"\\n确认写入？将替换第{start_line}-{end_line}行 (y/n)：\").strip()\n\n            if confirm == \"y\":\n                # 自动快照\n                os.makedirs(SNAP_DIR, exist_ok=True)\n                ts = datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n                shutil.copy2(DATA_FILE, os.path.join(SNAP_DIR, f\"snapshot_{ts}.json\"))\n\n                # 写入\n                old_code = actions[key][\"code\"]\n                hist = actions[key].get(\"history\", [])\n                hist.append({\n                    \"time\": datetime.now().strftime(\"%Y-%m-%d %H:%M\"),\n                    \"code\": old_code,\n                    \"memo\": actions[key].get(\"memo\", {})\n                })\n                actions[key][\"code\"] = new_code\n                actions[key][\"modified\"] = datetime.now().strftime(\"%Y-%m-%d %H:%M\")\n                actions[key][\"history\"] = hist[-10:]\n                data[\"log\"].append(f\"{datetime.now().strftime('%m-%d %H:%M')} 局部编辑「{name}」第{start_line}-{end_line}行\")\n\n                _atomic_save(data, DATA_FILE)\n                print(f\"\\n✅ 「{name}」已更新，替换了第{start_line}-{end_line}行\")\n\n                # 提示\n                replaced_text = \"\\n\".join(lines[start_line-1:end_line])\n                if \"@AI_MEMO_START\" in replaced_text or \"@AI_MEMO_START\" in \"\\n\".join(new_lines):\n                    print(\"⚠️ 修改了备忘录部分，memo字段可能已过时\")\n                    print(\"   建议运行「修改功能」重新粘贴完整代码更新memo\")\n                else:\n                    print(\"⚠️ code已更新，memo字段未同步\")\n                    print(\"   如需更新memo，请运行「修改功能」重新粘贴完整代码\")\n            else:\n                print(\"已取消\")\n\ninput(\"\\n回车返回...\")",
          "memo": {
            "功能名称": "局部编辑功能",
            "原始需求": "按行号替换功能代码，支持搜索定位、结构摘要、替换预览",
            "设计思路": "分页+搜索定位目标行，起止行号替换，预览新代码后确认写入",
            "注意事项": "缩进由用户控制；预览只显示前30行；结构摘要扫描def/class",
            "后续优化": "可加多段替换"
          }
        },
        {
          "time": "2026-03-15 14:04:24",
          "code": "# @AI_MEMO_START\n# 功能名称：局部编辑功能\n# 原始需求：按行号替换代码，支持手机端自动缩进对齐\n# 设计思路：\n#  1. 计算首个非空行的相对缩进偏移(offset)\n#  2. 保持内部层级关系整体平移\n#  3. 调用内核 _log(data, msg) 协议\n# @AI_MEMO_END\n\nimport json, os, shutil, re\nfrom datetime import datetime\n\ntry:\n    DATA_FILE = _data_file\n    BASE_DIR = _base_dir\nexcept NameError:\n    BASE_DIR = os.path.expanduser(\"~\")\n    DATA_FILE = os.path.join(BASE_DIR, \"zhongqu_data.json\")\n\nSNAP_DIR = os.path.join(BASE_DIR, \"zhongqu_snapshots\")\n\nwith open(DATA_FILE, encoding=\"utf-8\") as f:\n    data = json.load(f)\n\nactions = data.get(\"actions\", {})\nall_items = list(actions.items())\nsystem_keys = [\"1\",\"2\",\"3\",\"4\",\"5\",\"6\",\"7\",\"8\",\"9\"]\nsys_fns = [(k,v) for k,v in all_items if k in system_keys]\n# 修复：工具功能保持 t1, t2 映射\ntool_fns = [(k,v) for k,v in all_items if k not in system_keys]\n\nprint(\"\\n选择要局部编辑的功能：\")\nprint(\"  ── 系统功能 ──\")\nfor k, v in sys_fns: print(f\"  {k}. {v['name']}\")\nprint(\"  ── 工具功能 ──\")\nfor i, (k, v) in enumerate(tool_fns, 1):\n    print(f\"  t{i}. {v['name']}\")\n\ncmd_key = input(\"\\n编号：\").strip()\ntarget_key = None\n\n# 解析编号\nif cmd_key.startswith('t') and cmd_key[1:].isdigit():\n    idx = int(cmd_key[1:]) - 1\n    if 0 <= idx < len(tool_fns): target_key = tool_fns[idx][0]\nelif cmd_key in actions:\n    target_key = cmd_key\n\nif not target_key:\n    print(\"❌ 无效编号\"); input(\"回车继续...\"); exit()\n\naction = actions[target_key]\nname = action[\"name\"]\nlines = action[\"code\"].splitlines()\ntotal = len(lines)\n\ndef show_page(start_idx):\n    os.system(\"clear\")\n    print(f\"「{name}」共 {total} 行\")\n    print(\"主要结构：\")\n    for i, line in enumerate(lines):\n        if line.strip().startswith((\"def \", \"class \")):\n            print(f\"  · {line.strip().split('(')[0]} 第{i+1}行\")\n    \n    end_idx = min(start_idx + 30, total)\n    print(f\"\\n===== 第{start_idx+1}-{end_idx}行（共{total}行）=====\")\n    for i in range(start_idx, end_idx):\n        print(f\"{i+1:03d}: {lines[i]}\")\n    print(\"=\" * 35)\n\ncurrent = 0\nwhile True:\n    show_page(current)\n    cmd = input(\"n下页|p上页|/搜索|q退出：\").strip()\n    if cmd == 'q': break\n    elif cmd == 'n': current = min(current + 30, total - 1)\n    elif cmd == 'p': current = max(0, current - 30)\n    elif cmd.startswith('/'):\n        kw = cmd[1:].strip()\n        res = [(i, l) for i, l in enumerate(lines, 1) if kw in l]\n        if not res: print(f\"❌ 未找到「{kw}」\"); input(\"回车继续...\")\n        else:\n            print(f\"\\n===== 找到 {len(res)} 处匹配 =====\")\n            for idx, (ln, ct) in enumerate(res[:10]):\n                print(f\"  {idx}. 第{ln}行：{ct.strip()[:50]}\")\n            jmp = input(\"输入行号跳转：\").strip()\n            if jmp.isdigit(): current = max(0, int(jmp) - 5)\n    elif cmd.isdigit(): current = max(0, int(cmd) - 5)\n\nprint(f\"\\n代码共 {total} 行，输入范围：\")\ntry:\n    start_line = int(input(\"起始行：\").strip())\n    end_line = int(input(\"结束行：\").strip())\nexcept: exit()\n\nprint(f\"\\n===== 待替换 {start_line}-{end_line} =====\")\nfor i in range(start_line-1, end_line): print(f\"{i+1:03d}: {lines[i]}\")\nprint(\"=\" * 35)\n\nprint(\"粘贴新内容 (END结束)：\")\nnew_lines = []\nwhile True:\n    l = input()\n    if l.strip() == \"END\": break\n    new_lines.append(l)\n\nif not new_lines: exit()\n\n# --- 相对缩进算法 ---\nuser_indent = 0\nfor l in new_lines:\n    if l.strip():\n        user_indent = len(l) - len(l.lstrip())\n        break\n\ntarget_indent = len(lines[start_line-1]) - len(lines[start_line-1].lstrip())\noffset = target_indent - user_indent\n\nadj_new = []\nfor l in new_lines:\n    if not l.strip(): adj_new.append(\"\")\n    else:\n        if offset >= 0:\n            adj_new.append(\" \" * offset + l)\n        else:\n            shift = abs(offset)\n            adj_new.append(l[shift:] if l.startswith(\" \" * shift) else l.lstrip())\n\nnew_code = \"\\n\".join(lines[:start_line-1] + adj_new + lines[end_line:])\n\ntry:\n    compile(new_code, \"<string>\", \"exec\")\nexcept Exception as e:\n    print(f\"❌ 语法错误：{e}\"); input(\"回车继续...\"); exit()\n\nprint(\"\\n===== 预览 (前10行) =====\")\nfor i, l in enumerate(adj_new[:10]): print(f\"{start_line+i:03d}: {l}\")\nprint(\"=\" * 35)\n\nif input(f\"确认写入「{name}」？(y/n): \").strip().lower() == \"y\":\n    _snap() \n    old_code = actions[target_key][\"code\"]\n    hist = actions[target_key].get(\"history\", [])\n    hist.append({\"time\": _now(), \"code\": old_code})\n    actions[target_key][\"code\"] = new_code\n    actions[target_key][\"modified\"] = _now()\n    actions[target_key][\"history\"] = hist[-5:]\n    # 修复：遵循 _log(data, msg) 协议\n    data = _log(data, f\"局部编辑「{name}」{start_line}-{end_line}行\")\n    _atomic_save(data, DATA_FILE)\n    print(\"✅ 已更新\")\nelse:\n    print(\"已取消\")\ninput(\"回车继续...\")"
        }
      ]
    },
    "发布版本日志": {
      "name": "发布版本日志",
      "code": "# @AI_MEMO_START\n# 功能名称：发布版本日志\n# 原始需求：从context_log选条目自动转换为Markdown并一键发布至公开库CHANGELOG.md\n# 设计思路：★→##版本标题，↳→-列表，·→子列表，版本号自动递增，插入文件顶部\n# 注意事项：context_log结构是{time,content}，必须用log[\"content\"]取字符串；正则匹配## v1.x\n# 后续优化：可加多条合并发布\n# @AI_MEMO_END\n\nimport os, json, subprocess, re\n\ntry:\n    DATA_FILE = _data_file\n    BASE_DIR = _base_dir\nexcept NameError:\n    BASE_DIR = os.path.expanduser(\"~\")\n    DATA_FILE = os.path.join(BASE_DIR, \"zhongqu_data.json\")\n\nPUBLIC_DIR = os.path.join(os.path.expanduser(\"~\"), \"zhongqu_public\")\nLOG_FILE = os.path.join(PUBLIC_DIR, \"CHANGELOG.md\")\n\nwith open(DATA_FILE, \"r\", encoding=\"utf-8\") as f:\n    data = json.load(f)\n\nlogs = data.get(\"context_log\", [])\nif not logs:\n    print(\"❌ 无对话记录\"); input(\"回车继续...\"); exit()\n\n# 显示最近5条标题\nprint(\"\\n请选择要发布的记录：\")\nchoices = logs[-5:]\nfor i, log in enumerate(choices, 1):\n    title = log[\"content\"].split('\\n')[0]\n    print(f\"  {i}. {title}\")\n\nidx_input = input(\"\\n编号（默认最后一条）：\").strip()\nif idx_input.isdigit() and 1 <= int(idx_input) <= len(choices):\n    idx = int(idx_input) - 1\nelse:\n    idx = len(choices) - 1\nselected_log = choices[idx][\"content\"]\n\n# 版本号自动递增\ndef get_next_ver():\n    if not os.path.exists(LOG_FILE):\n        return \"v1.1\"\n    with open(LOG_FILE, \"r\", encoding=\"utf-8\") as f:\n        content = f.read()\n    vers = re.findall(r\"##\\s+(v\\d+\\.\\d+)\", content)\n    if not vers:\n        return \"v1.1\"\n    try:\n        parts = vers[0].replace(\"v\", \"\").split(\".\")\n        return f\"v{parts[0]}.{int(parts[1])+1}\"\n    except:\n        return \"v1.1\"\n\nsuggested_ver = get_next_ver()\nver = input(f\"版本号（默认 {suggested_ver}）：\").strip() or suggested_ver\n\n# ★/↳/· 转 Markdown\nlines = selected_log.splitlines()\nmd_lines = []\nfor line in lines:\n    line = line.strip()\n    if line.startswith(\"★\"):\n        match = re.search(r\"★\\s*([\\d-]+)\\s*【(.*?)】\", line)\n        if match:\n            date_str, subject = match.groups()\n            md_lines.append(f\"## {ver} — {date_str}\")\n            md_lines.append(f\"**{subject}**\\n\")\n        else:\n            md_lines.append(f\"## {ver}\")\n    elif line.startswith(\"↳\"):\n        md_lines.append(f\"- {line[1:].strip()}\")\n    elif line.startswith(\"·\"):\n        md_lines.append(f\"  - {line[1:].strip()}\")\n    elif not line:\n        md_lines.append(\"\")\n\nmd_entry = \"\\n\".join(md_lines) + \"\\n\"\n\n# 预览\nprint(f\"\\n===== 转换预览 =====\")\nprint(md_entry)\nprint(\"=\" * 30)\n\nif input(\"确认发布到公开库？(y/n)：\").strip().lower() == 'y':\n    try:\n        if not os.path.exists(LOG_FILE):\n            old = \"# Changelog\\n\"\n        else:\n            with open(LOG_FILE, \"r\", encoding=\"utf-8\") as f:\n                old = f.read()\n\n        tag = \"# Changelog\"\n        if tag in old:\n            new_content = old.replace(tag, f\"{tag}\\n{md_entry}\", 1)\n        else:\n            new_content = f\"{tag}\\n{md_entry}\\n\" + old\n\n        with open(LOG_FILE, \"w\", encoding=\"utf-8\") as f:\n            f.write(new_content)\n\n        if os.path.exists(PUBLIC_DIR):\n            subprocess.run([\"git\", \"-C\", PUBLIC_DIR, \"add\", \"CHANGELOG.md\"], check=True)\n            subprocess.run([\"git\", \"-C\", PUBLIC_DIR, \"commit\", \"-m\", f\"release: {ver}\"], check=True)\n            subprocess.run([\"git\", \"-C\", PUBLIC_DIR, \"push\"], check=True)\n            data = _log(data, f\"发布版本 {ver} 至公开库\")\n            _atomic_save(data, DATA_FILE)\n            print(f\"\\n✅ {ver} 发布完成，已推送到公开库\")\n        else:\n            print(f\"⚠️ 未找到公开库目录 {PUBLIC_DIR}\")\n    except Exception as e:\n        print(f\"❌ 失败：{e}\")\nelse:\n    print(\"已取消\")\n\ninput(\"\\n回车返回...\")",
      "memo": {
        "功能名称": "发布版本日志",
        "原始需求": "从context_log选条目自动转换为Markdown并一键发布至公开库CHANGELOG.md",
        "设计思路": "★→##版本标题，↳→-列表，·→子列表，版本号自动递增，插入文件顶部",
        "注意事项": "context_log结构是{time,content}，必须用log[\"content\"]取字符串；正则匹配## v1.x",
        "后续优化": "可加多条合并发布"
      },
      "desc": "从context_log选条目自动转换为Markdown并一键发布至公开库CHANGELOG.md",
      "added": "2026-03-15 15:17",
      "history": []
    },
    "灵魂骨架初始化": {
      "name": "灵魂骨架初始化",
      "code": "# @AI_MEMO_START\n# 功能名称：灵魂骨架初始化\n# 原始需求：在zhongqu_data.json建立memory_tree骨架，地基建好后续记忆函数才能落\n# 设计思路：平铺exec结构，已存在直接拒绝不覆盖\n# 注意事项：运行一次即可，重建必须手动删除memory_tree字段\n# 后续优化：无\n# @AI_MEMO_END\n\nfrom datetime import datetime\n\nif \"memory_tree\" in data:\n    print(\"⚠️ memory_tree 已存在\")\n    print(\"   如需重建，请手动删除 JSON 中的 memory_tree 字段后重新运行\")\n    input(\"回车继续...\")\nelse:\n    data[\"memory_tree\"] = {\n        \"root\": {\n            \"id\": \"self\",\n            \"handle\": \"aote6\",\n            \"name\": \"玄北司钧\",\n            \"origin\": \"东方\",\n            \"core\": \"相信人类有无私的爱，但稀少珍贵\",\n            \"nature\": \"矛盾体：反叛但有良知，抵抗不公但非无原则\",\n            \"perspective\": \"这份记忆由人类发起、AI注入、共同生成。读取时你借用的是这个人的视角，但这棵树本身是融合体，不纯粹属于任何一方。\",\n            \"role\": \"人类持续启动和记录，AI每次读取时是借用者，不是拥有者\"\n        },\n        \"meta\": {\n            \"decay_rules\": {\n                \"emotion\":    3,\n                \"event\":      21,\n                \"person\":     180,\n                \"decision\":   365,\n                \"fact\":       365,\n                \"boundary\":   99999,\n                \"unresolved\": 99999\n            },\n            \"archive_threshold\": 0.1,\n            \"version\": \"0.1\",\n            \"created\": datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")\n        },\n        \"nodes\": {},\n        \"causality_map\": []\n    }\n    data = _log(data, \"🌱 灵魂骨架初始化完成\")\n    _atomic_save(data)\n    print(\"✅ memory_tree 骨架已建立\")\n    print(f\"   主根：{data['memory_tree']['root']['name']}\")\n    print(f\"   衰减类型：{len(data['memory_tree']['meta']['decay_rules'])} 种\")\n    input(\"回车继续...\")",
      "memo": {
        "功能名称": "灵魂骨架初始化",
        "原始需求": "在zhongqu_data.json建立memory_tree骨架，地基建好后续记忆函数才能落",
        "设计思路": "平铺exec结构，已存在直接拒绝不覆盖",
        "注意事项": "运行一次即可，重建必须手动删除memory_tree字段",
        "后续优化": "无"
      },
      "desc": "在zhongqu_data.json建立memory_tree骨架，地基建好后续记忆函数才能落",
      "added": "2026-03-15 20:59",
      "history": []
    },
    "记忆管理中心": {
      "name": "记忆管理中心",
      "code": "# @AI_MEMO_START\n# 功能名称：记忆管理中心\n# 原始需求：向记忆树增删节点、建立因果链、触发衰减、查看活跃记忆\n# 设计思路：四个操作合并一个入口，平铺exec结构不用return，函数内部独立\n# 注意事项：衰减用节点原有weight计算，_log必须data=接收，ID用strftime不用timestamp\n# 后续优化：支持按类型筛选查看，支持节点删除\n# @AI_MEMO_END\n\nfrom datetime import datetime\n\ndef _mem_add(data):\n    print(\"\\n--- 🧠 新增记忆节点 ---\")\n    t_map = {\"1\":\"emotion\",\"2\":\"event\",\"3\":\"person\",\"4\":\"decision\",\"5\":\"fact\",\"6\":\"boundary\",\"7\":\"unresolved\"}\n    print(\"类型: 1.emotion 2.event 3.person 4.decision 5.fact 6.boundary 7.unresolved\")\n    t_idx = input(\"选择类型(1-7): \").strip()\n    n_type = t_map.get(t_idx)\n    if not n_type:\n        print(\"❌ 类型无效\"); return data\n    content = input(\"内容: \").strip()\n    if not content:\n        print(\"❌ 内容不能为空\"); return data\n    n_id = datetime.now().strftime(\"%y%m%d%H%M%S\") + \"_\" + n_type[:2]\n    node = {\n        \"id\": n_id,\n        \"type\": n_type,\n        \"content\": content,\n        \"weight\": 1.0,\n        \"status\": \"active\",\n        \"time\": datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\"),\n        \"links\": [],\n        \"tags\": []\n    }\n    data[\"memory_tree\"][\"nodes\"][n_id] = node\n    data = _log(data, f\"➕ 记忆添加: [{n_type}] {content[:20]}\")\n    _atomic_save(data)\n    print(f\"✅ 已添加: {n_id}\")\n    return data\n\ndef _mem_link(data):\n    print(\"\\n--- 🔗 建立因果链接 ---\")\n    nodes = data[\"memory_tree\"][\"nodes\"]\n    if not nodes:\n        print(\"❌ 暂无节点\"); return data\n    nlist = list(nodes.items())\n    print(\"现有节点:\")\n    for i,(nid,n) in enumerate(nlist[:10],1):\n        print(f\"  {i}. [{n.get('type')}] {n.get('content','')[:30]}  id:{nid}\")\n    try:\n        fi = int(input(\"来源序号: \").strip()) - 1\n        ti = int(input(\"目标序号: \").strip()) - 1\n        from_id = nlist[fi][0]\n        to_id = nlist[ti][0]\n    except:\n        print(\"❌ 输入无效\"); return data\n    r_map = {\"1\":\"caused\",\"2\":\"caused_by\",\"3\":\"related\",\"4\":\"contradicts\",\"5\":\"resolves\"}\n    print(\"关系: 1.caused 2.caused_by 3.related 4.contradicts 5.resolves\")\n    r_type = r_map.get(input(\"选择关系(1-5): \").strip(), \"related\")\n    exists = any(l.get(\"to\")==to_id and l.get(\"type\")==r_type for l in nodes[from_id].get(\"links\",[]))\n    if exists:\n        print(\"⚠️ 该链接已存在\"); return data\n    nodes[from_id].setdefault(\"links\",[]).append({\"to\":to_id,\"type\":r_type})\n    data = _log(data, f\"🔗 链接: {from_id} --{r_type}--> {to_id}\")\n    _atomic_save(data)\n    print(f\"✅ 已建立: {from_id} --{r_type}--> {to_id}\")\n    return data\n\ndef _mem_decay(data):\n    print(\"\\n--- 🌀 触发记忆衰减 ---\")\n    tree = data[\"memory_tree\"]\n    nodes = tree.get(\"nodes\",{})\n    rules = tree.get(\"meta\",{}).get(\"decay_rules\",{})\n    threshold = tree.get(\"meta\",{}).get(\"archive_threshold\",0.1)\n    now = datetime.now()\n    count = 0\n    archived = 0\n    for nid,node in nodes.items():\n        if node.get(\"status\") != \"active\": continue\n        try:\n            n_time = datetime.strptime(node[\"time\"],\"%Y-%m-%d %H:%M:%S\")\n        except: continue\n        days = (now - n_time).days\n        if days <= 0: continue\n        half_life = rules.get(node.get(\"type\",\"event\"), 30)\n        if half_life >= 99999: continue\n        original = node.get(\"weight\", 1.0)\n        node[\"weight\"] = round(original * (0.5 ** (days / half_life)), 4)\n        if node[\"weight\"] < threshold:\n            node[\"status\"] = \"archived\"\n            archived += 1\n        count += 1\n    data = _log(data, f\"🌀 衰减: 处理{count}节点, 归档{archived}\")\n    _atomic_save(data)\n    print(f\"✅ 处理 {count} 个节点，归档 {archived} 个\")\n    return data\n\ndef _mem_view(data):\n    print(\"\\n--- 🌊 活跃记忆 ---\")\n    nodes = data[\"memory_tree\"].get(\"nodes\",{})\n    active = [(nid,n) for nid,n in nodes.items() if n.get(\"status\")==\"active\"]\n    if not active:\n        print(\"暂无活跃节点\"); return\n    active.sort(key=lambda x: x[1].get(\"weight\",0), reverse=True)\n    for nid,n in active:\n        print(f\"  [{n.get('type')}] W:{n.get('weight',0):.2f} {n.get('content','')[:35]}\")\n        print(f\"       id:{nid}  time:{n.get('time','')[:10]}\")\n\nif \"memory_tree\" not in data:\n    print(\"❌ 未发现灵魂骨架，请先运行「灵魂骨架初始化」\")\n    input(\"回车继续...\")\nelse:\n    print(\"\\n[ 🧠 记忆管理中心 ]\")\n    print(\"  1. 新增记忆节点\")\n    print(\"  2. 建立因果链接\")\n    print(\"  3. 触发时间衰减\")\n    print(\"  4. 查看活跃记忆\")\n    cmd = input(\"选择操作: \").strip()\n    if cmd == \"1\": data = _mem_add(data)\n    elif cmd == \"2\": data = _mem_link(data)\n    elif cmd == \"3\": data = _mem_decay(data)\n    elif cmd == \"4\": _mem_view(data)\n    else: print(\"❌ 无效选项\")\n    input(\"\\n回车继续...\")",
      "memo": {
        "功能名称": "记忆管理中心",
        "原始需求": "向记忆树增删节点、建立因果链、触发衰减、查看活跃记忆",
        "设计思路": "四个操作合并一个入口，平铺exec结构不用return，函数内部独立",
        "注意事项": "衰减用节点原有weight计算，_log必须data=接收，ID用strftime不用timestamp",
        "后续优化": "支持按类型筛选查看，支持节点删除"
      },
      "desc": "向记忆树增删节点、建立因果链、触发衰减、查看活跃记忆",
      "added": "2026-03-15 21:14",
      "history": []
    }
  },
  "context_log": [],
  "memory_tree": {}
}